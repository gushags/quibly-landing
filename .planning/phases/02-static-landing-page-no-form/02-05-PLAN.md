---
phase: 02-static-landing-page-no-form
plan: 05
type: execute
wave: 4
depends_on:
  - 04
revision: reviews-2026-04-27
files_modified:
  - .lighthouserc.json
  - .github/workflows/lighthouse.yml
  - package.json
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
tags:
  - lighthouse-ci
  - github-actions
  - perf-gate
  - vercel-preview
  - branch-protection
  - human-verify

must_haves:
  truths:
    - "Lighthouse mobile performance score is ≥0.90 on the composed `/` page (PERF-01), verified by LHCI running locally before CI wires up."
    - "CLS is <0.1 (PERF-02) — no layout-shifting elements from font swap or gradient positioning."
    - "No render-blocking third-party scripts on first paint (PERF-03) — assertion is `error`-level (D-33), not `warn`. Phase 2 ships zero third-party JS so the gate is green; if a future phase adds one, the gate fails HARD instead of merely warning."
    - "Zero `'use client'` directives exist in `app/`, `components/sections/`, or `components/quibs/` — the server-component invariant from Plans 02-01..04 is still intact at the LHCI gate."
    - "The Lighthouse CI gate runs on every PR AND blocks merge — branch protection requires the `Lighthouse CI / lighthouse` status check (D-34)."
  artifacts:
    - path: ".lighthouserc.json"
      provides: "LHCI configuration — mobile preset, 3 runs, error-level performance + CLS + render-blocking-resources assertions, warn-level a11y + best-practices + SEO"
      contains: "\"render-blocking-resources\": [\"error\""
    - path: ".github/workflows/lighthouse.yml"
      provides: "GitHub Actions workflow — waits for Vercel preview, runs LHCI, uploads artifacts"
      contains: "treosh/lighthouse-ci-action@v12"
  key_links:
    - from: ".github/workflows/lighthouse.yml"
      to: ".lighthouserc.json"
      via: "`configPath: .lighthouserc.json` in the `treosh/lighthouse-ci-action` step"
      pattern: "configPath"
    - from: ".github/workflows/lighthouse.yml"
      to: "secrets.VERCEL_TOKEN"
      via: "`env: VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}` — encrypted GitHub secret, never echoed, never logged, never written to artifact"
      pattern: "VERCEL_TOKEN"
    - from: "GitHub repository → Settings → Branches → main → Branch protection rules"
      to: "Lighthouse CI / lighthouse status check"
      via: "Required status check entry — D-34. Without this, the gate runs but cannot actually block merge."
      pattern: "branch protection"
  decision_refs:
    - "D-29: Phase 2 wires a Lighthouse CI workflow on PRs using @lhci/cli GitHub Action — enforces performance ≥0.90, cls <0.1, mobile profile, runs against the Vercel preview URL, fails the PR if thresholds drop"
    - "D-33 (post-review): `render-blocking-resources` assertion promoted from `warn` to `error` — aligns the gate with SC #4's unconditional wording"
    - "D-34 (post-review): branch protection on `main` requires the `Lighthouse CI / lighthouse` status check — without this, the gate runs but cannot actually block merge"
---

<objective>
Wire the Lighthouse CI gate that asserts Lighthouse mobile performance ≥0.90, CLS <0.1, AND zero render-blocking resources (D-33 — error-level, not warn-level) on every PR's Vercel preview URL, AND make this check a REQUIRED status check on `main` so it actually blocks merges (D-34). Verify the gate locally before committing.

Purpose: Without this plan, "Lighthouse mobile ≥90" (ROADMAP Phase 2 SC #4) is a claim, not a proof. Without D-34's branch protection, the workflow runs but the gate is advisory. With D-33's promotion of `render-blocking-resources` to `error`, the gate enforces SC #4's unconditional "no render-blocking third-party scripts on first paint" wording.

Output:
1. `.lighthouserc.json` (LHCI configuration with D-33's error-level promotion).
2. `.github/workflows/lighthouse.yml` (CI workflow).
3. A confirmed local LHCI run against the built page.
4. A confirmed GitHub secret (`VERCEL_TOKEN`).
5. **NEW: A confirmed branch protection rule on `main` requiring the LHCI status check (D-34).**
6. A green Lighthouse CI job on a test PR.

**Post-review note (revision: reviews-2026-04-27):**
- D-33 promotes `render-blocking-resources` from `warn` to `error` (concern #8).
- D-34 adds branch protection as an explicit deliverable inside Task 4's checkpoint (concern #5).
- Per the cross-AI review, Gemini's "Vercel token security" concern is explicitly addressed in this plan's threat model — `VERCEL_TOKEN` is referenced ONLY via `secrets.VERCEL_TOKEN` (encrypted at rest, masked in logs by GitHub Actions, never echoed, never written to any artifact). This is correct posture; no code change is needed for that concern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-landing-page-no-form/02-CONTEXT.md
@.planning/phases/02-static-landing-page-no-form/02-RESEARCH.md
@.planning/phases/02-static-landing-page-no-form/02-VALIDATION.md
@.planning/phases/02-static-landing-page-no-form/02-REVIEWS.md
@.planning/phases/02-static-landing-page-no-form/02-04-SUMMARY.md
@CLAUDE.md
@package.json
@app/page.tsx

<interfaces>
<!-- No new code interfaces — this plan creates config files and a CI workflow.
     The LHCI assertion targets the already-built static page at localhost:3000 (or Vercel preview).
     The only runtime interface is the LHCI CLI itself: -->

LHCI CLI (installed as devDependency @lhci/cli):
  npx @lhci/cli autorun --config=.lighthouserc.json

  Reads: .lighthouserc.json (ci.collect.* + ci.assert.* + ci.upload.*)
  Writes: .lighthouseci/ temporary directory (HTML reports, JSON results)
  Returns: exit 0 if all "error"-level assertions pass; exit 1 if any fail

patrickedqvist/wait-for-vercel-preview@v1.3.1 (GitHub Action):
  Outputs: steps.vercel-preview.outputs.url — the Vercel preview URL
  Requires: secrets.VERCEL_TOKEN in env; secrets.GITHUB_TOKEN in `token` input

treosh/lighthouse-ci-action@v12 (GitHub Action):
  Inputs: urls, configPath, uploadArtifacts, temporaryPublicStorage
  Env:    LHCI_GITHUB_APP_TOKEN (optional — enables PR status annotations)
  Posts:  Lighthouse HTML report to temporary public storage; uploads as GitHub Actions artifact
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `.lighthouserc.json` (with D-33 error-level render-blocking-resources) and add `@lhci/cli` devDependency</name>
  <files>.lighthouserc.json, package.json</files>
  <read_first>
    - package.json (confirm `@lhci/cli` is not already present in devDependencies)
    - .planning/phases/02-static-landing-page-no-form/02-RESEARCH.md § Lighthouse CI Configuration
    - .planning/phases/02-static-landing-page-no-form/02-CONTEXT.md D-29, D-33 (post-review render-blocking-resources promotion), CD-05
    - .planning/phases/02-static-landing-page-no-form/02-REVIEWS.md § Consensus Summary item #8 (promote render-blocking-resources from warn to error)
    - .planning/phases/02-static-landing-page-no-form/02-VALIDATION.md § Wave 0 Requirements
  </read_first>
  <action>
    **Sub-step 1 — Add `@lhci/cli` as a devDependency:**

    Run in the repo root:
    ```bash
    npm install -D @lhci/cli
    ```

    This adds `"@lhci/cli"` to `devDependencies` in `package.json` and updates `package-lock.json`. After install, verify the version landed in devDependencies (expected `^0.15.x` or higher).

    **Sub-step 2 — Create `.lighthouserc.json`:**

    Create the file at the root of the repo with this EXACT content (note: `render-blocking-resources` is `"error"`, NOT `"warn"`, per D-33 post-review):

    ```json
    {
      "ci": {
        "collect": {
          "settings": {
            "preset": "mobile"
          },
          "numberOfRuns": 3
        },
        "assert": {
          "assertions": {
            "categories:performance": ["error", { "minScore": 0.90 }],
            "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }],
            "render-blocking-resources": ["error", { "maxLength": 0 }],
            "categories:accessibility": ["warn", { "minScore": 0.90 }],
            "categories:best-practices": ["warn", { "minScore": 0.90 }],
            "categories:seo": ["warn", { "minScore": 0.80 }]
          }
        },
        "upload": {
          "target": "temporary-public-storage"
        }
      }
    }
    ```

    Assertion semantics (per D-29, D-33, and CD-05):
    - `categories:performance >= 0.90` is `"error"` — fails PR. PERF-01.
    - `cumulative-layout-shift maxNumericValue: 0.1` is `"error"` — fails PR. PERF-02.
    - **`render-blocking-resources maxLength: 0` is `"error"` (D-33, post-review) — fails PR. PERF-03.** Phase 2 ships zero render-blocking JS, so the gate is green on day 1; if a future phase adds a render-blocking script, the gate fires HARD instead of warning.
    - `categories:accessibility >= 0.90` is `"warn"` — advisory per CD-05.
    - `categories:best-practices >= 0.90` is `"warn"` — advisory per CD-05.
    - `categories:seo >= 0.80` is `"warn"` (not 0.90) — Phase 5 finalizes OG/title/description.

    `numberOfRuns: 3` takes the median across three runs to reduce noise. (The cross-AI review's Gemini reviewer suggested `5` for additional CI smoothing — left at `3` per the review's "non-blocking suggestion" guidance; bump only if observed CI flakiness becomes a problem.)

    Do NOT add a `"url"` in `ci.collect` — the URL is provided at runtime.

    Do NOT add `"startServerCommand"` or `"staticDistDir"`.
  </action>
  <verify>
    <automated>test -f .lighthouserc.json &amp;&amp; node -e "const c=require('./.lighthouserc.json');const a=c.ci.assert.assertions;if(a['categories:performance'][0]!=='error'||a['categories:performance'][1].minScore!==0.90)throw new Error('perf assertion wrong');if(a['cumulative-layout-shift'][0]!=='error'||a['cumulative-layout-shift'][1].maxNumericValue!==0.1)throw new Error('cls assertion wrong');if(a['render-blocking-resources'][0]!=='error'||a['render-blocking-resources'][1].maxLength!==0)throw new Error('render-blocking-resources must be error level with maxLength 0 per D-33');if(c.ci.collect.numberOfRuns!==3)throw new Error('numberOfRuns wrong');console.log('config valid')" &amp;&amp; node -e "const p=require('./package.json');if(!p.devDependencies['@lhci/cli'])throw new Error('@lhci/cli missing from devDependencies');console.log('@lhci/cli present:', p.devDependencies['@lhci/cli'])"</automated>
  </verify>
  <acceptance_criteria>
    - `.lighthouserc.json` exists at the repo root
    - `node -e "require('./.lighthouserc.json')"` exits 0 (valid JSON)
    - `categories:performance` assertion is `["error", { "minScore": 0.90 }]`
    - `cumulative-layout-shift` assertion is `["error", { "maxNumericValue": 0.1 }]`
    - **(Review concern #8 — D-33):** `render-blocking-resources` assertion is `["error", { "maxLength": 0 }]` (NOT `"warn"`)
    - `ci.collect.numberOfRuns` is `3`
    - `ci.collect.settings.preset` is `"mobile"`
    - `ci.upload.target` is `"temporary-public-storage"`
    - `package.json` `devDependencies` contains `"@lhci/cli"` with a caret range
    - `package-lock.json` updated (confirms `npm install -D` ran)
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>
    `.lighthouserc.json` exists with three error-level assertions (performance, CLS, render-blocking-resources per D-33); `@lhci/cli` is in `devDependencies`. The config is parseable JSON and matches RESEARCH.md + post-review D-33.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Local LHCI verification run against `next start`</name>
  <files></files>
  <read_first>
    - .lighthouserc.json (Task 1 output — confirm the config is in place before running, AND that `render-blocking-resources` is at `"error"` level)
    - .planning/phases/02-static-landing-page-no-form/02-04-SUMMARY.md
    - .planning/phases/02-static-landing-page-no-form/02-RESEARCH.md § Common Pitfalls #6 (running LHCI against localhost vs Vercel preview)
    - .planning/phases/02-static-landing-page-no-form/02-VALIDATION.md § Sampling Rate
  </read_first>
  <action>
    This task runs LHCI locally against `next start` to verify the page passes ALL THREE error-level assertions (performance, CLS, render-blocking-resources per D-33) before wiring the CI gate. It does NOT produce a committed file.

    **Steps:**

    1. **PRE-FLIGHT — verify zero-client-JS invariant (T-02-01 mitigation, runs BEFORE LHCI):**
       ```bash
       grep -rn "use client" app/ components/sections/ components/quibs/ && echo "FAIL: 'use client' found — Phase 2 invariant violated" && exit 1 || echo "OK: zero use-client directives"
       ```
       This MUST exit 0 (no matches found, exit 1 from grep, second branch executes). If `'use client'` is found anywhere in the searched paths, return `## CHECKPOINT REACHED` and route back to the offending plan.

    2. Run a production build:
       ```bash
       npm run build
       ```
       Confirm exit 0.

    3. Start the Next.js production server in the background:
       ```bash
       npm run start &
       SERVER_PID=$!
       sleep 3
       ```

    4. Run LHCI:
       ```bash
       npx @lhci/cli autorun --config=.lighthouserc.json --collect.url=http://localhost:3000
       ```

    5. Kill the background server:
       ```bash
       kill $SERVER_PID 2>/dev/null
       ```

    6. Read LHCI's output:
       - Scan for the assertion summary lines.
       - If ALL THREE error-level assertions show ✅ — proceed to Task 3.
       - If ANY error-level assertion shows ❌ — return `## CHECKPOINT REACHED` so the orchestrator can route the failure back to the section/composition plans.

    **Expected scores for Phase 2 (pure static RSC, zero client JS, Next.js 16.2 on localhost):**
    - Performance: 92–98
    - CLS: 0–0.02
    - Render-blocking resources: 0 — Phase 2 ships only self-hosted fonts via `next/font/google`, no third-party scripts. With D-33 promoting this to `error`-level, any future addition of a render-blocking script will be caught immediately.

    **If performance is 85–89 on localhost:** Investigate (LCP element identity, JS execution time, render-blocking resources, gradient containment).

    Add `.lighthouseci/` to `.gitignore` as part of this task:
    ```bash
    grep -F '.lighthouseci/' .gitignore || echo '.lighthouseci/' >> .gitignore
    ```

    Verify `.gitignore` now contains the entry.
  </action>
  <verify>
    <automated>grep -F '.lighthouseci/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `.lighthouseci/`
    - `grep -rn "use client" app/ components/sections/ components/quibs/` returns zero matches (T-02-01 mitigation, runs before LHCI)
    - LHCI run exits 0 — ALL THREE error-level assertions pass:
      - `categories:performance ≥ 0.90` shows ✅
      - `cumulative-layout-shift < 0.1` shows ✅
      - **(Review concern #8 — D-33) `render-blocking-resources maxLength: 0` shows ✅**
    - The LHCI output shows the `<h1>` headline as the LCP element (not the mascot wrapper div, not the sub-headline `<p>`)
    - `npm run build` exits 0 before the LHCI run
    - The `.lighthouseci/` directory is NOT committed (confirmed via `git status`)

    **CHECKPOINT CONDITION:** If any of the THREE error-level assertions fails, DO NOT proceed to Task 3. Return `## CHECKPOINT REACHED` with the LHCI failure output, the LCP element reported, and the top Lighthouse opportunity items.
  </acceptance_criteria>
  <done>
    Local LHCI run exits 0 with all three error-level assertions passing (perf, CLS, render-blocking-resources per D-33); `.lighthouseci/` added to `.gitignore`. The composed page provably scores Lighthouse mobile ≥90 with zero render-blocking resources before any CI configuration is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create `.github/workflows/lighthouse.yml`</name>
  <files>.github/workflows/lighthouse.yml</files>
  <read_first>
    - .planning/phases/02-static-landing-page-no-form/02-RESEARCH.md § GitHub Actions Workflow
    - .planning/phases/02-static-landing-page-no-form/02-CONTEXT.md D-29 (workflow target), D-34 (branch protection requirement — the workflow must produce a recognizable status check name)
    - .planning/phases/02-static-landing-page-no-form/02-RESEARCH.md § Common Pitfalls #6
    - .planning/REQUIREMENTS.md PERF-01, PERF-02, PERF-03
    - .planning/phases/02-static-landing-page-no-form/02-REVIEWS.md (note Gemini's Vercel-token concern is a misread of GitHub Secrets; see threat model below)
  </read_first>
  <action>
    Create the directory `.github/workflows/` if it does not exist, then create `.github/workflows/lighthouse.yml` with this EXACT content (verbatim from RESEARCH.md):

    ```yaml
    # Source: treosh/lighthouse-ci-action v12 + patrickedqvist/wait-for-vercel-preview
    # Status check name: "Lighthouse CI / lighthouse" — the value used by D-34's branch protection.
    name: Lighthouse CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      lighthouse:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Wait for Vercel Preview
            id: vercel-preview
            uses: patrickedqvist/wait-for-vercel-preview@v1.3.1
            with:
              token: ${{ secrets.GITHUB_TOKEN }}
              max_timeout: 300
            env:
              VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

          - name: Run Lighthouse CI
            uses: treosh/lighthouse-ci-action@v12
            with:
              urls: ${{ steps.vercel-preview.outputs.url }}
              configPath: .lighthouserc.json
              uploadArtifacts: true
              temporaryPublicStorage: true
            env:
              LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
    ```

    Line-by-line rationale:
    - `name: Lighthouse CI` — combined with the job name `lighthouse` produces the GitHub-rendered status check name `Lighthouse CI / lighthouse`. THIS IS THE EXACT STRING D-34's branch protection rule references — DO NOT rename either the workflow or the job.
    - `on: pull_request: branches: [main]` — runs on every PR targeting `main`. This is the gate that matters for blocking merge.
    - `patrickedqvist/wait-for-vercel-preview@v1.3.1` — polls the Vercel API. The `VERCEL_TOKEN` secret goes in `env:` (NOT `with:`) per this action's documented usage AND per security best practice (env vars are masked in logs; `with:` inputs sometimes appear in workflow metadata).
    - `treosh/lighthouse-ci-action@v12` — runs LHCI against the Vercel preview URL.

    **Required secrets:**
    - `VERCEL_TOKEN` — must be set as a repo secret before the action can poll Vercel. Stored as an encrypted GitHub secret. Set up in Task 4's checkpoint.
    - `LHCI_GITHUB_APP_TOKEN` — optional; enables rich PR annotations.
    - `GITHUB_TOKEN` — automatically provided.

    **The `Lighthouse CI / lighthouse` status check name is load-bearing for D-34** — Task 4 references this exact string when configuring branch protection. If the workflow `name:` or job key changes, D-34's branch-protection rule must update in lockstep (otherwise the rule won't match and the gate becomes advisory).
  </action>
  <verify>
    <automated>test -f .github/workflows/lighthouse.yml &amp;&amp; grep -F 'treosh/lighthouse-ci-action@v12' .github/workflows/lighthouse.yml &amp;&amp; grep -F 'patrickedqvist/wait-for-vercel-preview@v1.3.1' .github/workflows/lighthouse.yml &amp;&amp; grep -F 'configPath: .lighthouserc.json' .github/workflows/lighthouse.yml &amp;&amp; grep -F 'VERCEL_TOKEN' .github/workflows/lighthouse.yml &amp;&amp; grep -F 'max_timeout: 300' .github/workflows/lighthouse.yml &amp;&amp; grep -F 'name: Lighthouse CI' .github/workflows/lighthouse.yml &amp;&amp; grep -E '^\s*lighthouse:\s*$' .github/workflows/lighthouse.yml &amp;&amp; python3 -c "content=open('.github/workflows/lighthouse.yml').read(); print('YAML basic check OK') if 'lighthouse:' in content and 'runs-on:' in content else exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/lighthouse.yml` exists
    - `grep -F 'treosh/lighthouse-ci-action@v12' .github/workflows/lighthouse.yml` returns 1 match
    - `grep -F 'patrickedqvist/wait-for-vercel-preview@v1.3.1' .github/workflows/lighthouse.yml` returns 1 match
    - `grep -F 'configPath: .lighthouserc.json' .github/workflows/lighthouse.yml` returns 1 match
    - `grep -F 'VERCEL_TOKEN' .github/workflows/lighthouse.yml` returns at least 1 match
    - `grep -F 'max_timeout: 300' .github/workflows/lighthouse.yml` returns 1 match
    - `grep -F 'uploadArtifacts: true' .github/workflows/lighthouse.yml` returns 1 match
    - `grep -F 'temporaryPublicStorage: true' .github/workflows/lighthouse.yml` returns 1 match
    - `grep -F 'LHCI_GITHUB_APP_TOKEN' .github/workflows/lighthouse.yml` returns 1 match
    - `grep -F 'actions/checkout@v4' .github/workflows/lighthouse.yml` returns 1 match
    - **(D-34 — branch-protection name match):** `grep -F 'name: Lighthouse CI' .github/workflows/lighthouse.yml` returns 1 match (workflow name)
    - **(D-34 — branch-protection name match):** the job key is `lighthouse:` (verified by grep `^  lighthouse:` — produces the status check name `Lighthouse CI / lighthouse`)
    - The `on:` block includes `push: branches: [main]` AND `pull_request: branches: [main]`
    - File contains NO `localhost` references
    - **(Threat model T-02-02):** `grep -E 'echo.*VERCEL_TOKEN|cat.*VERCEL_TOKEN|>>?.*VERCEL_TOKEN' .github/workflows/lighthouse.yml` returns ZERO matches (token is never echoed to logs or written to files)
  </acceptance_criteria>
  <done>
    `.github/workflows/lighthouse.yml` exists verbatim per RESEARCH.md content, with the workflow name and job key configured to produce the `Lighthouse CI / lighthouse` status check name (which D-34's branch protection will reference). `VERCEL_TOKEN` is referenced via `env:` only — never echoed, never logged, never written to artifacts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Add `VERCEL_TOKEN` GitHub secret, configure D-34 branch protection, and confirm CI gate fires + blocks</name>
  <what-built>
    Plans 02-05 Tasks 1–3 created:
    - `.lighthouserc.json` with three error-level assertions (performance ≥0.90, CLS <0.1, render-blocking-resources ≤0 per D-33), 3 runs, mobile preset
    - `@lhci/cli` added to devDependencies and a local LHCI run was verified to pass
    - `.github/workflows/lighthouse.yml` with the CI workflow producing the `Lighthouse CI / lighthouse` status check

    The CI gate cannot run against the Vercel preview URL without `VERCEL_TOKEN` as a GitHub repository secret (Step 1–2 below). The CI gate also cannot ACTUALLY BLOCK MERGES without a branch protection rule on `main` requiring this status check (Step 3 below — **D-34**). Both are one-time manual GitHub UI steps.

    Optional but recommended: Installing the `lighthouse-ci` GitHub App enables rich per-assertion PR annotations.
  </what-built>
  <how-to-verify>
    **Step 1 — Get the Vercel token (if you don't already have one):**
    - Go to: https://vercel.com/account/tokens
    - Click "Create Token"
    - Name: `quibly-landing-lhci`
    - Scope: Your personal team (where the quibly-landing project lives)
    - Expiration: No expiry (or 1 year — your preference)
    - Copy the token value immediately (it's shown once)

    **Step 2 — Add the token as a GitHub repository secret:**
    - Go to: https://github.com/gushags/quibly-landing/settings/secrets/actions
    - Name: `VERCEL_TOKEN` (exact uppercase, no trailing space)
    - Value: Paste the Vercel token from Step 1
    - Click "Add secret"

    **Step 3 — Configure branch protection on `main` to REQUIRE the Lighthouse CI status check (D-34 — concern #5):**
    - Go to: https://github.com/gushags/quibly-landing/settings/branches
    - Under "Branch protection rules" → if no rule exists for `main`, click "Add branch protection rule"; otherwise click the existing rule
    - **Branch name pattern:** `main`
    - Enable: **Require status checks to pass before merging**
    - Enable: **Require branches to be up to date before merging** (recommended, not strictly required)
    - In the "Search for status checks" field, type: `Lighthouse CI / lighthouse`
      - Note: the status check only appears in the dropdown AFTER the workflow has run at least once on a recent PR or push. If it does NOT appear, push a no-op commit to a feature branch first to trigger the workflow, then return to this step.
    - Click the entry to add it as a required check
    - Click "Save changes"
    - Verify: navigate back to the rule and confirm `Lighthouse CI / lighthouse` is listed under "Required status checks"

    **Step 4 — Optional: Install the Lighthouse CI GitHub App for richer PR annotations:**
    - Go to: https://github.com/apps/lighthouse-ci
    - Click "Install" → select `gushags/quibly-landing`
    - After installation, the app generates a token — copy it
    - Add it as a secret: Name `LHCI_GITHUB_APP_TOKEN`, paste the token

    **Step 5 — Verify `VERCEL_TOKEN` is set:**
    - Run: `gh secret list` (if `gh` CLI is installed) — should show `VERCEL_TOKEN`
    - OR: navigate to GitHub repo → Settings → Secrets and variables → Actions → confirm `VERCEL_TOKEN` appears

    **Step 6 — Push the Plan 02-05 changes and confirm the CI gate fires:**
    - Commit Tasks 1–3 (`.lighthouserc.json`, `.github/workflows/lighthouse.yml`, `package.json`+`package-lock.json` changes, `.gitignore` change)
    - Push to a feature branch (do NOT push directly to `main` — the goal is to verify the gate blocks)
    - Open a PR targeting `main`
    - On the GitHub Actions tab, confirm the `Lighthouse CI` workflow appears and runs
    - Wait for the `Wait for Vercel Preview` step to complete
    - Confirm the `Run Lighthouse CI` step completes and shows the HTML report URL

    **Step 7 — Confirm the gate shows green AND is required:**
    - In the Actions run, expand the `Run Lighthouse CI` step
    - Look for: `✅ categories:performance.minScore`, `✅ cumulative-layout-shift.maxNumericValue`, AND `✅ render-blocking-resources` (D-33 — three error-level checks, all green)
    - Navigate back to the PR page
    - In the "Checks" or "Merge box" area, confirm:
      - `Lighthouse CI / lighthouse — Required` appears in the merge requirements
      - The "Merge pull request" button is ENABLED (because all required checks pass)
    - To prove the gate BLOCKS: temporarily disable the workflow (rename the job key in `.github/workflows/lighthouse.yml` to a non-required name, push) and confirm the merge button reverts to BLOCKED with "Required statuses must pass before merging" — then revert this experiment commit.

    **If the gate fails on CI but passed locally:** Vercel preview scores ~5 points lower than localhost. Document the CI failure output in your response.

    **If `Wait for Vercel Preview` times out:**
    - Verify Vercel is connected to this GitHub repo
    - Verify `VERCEL_TOKEN` was saved correctly
    - Verify `.vercel/project.json` exists in the repo (it was created in Phase 1)

    **If the `Lighthouse CI / lighthouse` status check does NOT appear in the branch-protection dropdown:**
    - This means the workflow has not run yet on a tracked branch. Push a commit to ANY branch to trigger it, then return to Step 3.
  </how-to-verify>
  <resume-signal>
    Type **"approved"** if:
    - `VERCEL_TOKEN` is set as a GitHub secret (confirmed via `gh secret list` or GitHub UI) — Step 5
    - **Branch protection rule on `main` requires the `Lighthouse CI / lighthouse` status check — Step 3, D-34, concern #5**
    - The Lighthouse CI workflow ran on the pushed commit — Step 6
    - All THREE error-level assertions pass (performance, CLS, render-blocking-resources show ✅) — Step 7
    - The HTML report is linked in the CI step output
    - The PR's merge button correctly enforces the required status check (enabled when green; blocked when failed)

    Otherwise describe:
    - Which step failed (1–7)
    - The exact error or CI output
    - The Lighthouse score values if the gate ran but failed
    - Whether the branch protection rule was successfully added or not
    so a remediation plan can be created before closing Phase 2.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Vercel API | `VERCEL_TOKEN` travels as an env var inside the GitHub Actions VM. It is encrypted at rest in GitHub Secrets, masked in logs by GitHub Actions, and never echoed/logged/written to any artifact by the workflow as authored. |
| GitHub Actions runner → LHCI temporary-public-storage | Lighthouse HTML reports are uploaded to a public Google Cloud Storage bucket. Reports contain only performance metrics and element selectors — no secrets, no user data, no auth tokens. |
| GitHub Actions → public Vercel preview URL | The preview URL is unauthenticated. No auth tokens are sent to the preview URL; LHCI only performs GET requests. |
| Repo committer → `.lighthouserc.json` | Public file with no secrets. Contains only assertion thresholds and run configuration. |
| Repo committer → `.github/workflows/lighthouse.yml` | Public file. References secrets via `${{ secrets.* }}` — never inlines actual secret values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `components/sections/`, `app/`, `components/quibs/` | mitigate | Task 2's local LHCI run is preceded by a pre-flight `grep -rn "use client" app/ components/sections/ components/quibs/` gate. If any match is found, LHCI is not run — the executor returns `## CHECKPOINT REACHED`. Enforces the zero-client-JS Phase 2 invariant at the gate layer. |
| T-02-02 | Information Disclosure (secret-handling) | `secrets.VERCEL_TOKEN` in `.github/workflows/lighthouse.yml` | mitigate | `VERCEL_TOKEN` is referenced ONLY via `env: VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}` — never via `with:` (which can appear in workflow metadata), never echoed (`echo $VERCEL_TOKEN`), never written to any artifact (`> some-file`), never piped (`| tee`). Acceptance criteria explicitly grep `'echo.*VERCEL_TOKEN|cat.*VERCEL_TOKEN|>>?.*VERCEL_TOKEN'` and require zero matches. The token has the minimum scope needed (personal team access for preview URL polling). If leaked, rotate at vercel.com/account/tokens. **This addresses Gemini's review concern, which was a misread of the plan's flow.** |
| T-02-03 | Tampering | `.lighthouserc.json` assertion thresholds | mitigate | The three `"error"` levels (performance, CLS, render-blocking-resources per D-33) are reviewed in this plan's acceptance criteria. A future commit that downgrades any to `"warn"` would silently allow failing pages — the plan-checker step at the end of phase verification would catch a regression by re-grepping the assertion structure. |
| T-02-04 | Denial of Service | `patrickedqvist/wait-for-vercel-preview` timeout | accept | The 300s timeout is a reasonable upper bound for Vercel preview deploys. Flaky timeout does not merge bad code — it only delays CI. |
| T-02-05 | Resource exposure | `.lighthouserc.json` published artifact | accept | The file is committed to source control and contains zero secrets — only public assertion thresholds. Safe to publish. |
| T-02-06 | Bypass of merge gate | branch protection rule on `main` (D-34) | mitigate | Task 4's checkpoint EXPLICITLY requires the developer to add `Lighthouse CI / lighthouse` as a required status check in repo settings. Without this, the workflow runs but cannot block merges. The acceptance criteria for Task 4 require visual confirmation of the branch protection rule. |
| T-02-07 | Supply-chain | `treosh/lighthouse-ci-action@v12`, `patrickedqvist/wait-for-vercel-preview@v1.3.1`, `actions/checkout@v4` | accept | All three actions are pinned to specific version tags (not `@latest` or `@main`). Both `treosh/lighthouse-ci-action` and `actions/checkout` are widely used and audited; `patrickedqvist/wait-for-vercel-preview@v1.3.1` is a smaller third-party action — review accepted given the limited blast radius (CI-only, no production code). |
</threat_model>

<verification>
After all four tasks land (Tasks 1–3 automated + Task 4 developer-confirmed):

1. `grep -rn "use client" app/ components/sections/ components/quibs/` returns zero matches.

2. Local LHCI run exits 0 — ALL THREE error-level assertions pass:
   - `categories:performance ≥ 0.90` ✅
   - `cumulative-layout-shift < 0.1` ✅
   - **(D-33) `render-blocking-resources maxLength: 0` ✅**

3. `.lighthouserc.json` contains the exact assertion structure (performance, CLS, render-blocking-resources at error level):
   ```bash
   node -e "const c=require('./.lighthouserc.json');const a=c.ci.assert.assertions;console.log('perf:',a['categories:performance'][0]);console.log('cls:',a['cumulative-layout-shift'][0]);console.log('render-blocking:',a['render-blocking-resources'][0])"
   ```
   All three should print `error`.

4. `.github/workflows/lighthouse.yml` references:
   - `treosh/lighthouse-ci-action@v12`
   - `patrickedqvist/wait-for-vercel-preview@v1.3.1`
   - `configPath: .lighthouserc.json`
   - `VERCEL_TOKEN` via `env:` block (never via `with:`)
   - The workflow `name: Lighthouse CI` and job key `lighthouse` produce the GitHub-rendered status check name `Lighthouse CI / lighthouse` (D-34 reference)

5. Developer confirms (Task 4 checkpoint):
   - `VERCEL_TOKEN` is set as a GitHub secret
   - **Branch protection rule on `main` requires `Lighthouse CI / lighthouse` status check (D-34 — concern #5 resolved)**
   - The workflow ran on the pushed commit
   - All three error-level assertions showed ✅ in the CI run
   - HTML report URL is visible in the CI step output
   - The PR's merge button correctly enforces the required status check

6. Phase 2 ROADMAP SC #4 is now satisfied (and ENFORCED, not advisory): "Lighthouse mobile performance ≥90, CLS <0.1, with no render-blocking third-party scripts on first paint — verified in CI on every PR AND blocks merge via branch protection."
</verification>

<success_criteria>
- [ ] `.lighthouserc.json` exists at repo root with THREE error-level assertions: `categories:performance` ≥0.90, `cumulative-layout-shift` <0.1, **and `render-blocking-resources` ≤0 (D-33)**.
- [ ] `@lhci/cli` is present in `package.json` devDependencies with a caret range.
- [ ] `.github/workflows/lighthouse.yml` exists with `treosh/lighthouse-ci-action@v12`, `patrickedqvist/wait-for-vercel-preview@v1.3.1`, `configPath: .lighthouserc.json`, and `VERCEL_TOKEN` via `env:` block.
- [ ] The workflow name + job key produce the status check name `Lighthouse CI / lighthouse` (D-34 reference).
- [ ] `.lighthouseci/` is in `.gitignore`.
- [ ] `grep -rn "use client" app/ components/sections/ components/quibs/` returns zero matches (T-02-01).
- [ ] Local LHCI run exits 0 with ALL THREE error-level assertions passing.
- [ ] No `echo`/`cat`/redirect of `VERCEL_TOKEN` exists in the workflow file (T-02-02 — Gemini's concern resolved).
- [ ] Developer confirms `VERCEL_TOKEN` is set as a GitHub secret (Task 4 Step 5).
- [ ] **(D-34, concern #5):** Developer confirms branch protection rule on `main` requires `Lighthouse CI / lighthouse` status check (Task 4 Step 3 + 7).
- [ ] Lighthouse CI GitHub Actions workflow runs on push/PR and shows green — all three error-level assertions pass in CI.
- [ ] HTML report URL is linked in the CI step output.
- [ ] ROADMAP Phase 2 SC #4 is satisfied AND enforced: gate is live AND blocking on regression.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-landing-page-no-form/02-05-SUMMARY.md` documenting:
- The `.lighthouserc.json` content (verbatim — small enough to include in full).
- The `@lhci/cli` version installed.
- The local LHCI run output — the exact assertion lines (✅/⚠️/❌) and the Lighthouse scores for performance, CLS, and render-blocking-resources.
- The LCP element selector reported by LHCI (confirms the H1 guard from Plan 02-02 is intact).
- Confirmation that `grep -rn "use client" app/ components/sections/ components/quibs/` returned zero matches.
- Confirmation that `grep -E 'echo.*VERCEL_TOKEN|cat.*VERCEL_TOKEN' .github/workflows/lighthouse.yml` returned zero matches (T-02-02 enforcement).
- The developer's Task 4 checkpoint confirmation, including:
  - Vercel token URL/screenshot reference (without leaking the token itself)
  - The exact text shown in the GitHub Branch Protection Rule "Required status checks" list (must include `Lighthouse CI / lighthouse`) — D-34 evidence
  - CI run URL + HTML report URL
  - Screenshot description of the PR's merge button enforcing the required status check
- A note that Phase 2 SC #4 is now satisfied AND enforced; phase is ready for `/gsd-verify-work`.
</output>
