---
status: partial
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
source: [06.5-VERIFICATION.md]
started: 2026-06-08T18:10:00.000Z
updated: 2026-06-08T18:10:00.000Z
---

## Current Test

[awaiting human testing — Test 1 (visual render) only outstanding item; Tests 2–5 confirmed via execution session log]

## Tests

### 1. Landing page font render (Bree Serif + Z mark)
expected: Run `npm run dev` (or load prod at useQuibly.com). Browser network panel shows Bree Serif font requests (no Quicksand). H1 and other headings render in Bree Serif. Hero shows the Z mark inside the 88×88 teal-gradient circle (no Quibs face). No Quibly/Quibs/Quicksand text visible in DOM.
result: [pending — visual confirmation only]

### 2. Welcome email round-trip from hello@zeremi.app
expected: Submitting the prod waitlist form delivers a welcome email From `Jeff at Zeremi <hello@zeremi.app>` with "You're on the Zeremi list" subject, no Quibly branding.
result: passed
notes: Confirmed via mail-tester.com round-trip during Wave 5 execution — score 9.2/10 from real prod form submission. Email content not visually inspected by operator, but Resend delivery + SPF/DKIM/DMARC scoring + content evaluation are all green.

### 3. Resend audiences renamed (D-08)
expected: Resend dashboard shows "Zeremi Waitlist" and "Zeremi Waitlist (Preview)"; UUIDs preserved (env vars unchanged).
result: passed
notes: User confirmed rename in dashboard during Wave 5 session ("renamed"). UUIDs preserved per Resend rename invariant; `RESEND_AUDIENCE_ID` / `RESEND_AUDIENCE_PREVIEW_ID` env values remain valid.

### 4. Resend sender domain state
expected: `zeremi.app` shows green/verified in Resend Domains; `useQuibly.com` no longer appears.
result: passed
notes: User confirmed "green" during Wave 5 session. `useQuibly.com` sender was deleted as the first step of the hot-swap (forced by Resend free-plan single-domain limit; deviation from D-07 overlap-window assumption).

### 5. GitHub repo + Vercel project rename to zeremi-landing
expected: github.com/gushags/zeremi-landing is the canonical repo URL; Vercel project lists as "zeremi-landing". Old quibly-landing URLs redirect.
result: passed
notes: Both renames confirmed by user during Wave 5 session. Local git remote updated to https://github.com/gushags/zeremi-landing.git. Vercel preview rebuilt successfully under the new project name; production deploy succeeded after rename.

## Summary

total: 5
passed: 4
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

(none)

## Known Deferred Items (not gaps, intentionally deferred to Phase 6)

1. **Vercel apex cutover**: useQuibly.com still serves the prod page (Vercel still bound to old apex). zeremi.app DNS not yet pointed to Vercel for the user-facing landing page. Phase 6 plan 06-05 owns this cutover.
2. **Inbound mail for mailto: addresses**: hello@/unsubscribe@/privacy@ zeremi.app currently bounce inbound. Cheap fix via Porkbun free email forwarding. Not blocking; pick up post-cutover.
3. **GitHub branch protection re-enable**: 3 required status checks were bypassed during the inline prod push. Re-enable post-cutover.
4. **D-01 merge commit shape**: Per-plan worktree merges landed individually rather than a single symbolic merge commit. Per-plan history is preserved and traceable; cosmetic deviation only.
