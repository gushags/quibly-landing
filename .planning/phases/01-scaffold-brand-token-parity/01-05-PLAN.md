---
phase: 01-scaffold-brand-token-parity
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - app/page.tsx
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "`npm run build` exits 0 with `.env.local` populated (env validation passes; Plan 01's Zod schema parses successfully)."
    - "`npm run dev` starts a local server that renders `app/page.tsx` showing all four parity surfaces: teal mascot, Quicksand 'Quibly' heading, Figtree body paragraph, shadcn Button with pill rounding."
    - "User visually confirms Phase 1 success criterion #1: 'A blank page renders in Quibly teal/amber with Quicksand headings and Figtree body, visually indistinguishable from marketing-app's tokens' (D-12)."
    - "Render path of mascot in smoke-test page is `<QuibsIcon className=\"text-primary size-12\" />` — uses `currentColor` so the teal token drives the color, satisfying Phase 1 success criterion #2 (D-03)."
    - "`app/page.tsx` is intentionally throwaway — Phase 2 replaces it entirely with `<Hero> + <WhyQuibly> + <Footer>`. No layout polish, copy refinement, accessibility-beyond-defaults, or responsiveness in Phase 1's smoke test (D-13)."
    - "Vercel project is linked to the Quibly team so PR previews exist from day one (D-15) — apex domain binding stays Phase 6."
    - "Pre-commit hook smoke-test confirms staging a fake `re_*` key blocks the commit (Plan 04 already empirically tested this; user re-confirms in this plan as the final phase deliverable for INFRA-08 success criterion #5)."
  artifacts:
    - path: "app/page.tsx"
      provides: "Throwaway smoke-test page demonstrating all four parity surfaces in one screenshot (D-12)."
      contains: "QuibsIcon"
      min_lines: 10
  key_links:
    - from: "app/page.tsx"
      to: "components/quibs/quibs-icon.tsx (Plan 02)"
      via: "import { QuibsIcon } from '@/components/quibs/quibs-icon'"
      pattern: "from '@/components/quibs/quibs-icon'"
    - from: "app/page.tsx"
      to: "components/ui/button.tsx (Plan 03)"
      via: "import { Button } from '@/components/ui/button'"
      pattern: "from '@/components/ui/button'"
    - from: "QuibsIcon with text-primary class"
      to: "globals.css `--color-primary` token (Plan 02)"
      via: "Tailwind `text-primary` utility -> @theme inline -> --primary oklch(0.6002 0.1038 184.704)"
      pattern: "text-primary"
---

<objective>
Compose the throwaway smoke-test page that visually proves all four Phase 1 parity surfaces in one screenshot (D-12), then run the verification gauntlet that confirms Phase 1 is complete:
1. `npm run build` succeeds (env validation, fonts, tokens, components all wire up correctly).
2. `npm run dev` renders the smoke-test page; user visually confirms parity (Phase 1 success criterion #1).
3. `vercel link` is run interactively to bind the Quibly Vercel team (D-15).
4. Pre-commit hook smoke-test re-runs with the user's eyes on the output (INFRA-08 success criterion #5).

Purpose: Phase 1 success criteria #1 (visual parity) and #2 (mascot rendered). This is the final plan — after this, Phase 1 is shippable and Phase 2 (static landing page) can begin.

Output: A working dev server showing all four parity surfaces; a linked Vercel project; a confirmed pre-commit hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-scaffold-brand-token-parity/01-CONTEXT.md
@.planning/phases/01-scaffold-brand-token-parity/01-PATTERNS.md
@.planning/phases/01-scaffold-brand-token-parity/01-01-SUMMARY.md
@.planning/phases/01-scaffold-brand-token-parity/01-02-SUMMARY.md
@.planning/phases/01-scaffold-brand-token-parity/01-03-SUMMARY.md
@.planning/phases/01-scaffold-brand-token-parity/01-04-SUMMARY.md

<interfaces>
<!-- Components and types this plan composes (all defined in Plans 01-03): -->

From components/quibs/quibs-icon.tsx (Plan 02):
```typescript
interface QuibsIconProps { className?: string; width?: number; height?: number }
export function QuibsIcon(props: QuibsIconProps): JSX.Element
```

From components/ui/button.tsx (Plan 03):
```typescript
export function Button(props: React.ComponentProps<"button"> & VariantProps<typeof buttonVariants> & { asChild?: boolean }): JSX.Element
```

From globals.css (Plan 02):
- `text-primary` Tailwind utility -> `--color-primary: var(--primary)` -> `--primary: oklch(0.6002 0.1038 184.704)` (Quibly teal)
- `font-heading` Tailwind utility -> `--font-heading: var(--font-quicksand)` -> Quicksand
- `font-sans` Tailwind utility (default) -> `--font-sans: var(--font-figtree)` -> Figtree
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write app/page.tsx smoke-test page (all four parity surfaces in one render)</name>
  <files>
    - app/page.tsx
  </files>
  <read_first>
    - .planning/phases/01-scaffold-brand-token-parity/01-PATTERNS.md (section "app/page.tsx")
    - .planning/phases/01-scaffold-brand-token-parity/01-CONTEXT.md (decisions D-12, D-13)
    - components/quibs/quibs-icon.tsx (this repo, Plan 02 — the imported component)
    - components/ui/button.tsx (this repo, Plan 03 — the imported component)
    - app/globals.css (this repo, Plan 02 — confirms `--font-heading` and `--font-sans` mappings exist; the smoke page exercises both)
  </read_first>
  <action>
Write `app/page.tsx` with this EXACT content (per D-12 — must surface all four parity surfaces in one screenshot):

```typescript
import { Button } from '@/components/ui/button'
import { QuibsIcon } from '@/components/quibs/quibs-icon'

/**
 * Phase 1 smoke-test page (D-12).
 *
 * INTENTIONALLY THROWAWAY. Phase 2 replaces this entire file with the real
 * <Hero> + <WhyQuibly> + <Footer> composition. No effort is spent on
 * layout polish, copy, accessibility-beyond-defaults, or responsiveness here.
 *
 * What this page proves (Phase 1 success criterion #1 — "blank page in Quibly
 * teal/amber with Quicksand headings and Figtree body, visually indistinguishable
 * from marketing-app's tokens"):
 *
 *   1. <QuibsIcon className="text-primary size-12" />
 *      -> teal `oklch(0.6002 0.1038 184.704)` token + currentColor mascot wiring
 *   2. <h1 className="font-heading text-4xl font-bold">Quibly</h1>
 *      -> Quicksand variable font + --font-heading mapping (globals.css)
 *   3. <p className="font-sans text-base">Lorem ipsum...</p>
 *      -> Figtree variable font + --font-sans mapping (globals.css)
 *   4. <Button>Smoke test</Button>
 *      -> shadcn pill-radii (rounded-full) + bg-primary fill via the oklch token
 *
 * If any one of the four surfaces renders with the WRONG color/font/shape,
 * Phase 1 has a token-chain regression — see PATTERNS.md "Two-hop indirection"
 * shared pattern for the chain to inspect.
 */
export default function SmokeTestPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 p-8">
      <QuibsIcon className="text-primary size-12" />
      <h1 className="font-heading text-4xl font-bold">Quibly</h1>
      <p className="font-sans text-base text-muted-foreground">
        Lorem ipsum dolor sit amet, consectetur adipiscing elit.
      </p>
      <Button>Smoke test</Button>
    </main>
  )
}
```

**Why each element is non-negotiable** (D-12 — each independently proves a different parity surface):
- `<QuibsIcon className="text-primary size-12" />` — `currentColor` SVG fill resolves through Tailwind `text-primary` -> globals.css `--color-primary` -> oklch teal. If you see black or a default color, the `@theme inline` block in globals.css is broken.
- `<h1 className="font-heading text-4xl font-bold">Quibly</h1>` — `font-heading` Tailwind utility resolves through globals.css `--font-heading: var(--font-quicksand)` to Quicksand. If you see system serif or sans-serif default, the `next/font/google` Quicksand loader in layout.tsx is misconfigured OR the `--font-quicksand` CSS var isn't landing on `<html>`.
- `<p className="font-sans text-base">` — same chain for Figtree via `--font-sans: var(--font-figtree)`.
- `<Button>Smoke test</Button>` — proves shadcn pill-radii (`rounded-full` base class), primary token drives fill (`bg-primary`), AND that the radix-ui barrel import in button.tsx resolves at runtime (would fail to render if Plan 03's hybrid install picked the wrong shadcn style).

**Anti-patterns to avoid:**
- Do NOT add layout polish (responsiveness, breakpoints, hero composition). This is throwaway — D-13 is explicit.
- Do NOT add the `<QuibsAvatar>` component here. The smoke test uses `<QuibsIcon>` directly per D-12 because the avatar wraps the icon in a teal-gradient container that would mask the `text-primary` token validation.
- Do NOT add toast/sonner UI. Plan 02 explicitly dropped `<Toaster />` from layout.tsx. Phase 3 reintroduces it.
- Do NOT add metadata to this page. Layout.tsx's metadata (Plan 02) covers it.
  </action>
  <verify>
    <automated>
test -f app/page.tsx && \
grep -q "QuibsIcon" app/page.tsx && \
grep -q "Button" app/page.tsx && \
grep -q "font-heading" app/page.tsx && \
grep -q "font-sans" app/page.tsx && \
grep -q "text-primary" app/page.tsx && \
grep -q "from '@/components/quibs/quibs-icon'" app/page.tsx && \
grep -q "from '@/components/ui/button'" app/page.tsx && \
echo "Type-check the page file:" && \
node_modules/.bin/tsc --noEmit 2>&1 | head -10 ; \
TSC_EXIT=$? ; test $TSC_EXIT -eq 0 || (echo "FAIL: tsc errors" && exit 1) && \
echo "Lint check:" && \
npm run lint 2>&1 | tail -10 ; \
LINT_EXIT=$? ; test $LINT_EXIT -eq 0 || (echo "FAIL: lint errors" && exit 1) && \
echo "ALL SMOKE-TEST FILE CHECKS OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `app/page.tsx` exists.
    - `app/page.tsx` imports `QuibsIcon` from `@/components/quibs/quibs-icon` (Plan 02 dependency).
    - `app/page.tsx` imports `Button` from `@/components/ui/button` (Plan 03 dependency).
    - `app/page.tsx` renders a `<QuibsIcon>` with `className="text-primary size-12"` (proves teal token + currentColor — D-12 surface 1).
    - `app/page.tsx` renders an `<h1>` with `className="font-heading text-4xl font-bold"` (proves Quicksand mapping — D-12 surface 2).
    - `app/page.tsx` renders a `<p>` with `className` containing `font-sans` (proves Figtree mapping — D-12 surface 3).
    - `app/page.tsx` renders a `<Button>` (proves shadcn pill + primary fill — D-12 surface 4).
    - `app/page.tsx` does NOT use `<QuibsAvatar>` (D-12 specifies `<QuibsIcon>` directly so the teal-gradient container does not mask the text-primary validation).
    - `app/page.tsx` does NOT add metadata, Sonner Toaster, or layout polish (D-13 throwaway intent).
    - `tsc --noEmit` passes with exit 0.
    - `npm run lint` passes with exit 0 (no `process.env` in this file; no banned patterns).
  </acceptance_criteria>
  <done>The smoke-test page is in place, imports resolve, type-check and lint are clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build, then run pre-commit hook smoke-test (autonomous final checks)</name>
  <files>
    - (none tracked in git — `.env.local` is created as a side-effect but is gitignored)
  </files>
  <read_first>
    - app/page.tsx (just written in Task 1)
    - .env.example (this repo, Plan 01 — placeholder values to copy into .env.local)
    - .husky/pre-commit (this repo, Plan 04 — the hook to be empirically tested)
    - .gitleaks.toml (this repo, Plan 04)
    - .planning/phases/01-scaffold-brand-token-parity/01-04-SUMMARY.md (the gitleaks block test was already done in Plan 04; this task re-runs it as the phase-level confirmation)
  </read_first>
  <action>
**Side-effect note:** This task creates `.env.local` (copied from `.env.example`) so `npm run build` can satisfy the Zod schema. `.env.local` is **gitignored** (Plan 01's `.gitignore` covers `.env*.local`) and is therefore NOT listed in `files_modified` — only files committed to git belong there. The verifier should treat `.env.local`'s creation as an expected, untracked side-effect.

**Step 1: Provision `.env.local` with syntactically-valid placeholder values.**

Phase 1 doesn't actually USE the secrets — Plan 04 has empirically validated the Zod schema and gitleaks gate. To run `npm run build` we just need values that satisfy Zod's schema (string min(1) and a valid URL). Use the same placeholders as `.env.example`, but ensure `UPSTASH_REDIS_REST_URL` is a parseable URL (`z.string().url()` requires a valid URL):

```bash
test -f .env.local || cp .env.example .env.local
# Verify the URL field is parseable (the .env.example placeholder `https://your-instance.upstash.io` is a valid URL)
grep "UPSTASH_REDIS_REST_URL=" .env.local
# Sanity check: confirm git treats .env.local as untracked (gitignore from Plan 01 is honored)
git status --porcelain .env.local 2>/dev/null
# Expected: empty output OR a `??` (untracked) marker — never `A ` (added) or ` M` (modified-tracked)
```

**IMPORTANT: do NOT commit `.env.local`.** The `.gitignore` from Plan 01 covers `.env*.local`; the gitleaks rules in Plan 04 are also a backstop. The placeholder values would not match real Resend/Upstash secrets, but defense-in-depth.

**Step 2: Run `npm run build`.**

```bash
npm run build 2>&1 | tee /tmp/build-out.txt
BUILD_EXIT=${PIPESTATUS[0]}
echo "build exit code: $BUILD_EXIT"
test $BUILD_EXIT -eq 0 || (echo "FAIL: build did not succeed" && tail -50 /tmp/build-out.txt && exit 1)
```

Expected: clean build with exit 0. The build proves:
- `lib/env.ts` Zod schema parses `.env.local` values successfully (no boot crash with valid placeholders).
- `app/globals.css` Tailwind v4 token block parses (no `@theme inline` errors).
- `next/font/google` loads Quicksand + Figtree at build time (no font-fetch errors).
- All component imports (button, input, label, sonner, QuibsIcon, QuibsAvatar) resolve.
- TypeScript strict mode passes for the entire codebase.

If build fails, check the output for which layer broke (env -> tokens -> fonts -> components -> page).

**Step 3: Run the pre-commit hook simulation against a known-good staged change.**

```bash
git add app/page.tsx
.husky/pre-commit
HOOK_EXIT=$?
git restore --staged app/page.tsx 2>/dev/null
echo "pre-commit on clean stage exit: $HOOK_EXIT"
test $HOOK_EXIT -eq 0 || (echo "FAIL: pre-commit hook errored on clean stage" && exit 1)
```

Expected: exit 0. gitleaks finds nothing in `app/page.tsx`; tsc passes; eslint passes.

**Step 4: Re-run the empirical gitleaks block test (re-confirms Plan 04's empirical test is still valid after all subsequent changes).**

```bash
echo 'RESEND_API_KEY=re_BBBBBBBBBBBBBBBBBBBB' > ./leak-test-fake-2.txt
git add ./leak-test-fake-2.txt
gitleaks protect --staged --redact -c .gitleaks.toml
LEAK_EXIT=$?
git restore --staged ./leak-test-fake-2.txt 2>/dev/null
rm -f ./leak-test-fake-2.txt
test $LEAK_EXIT -ne 0 || (echo "FAIL: gitleaks no longer blocks fake re_ keys" && exit 1)
echo "OK: gitleaks block re-confirmed"
```

This is the INFRA-08 phase-level confirmation. It re-runs Plan 04's empirical test as a phase-completion gate.
  </action>
  <verify>
    <automated>
test -f .env.local || cp .env.example .env.local && \
echo "Build:" && \
npm run build 2>&1 | tail -20 ; \
BUILD_EXIT=${PIPESTATUS[0]} ; \
test $BUILD_EXIT -eq 0 || (echo "FAIL: build did not succeed" && exit 1) && \
echo "  build OK" && \
echo "Pre-commit hook on clean stage:" && \
git add app/page.tsx 2>/dev/null ; \
.husky/pre-commit > /tmp/hook-out.txt 2>&1 ; \
HOOK_EXIT=$? ; \
git restore --staged app/page.tsx 2>/dev/null ; \
test $HOOK_EXIT -eq 0 || (echo "FAIL: hook errored on clean stage" && cat /tmp/hook-out.txt && exit 1) && \
echo "  hook OK on clean stage" && \
echo "Pre-commit hook block test (re-confirmation):" && \
echo 'RESEND_API_KEY=re_BBBBBBBBBBBBBBBBBBBB' > ./leak-test-fake-2.txt && \
git add ./leak-test-fake-2.txt 2>/dev/null ; \
gitleaks protect --staged --redact -c .gitleaks.toml > /tmp/leak-out.txt 2>&1 ; \
LEAK_EXIT=$? ; \
git restore --staged ./leak-test-fake-2.txt 2>/dev/null ; \
rm -f ./leak-test-fake-2.txt /tmp/leak-out.txt /tmp/hook-out.txt ; \
test $LEAK_EXIT -ne 0 || (echo "FAIL: gitleaks regressed — fake re_ no longer blocked" && exit 1) && \
echo "  gitleaks block re-confirmed" && \
echo "" && \
echo "Confirm .env.local is NOT tracked (gitignore from Plan 01 honored):" && \
git ls-files --error-unmatch .env.local 2>/dev/null && (echo "FAIL: .env.local is tracked!" && exit 1) || echo "  .env.local untracked: OK" && \
echo "" && \
echo "PHASE 1 BUILD + HOOK GATE CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.env.local` exists (created from `.env.example` if missing) — Phase 1 acceptance is that the schema parses; values are placeholders. NOTE: `.env.local` is gitignored and intentionally excluded from this task's `files_modified` frontmatter — it's an untracked side-effect, not a tracked deliverable.
    - `git ls-files --error-unmatch .env.local` exits non-zero (i.e., `.env.local` is NOT tracked by git).
    - `npm run build` exits 0 with the smoke-test page in place.
    - The build output mentions no errors related to: env validation (Zod), `@theme inline` parsing, `next/font/google`, missing imports, or shadcn component compilation.
    - `.husky/pre-commit` (run with `app/page.tsx` staged) exits 0 — gitleaks finds nothing, tsc passes, lint passes.
    - **Re-confirmation of INFRA-08 success criterion #5:** staging a fresh fake `RESEND_API_KEY=re_BBBBBBBBBBBBBBBBBBBB` (different B-pattern than Plan 04's A-pattern, to prove the rule is regex-based not literal-match) produces non-zero exit from `gitleaks protect --staged`.
    - No `.env.local` is staged for commit (sanity check: `git status .env.local` shows untracked or not modified, never staged).
  </acceptance_criteria>
  <done>Build succeeds; pre-commit hook is empirically clean on a real change AND empirically blocks a fresh fake `re_` key; all gates fire as designed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User visually confirms all four parity surfaces in the smoke-test page</name>
  <files>
    - (none — verification-only checkpoint; user inspects rendered output of app/page.tsx)
  </files>
  <read_first>
    - app/page.tsx (the page being verified)
    - app/globals.css (the token chain being verified)
    - app/layout.tsx (the font loaders being verified)
    - .planning/phases/01-scaffold-brand-token-parity/01-CONTEXT.md (decisions D-12, D-13 — the four parity surfaces this checkpoint validates)
  </read_first>
  <action>
**Pause for human verification.** The executor (Claude) starts the dev server, then surfaces the verification steps below to the user and waits for `approved` or `failed: <surface>` resume signal.

**Executor actions before pausing:**

```bash
# Start dev server in background so the user can hit it.
# IMPORTANT: write the PID to a file so we can kill it after the user resumes —
# shell variables do not survive across task boundaries.
npm run dev &
echo $! > /tmp/quibly-dev.pid
sleep 5
# Verify it bound:
curl -s http://localhost:3000 | head -50 > /tmp/dev-html.txt
echo "Dev server PID: $(cat /tmp/quibly-dev.pid) — will be killed when this task resumes"
```

Then SURFACE the following verification script to the user verbatim and BLOCK on the `<resume-signal>` response.

<!-- horizontal rule -->

**What was built:**
- `app/page.tsx` rendered via `npm run dev` shows all four Phase 1 parity surfaces in a single viewport (D-12 + Phase 1 success criterion #1).
- Phase 1 success criterion #1: "A blank page renders in Quibly teal/amber with Quicksand headings and Figtree body, visually indistinguishable from marketing-app's tokens (oklch primary, radius scale, font CSS variables verbatim)."
- Phase 1 success criterion #2: "The Quibs Q-face mascot renders in the page as a reusable React component sourced from public/."
- Phase 1 success criterion #3: "shadcn button/input/label/sonner/form components are installed and styled to Quibly tokens (no default shadcn neutral)."

**How to verify (user action):**

1. Open `http://localhost:3000` in a browser.

2. Optionally, in a separate terminal, start `marketing-app` for side-by-side comparison:
   ```
   cd /Users/jeff/repos/marketing-app && npm run dev   # binds to :3002
   ```

3. Visually confirm all FOUR surfaces:
   - **MASCOT (D-12 surface 1):** The Quibs Q-face SVG renders at the top of the page in **teal**.
     - PASS: clearly the same teal as `marketing-app` (`#0D9488` ≈ `oklch(0.6002 0.1038 184.704)`).
     - FAIL signs: black, default-gray, blue, off-tone teal — indicates the `@theme inline` -> `--color-primary` chain is broken.
   - **HEADING (D-12 surface 2):** "Quibly" renders large and bold in **Quicksand** (rounded, friendly geometric).
     - PASS: rounded letterforms, distinct from system serif/sans-default.
     - FAIL signs: serif, default sans, or Figtree (mapping reversed). Fix: re-check `app/layout.tsx`'s font loaders and globals.css's `@theme inline` block.
   - **BODY (D-12 surface 3):** "Lorem ipsum dolor sit amet..." renders in **Figtree** (clean modern grotesque).
     - PASS: clean modern proportions, distinct from the heading.
     - FAIL signs: same font as heading (mapping reversed) or system default.
   - **BUTTON (D-12 surface 4):** "Smoke test" button is **pill-shaped** (fully rounded), filled with the same teal as the mascot.
     - PASS: completely rounded ends; teal fill; white text.
     - FAIL signs: square corners (default shadcn neutral leaked), gray fill (primary token broken).

4. Optionally resize to 320px wide — the page should not horizontally scroll. (Not a Phase 1 acceptance — Phase 2's MOB-* covers responsiveness; this is a baseline sanity check.)

5. Reply with the `<resume-signal>` value (`approved` or `failed: <surface>`).

**After the user resumes, the executor MUST kill the dev server before moving to Task 4:**

```bash
# Kill via the PID file we wrote earlier (variable would not have survived the task boundary).
if [ -f /tmp/quibly-dev.pid ]; then
  kill "$(cat /tmp/quibly-dev.pid)" 2>/dev/null || true
  rm -f /tmp/quibly-dev.pid
fi
# Belt-and-suspenders: kill any lingering `next dev` process.
pkill -f "next dev" 2>/dev/null || true
sleep 1
# Confirm port 3000 is free.
lsof -ti:3000 2>/dev/null && (echo "WARN: port 3000 still bound" && true) || echo "Dev server stopped, port 3000 free"
```

This kill step is mandatory — leaving `next dev` running blocks Plan 06+ work and may collide with marketing-app's port if the user starts it later.
  </action>
  <verify>
    <automated>
echo "User-driven visual verification — automated component is limited to the dev server boot." && \
echo "The user's resume-signal (approved / failed:<surface>) is the verification artifact." && \
echo "Sanity-check that dev server CAN bind:" && \
test -f app/page.tsx && \
test -f app/layout.tsx && \
test -f app/globals.css && \
echo "  prerequisite files present: OK" && \
echo "Post-resume cleanup check (dev server killed, port free):" && \
( lsof -ti:3000 2>/dev/null && (echo "  WARN: port 3000 still bound (run pkill -f 'next dev')" && exit 0) || echo "  port 3000 free: OK" )
    </automated>
  </verify>
  <acceptance_criteria>
    - Dev server starts on `http://localhost:3000` (`curl -s http://localhost:3000 | head` returns HTML containing `<h1` and `Quibly`).
    - PID is captured to `/tmp/quibly-dev.pid` so the kill step at the end of `<action>` can reach it (shell variables do NOT survive task boundaries).
    - User has visited the page in a browser and inspected all four parity surfaces.
    - User responds with `approved` (resume signal) — implies all four surfaces render correctly.
    - If user responds with `failed: <surface>`, the executor must re-attempt: investigate the broken chain (mascot=token chain, heading=Quicksand loader, body=Figtree loader, button=shadcn import) and re-run this checkpoint.
    - **Cleanup is mandatory:** before this task is marked done, the executor MUST kill the dev server (via `kill "$(cat /tmp/quibly-dev.pid)"` then `pkill -f "next dev"` as a fallback) and confirm port 3000 is free (`lsof -ti:3000` returns empty).
  </acceptance_criteria>
  <done>User has typed `approved` in response to the resume-signal prompt, confirming Phase 1 success criteria #1, #2, #3 visually; AND the dev server has been killed (port 3000 free).</done>
  <resume-signal>
Type `approved` if all four surfaces render correctly with Quibly teal + Quicksand + Figtree + pill button.

Type `failed: <which surface>` (e.g., `failed: mascot is gray`, `failed: heading is system serif`) so the executor can investigate the broken chain link before re-attempting.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run `vercel link` to bind the project to the Quibly Vercel team (D-15)</name>
  <files>
    - .vercel/project.json
  </files>
  <read_first>
    - .gitignore (this repo, Plan 01 — confirms `.vercel` is gitignored; `.vercel/project.json` is local-only)
    - .planning/phases/01-scaffold-brand-token-parity/01-CONTEXT.md (decision D-15)
  </read_first>
  <action>
**This is a true `checkpoint:human-action`** (the only one in Phase 1). `vercel link` is interactive: it requires the user to:
- Authenticate with Vercel (browser OAuth on first run).
- Select the `Quibly` team from a list (no API path for "select my team" — D-15 mandates team-level for Phase 6's atomic transfer).

The executor cannot automate either step — this task pauses for the user.

**Executor actions before pausing:**

```bash
# Pre-flight: confirm the Vercel CLI is on PATH
which vercel > /dev/null 2>&1
VERCEL_PRESENT=$?
if [ $VERCEL_PRESENT -ne 0 ]; then
  echo "Vercel CLI not installed. Install with: npm i -g vercel"
  echo "After install, re-run this task."
  exit 1
fi
echo "Vercel CLI present: $(vercel --version)"
```

Then SURFACE the following instructions to the user verbatim and BLOCK on the `<resume-signal>` response.

<!-- horizontal rule -->

**What was built:**
- The repo is ready to be linked to a Vercel team for PR previews from day one.
- D-15 mandates Vercel team-level linking in Phase 1 (apex domain binding stays Phase 6).
- This is the only TRUE checkpoint:human-action in Phase 1 — Vercel team selection is interactive in the CLI; no API key path exists for "select my team" prompts in `vercel link`.

**How to verify (user actions):**

1. Confirm Vercel CLI is installed:
   ```
   which vercel
   ```
   If missing: `npm i -g vercel`.

2. Run `vercel link` from the repo root:
   ```
   cd /Users/jeff/repos/quibly-landing
   vercel link
   ```

3. Answer the prompts:
   - **Set up and link?** Y
   - **Which scope?** Select your `Quibly` team (NOT your personal account — D-15 requires team-level for atomic transfer to `marketing-app` in Phase 6).
   - **Link to existing project?** N (this is a brand-new project; Vercel creates one).
   - **Project name?** `quibly-landing` (or accept the default).
   - **Directory containing your project?** `./` (current directory).

4. Confirm the link landed:
   ```
   cat .vercel/project.json
   ```
   Expected output: includes `"projectId": "..."` and `"orgId": "..."` (the org ID maps to the Quibly team).

5. **Optional but recommended:** Trigger a preview deploy on a branch:
   ```
   git checkout -b smoke-test/phase-1-preview
   git add app/page.tsx components/ lib/ public/ .planning/ package.json package-lock.json tsconfig.json next.config.ts postcss.config.mjs components.json eslint.config.mjs eslint-rules/ .husky/ .gitleaks.toml .env.example .gitignore app/globals.css app/layout.tsx
   git commit -m "phase-01: scaffold + brand tokens (smoke test)"
   git push -u origin smoke-test/phase-1-preview
   ```
   Expected: Vercel posts a preview URL. Visit it and re-confirm the four parity surfaces (validates that the production build path matches local dev).

   **If you do NOT want to deploy yet** (e.g., wait until Phase 5 SEO/legal lands), skip Step 5 — it is optional. The link itself (Steps 1-4) satisfies D-15.

**Important:** `.vercel/` is gitignored (Plan 01's `.gitignore` covers `.vercel`). It should not appear in `git status`.

**If the user opts to skip (resume signal `skipped: <reason>`):** D-15's contract is broken until Phase 6 re-attempts. The executor MUST take the following deterministic actions before marking the task done — the deferral cannot be hand-waved:

1. **Append to `.planning/phases/01-scaffold-brand-token-parity/01-05-SUMMARY.md`** a section with this exact structure:
   ```markdown
   ## Deferred to Phase 6: Vercel link (D-15)

   **Decision date:** {YYYY-MM-DD of resume signal}
   **User-provided reason:** {verbatim text from `skipped: <reason>` resume signal}
   **Contract impact:** D-15 ("Vercel team-level link in Phase 1") is unmet.
   **Phase 6 follow-up:** the deploy planner MUST re-attempt `vercel link` before any apex-domain binding work begins. The pre-deploy checklist in Phase 6 must include: "Verify .vercel/project.json exists; if missing, run `vercel link` interactively first."
   ```

2. **Update `.planning/STATE.md`** with a tracked decision item under `## Pending Decisions` (or create the section if it does not exist):
   ```markdown
   - [ ] **Vercel link deferred** (Phase 1 → Phase 6): {user reason}. Phase 6 deploy plan MUST re-run `vercel link` before apex-domain binding. (Logged from 01-05 Task 4 on {YYYY-MM-DD}.)
   ```

   If `.planning/STATE.md` does not exist yet, surface that as a separate warning to the user — do NOT silently skip the STATE update.

These two updates are mandatory on the `skipped` path. They convert "hand-waved deferral" into a concrete, greppable breadcrumb that Phase 6's planner will see.
  </action>
  <verify>
    <automated>
echo "Verify Vercel link landed:" && \
test -f .vercel/project.json && echo "  .vercel/project.json exists: OK" || (echo "  WARN: .vercel/project.json missing — user may have skipped or deferred" && exit 0) ; \
grep -q '"projectId"' .vercel/project.json && echo "  projectId present: OK" || echo "  FAIL: projectId missing" ; \
grep -q '"orgId"' .vercel/project.json && echo "  orgId present: OK" || echo "  FAIL: orgId missing" ; \
echo "  (Verification is conditional — `skipped:<reason>` resume signal is acceptable per D-15 contract; the action's deferral steps require updates to 01-05-SUMMARY.md AND STATE.md.)"
    </automated>
  </verify>
  <acceptance_criteria>
    - User responds with `linked` (resume signal): `.vercel/project.json` exists with both `projectId` and `orgId` keys present (verified via `grep -q '"projectId"' .vercel/project.json && grep -q '"orgId"' .vercel/project.json`).
    - OR user responds with `skipped: <reason>` (resume signal): the executor MUST (a) append a `## Deferred to Phase 6: Vercel link (D-15)` section to `01-05-SUMMARY.md` with the verbatim user reason, AND (b) add a tracked `Vercel link deferred` item to `.planning/STATE.md`'s `## Pending Decisions` section. Both updates are mandatory — neither is optional. If STATE.md does not exist, surface the warning explicitly.
    - The Vercel CLI is installed (`which vercel` exits 0); if not, the task surfaces the install instructions and stops.
    - `.vercel/` directory does NOT appear in `git status` as a tracked or staged change (covered by Plan 01's `.gitignore`).
  </acceptance_criteria>
  <done>User has typed `linked` (with `.vercel/project.json` materialized) OR `skipped: <reason>` (with the deferral logged to BOTH `01-05-SUMMARY.md` (new `## Deferred to Phase 6: Vercel link (D-15)` section, verbatim reason) AND `.planning/STATE.md` (tracked Pending Decisions item)).</done>
  <resume-signal>
Type `linked` after `cat .vercel/project.json` shows a valid `projectId` and `orgId`.

Type `skipped: <reason>` if you intentionally defer the link (e.g., not ready for the Quibly Vercel team account yet). Note: the executor will then write a `## Deferred to Phase 6: Vercel link (D-15)` section to `01-05-SUMMARY.md` and a Pending Decisions item to `.planning/STATE.md` so Phase 6's planner can pick it up.
  </resume-signal>
</task>

</tasks>

<verification>
**Phase-level checks (run after all four tasks):**

```bash
# 1. Smoke-test page exists and type-checks
test -f app/page.tsx && node_modules/.bin/tsc --noEmit

# 2. Build succeeds
npm run build

# 3. Pre-commit hook fires correctly on a clean stage
git add app/page.tsx
.husky/pre-commit
git restore --staged app/page.tsx

# 4. Pre-commit hook blocks fake Resend keys (working-tree file, not /tmp)
echo 'RESEND_API_KEY=re_CCCCCCCCCCCCCCCCCCCC' > ./tmp-leak.txt
git add ./tmp-leak.txt
gitleaks protect --staged --redact -c .gitleaks.toml
test $? -ne 0
git restore --staged ./tmp-leak.txt
rm ./tmp-leak.txt

# 5. Vercel link present (after Task 4) — conditional, may be skipped
test -f .vercel/project.json && grep -q '"projectId"' .vercel/project.json && grep -q '"orgId"' .vercel/project.json

# 6. Dev server killed (port 3000 free) — required after Task 3 cleanup
lsof -ti:3000 2>/dev/null && echo "WARN: port 3000 still bound" || echo "port 3000 free: OK"

# 7. User visual confirmation logged in Task 3 resume signal
```
</verification>

<success_criteria>
- [ ] `app/page.tsx` renders all four parity surfaces (mascot, Quicksand heading, Figtree body, pill button) per D-12.
- [ ] `npm run build` exits 0 with `.env.local` populated by `.env.example` placeholders.
- [ ] Pre-commit hook empirically blocks a fake `re_*` key (re-confirmation of Plan 04's empirical test).
- [ ] Pre-commit hook exits 0 on a known-good staged change.
- [ ] User has visually confirmed Phase 1 success criterion #1 (parity surfaces all render correctly) AND the dev server has been killed (port 3000 free).
- [ ] Vercel project is linked to the Quibly team (D-15) — `.vercel/project.json` exists with valid `projectId` and `orgId`. (Or skipped, with deferral logged to both `01-05-SUMMARY.md` and `STATE.md` Pending Decisions per Task 4's documented protocol.)
- [ ] All five Phase 1 ROADMAP success criteria are satisfied (this plan covers #1, #2, #3, #5; #4 was empirically validated in Plan 01 Task 3).
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-brand-token-parity/01-05-SUMMARY.md` documenting:
- A note that the smoke-test page renders correctly (Task 3 user resume signal: approved/failed).
- Vercel project ID + org ID from `.vercel/project.json` (or — if Task 4 was deferred — a `## Deferred to Phase 6: Vercel link (D-15)` section with the verbatim user reason; this section MUST exist on the `skipped` path per Task 4's acceptance criteria).
- The exit code of the empirical gitleaks block re-test (must be non-zero).
- The exit code of `npm run build` (must be 0).
- Confirmation that the dev server was killed at the end of Task 3 (port 3000 free).
- Optional: a screenshot path if the user captured one of the smoke-test page for the phase record.
- A check against the five Phase 1 ROADMAP success criteria with PASS/FAIL/PARTIAL for each:
  1. Visual parity (D-12 four surfaces): PASS / FAIL
  2. Quibs mascot reusable + sourced from public/: PASS (Plan 02 + this plan)
  3. shadcn button/input/label/sonner installed (form intentionally deferred): PARTIAL — surface for verify-phase
  4. Boot crashes on missing env (Plan 01 Task 3 empirical test): PASS
  5. Gitleaks blocks `re_*`: PASS (re-confirmed here)
</output>
