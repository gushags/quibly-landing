---
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/zeremi/zeremi-icon.tsx
  - components/zeremi/zeremi-avatar.tsx
  - components/quibs/quibs-icon.tsx
  - components/quibs/quibs-avatar.tsx
  - components/sections/hero-mascot.tsx
  - components/sections/why-quibly.tsx
  - components/sections/why-zeremi.tsx
  - app/page.tsx
autonomous: true
requirements:
  - D-04
  - CD-04
  - SC-2
must_haves:
  truths:
    - "D-04: Zeremi mark is rendered as inline SVG with fill='currentColor' via the three-component pattern (ZeremiIcon → ZeremiAvatar → HeroMascot), mirroring the prior Quibs three-component shape exactly"
    - "components/zeremi/ exists with ZeremiIcon (inline 56×88 SVG, fill='currentColor') and ZeremiAvatar (same SIZE_CONFIG + inverted prop shape as QuibsAvatar)"
    - "components/quibs/ directory is deleted — no QuibsIcon/QuibsAvatar files remain on disk"
    - "HeroMascot renders <ZeremiIcon width={36} height={56} /> inside the unchanged 88×88 rounded-3xl teal-gradient wrapper"
    - "WhyQuibly component is renamed to WhyZeremi (function + export + heading copy 'Why Zeremi') and its file is why-zeremi.tsx; app/page.tsx imports WhyZeremi from the new path"
  artifacts:
    - path: components/zeremi/zeremi-icon.tsx
      provides: "Inline 56×88 Z mark SVG component with fill='currentColor', aria-hidden, prop signature {className?, width?, height?}"
      contains: "ZeremiIcon"
      min_lines: 25
    - path: components/zeremi/zeremi-avatar.tsx
      provides: "ZeremiAvatar with size/inverted props, SIZE_CONFIG (message/header/fab), teal gradient or inverted-white container"
      contains: "ZeremiAvatar"
    - path: components/sections/why-zeremi.tsx
      provides: "WhyZeremi section component (renamed from WhyQuibly)"
      contains: "WhyZeremi"
  key_links:
    - from: components/zeremi/zeremi-avatar.tsx
      to: components/zeremi/zeremi-icon.tsx
      via: "import { ZeremiIcon } from '@/components/zeremi/zeremi-icon'"
      pattern: "from ['\"]@/components/zeremi/zeremi-icon['\"]"
    - from: components/sections/hero-mascot.tsx
      to: components/zeremi/zeremi-icon.tsx
      via: "import { ZeremiIcon } from '@/components/zeremi/zeremi-icon'"
      pattern: "ZeremiIcon"
    - from: app/page.tsx
      to: components/sections/why-zeremi.tsx
      via: "import { WhyZeremi } from '@/components/sections/why-zeremi'"
      pattern: "WhyZeremi"
---

<objective>
Wave 1 foundation B: rename the Quibs three-component pattern to the Zeremi three-component pattern per D-04. Create `components/zeremi/zeremi-icon.tsx` (inline Z mark SVG ported from `public/brand/zeremi/src/zeremi-mark.svg`), `components/zeremi/zeremi-avatar.tsx` (1:1 port of QuibsAvatar with the icon swap), modify `components/sections/hero-mascot.tsx` in place to use ZeremiIcon at 36×56, rename `components/sections/why-quibly.tsx` → `why-zeremi.tsx`, update `app/page.tsx` to import the renamed component, and delete `components/quibs/` (CD-04).

This plan runs in parallel with plan 01 — there is no file overlap. The Z mark SVG path data may be sourced either from `public/brand/zeremi/src/zeremi-mark.svg` (if plan 01 has landed) or directly from `/Users/jeff/repos/marketing-app/public/brand/zeremi/src/zeremi-mark.svg` (the SVG path data is reproduced verbatim in UI-SPEC §Z Mark Visual Contract for executor convenience).

Purpose: Lock D-04 component shape parity. Mirrors the existing QuibsIcon/QuibsAvatar/HeroMascot pattern exactly so prop APIs are stable for any future use; only filenames + inner glyph + (in HeroMascot) dimensions change.

Output: `components/zeremi/` directory created, `components/quibs/` deleted, hero-mascot + why-zeremi + page.tsx updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md
@components/quibs/quibs-icon.tsx
@components/quibs/quibs-avatar.tsx
@components/sections/hero-mascot.tsx
@components/sections/why-quibly.tsx
@app/page.tsx
@/Users/jeff/repos/marketing-app/public/brand/zeremi/src/zeremi-mark.svg
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create components/zeremi/zeremi-icon.tsx and components/zeremi/zeremi-avatar.tsx</name>
  <files>components/zeremi/zeremi-icon.tsx, components/zeremi/zeremi-avatar.tsx</files>
  <read_first>
    - components/quibs/quibs-icon.tsx (analog — prop signature, JSDoc tone, aria-hidden, fill='currentColor', `<svg>` wrapper)
    - components/quibs/quibs-avatar.tsx (analog — SIZE_CONFIG shape, inverted prop logic, gradient classes, cn() composition)
    - /Users/jeff/repos/marketing-app/public/brand/zeremi/src/zeremi-mark.svg (Z mark SVG source — viewBox 0 0 56 88; two <g transform>...</g> groups with three <path> elements total)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§Z Mark Visual Contract — reproduces the exact path data + transforms verbatim, including `style={{ fillRule: 'nonzero' }}` and transforms `matrix(1,0,0,1,-50.6594,-196.892)` / `matrix(1,0,0,1,-48.6594,-256.841)`)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pattern 2 + Pitfall 7 — strip `<defs>` gradient block, replace `fill="url(#g)"` with `fill="currentColor"` on the inner <g>; ZeremiIcon SIZE_CONFIG icon widths per UI-SPEC use 56:88 aspect)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§"components/zeremi/zeremi-icon.tsx" and §"components/zeremi/zeremi-avatar.tsx" — verbatim port instructions)
  </read_first>
  <action>
    Create `components/zeremi/zeremi-icon.tsx`:
    - Define `interface ZeremiIconProps { className?: string; width?: number; height?: number }` — identical signature to `QuibsIconProps`.
    - JSDoc: "Inline Zeremi Z mark SVG. Uses `fill='currentColor'` so color inherits from parent (typically set by `ZeremiAvatar` or `HeroMascot`). Always decorative — paired with text or container-level aria-label for meaning."
    - Export `function ZeremiIcon({ className, width, height }: ZeremiIconProps)`.
    - Render `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 56 88" fill="currentColor" width={width} height={height} className={className} aria-hidden="true">`.
    - Inside the `<svg>`, port the THREE path elements from `zeremi-mark.svg` per UI-SPEC §Z Mark Visual Contract. Per RESEARCH Pitfall 7, STRIP the `<defs><linearGradient id="g">...</linearGradient></defs>` block from the raw SVG. Reproduce two `<g>` blocks: first with `transform="matrix(1,0,0,1,-50.6594,-196.892)"` containing one `<path>` for the Z letter; second with `transform="matrix(1,0,0,1,-48.6594,-256.841)"` containing two `<path>` elements for the two dot glyphs. Each path uses `style={{ fillRule: 'nonzero' }}` in React JSX form. The fill is inherited from the `<svg>` element's `fill="currentColor"` — do NOT add a `fill="url(#g)"` or any gradient reference on the inner elements.

    Create `components/zeremi/zeremi-avatar.tsx` as a verbatim port of `components/quibs/quibs-avatar.tsx`:
    - Replace import `from '@/components/quibs/quibs-icon'` with `from '@/components/zeremi/zeremi-icon'`; rename imported symbol `QuibsIcon` → `ZeremiIcon`.
    - Keep `import { cn } from '@/lib/utils'`.
    - Rename `interface QuibsAvatarProps` → `interface ZeremiAvatarProps` (props unchanged: `size: 'message' | 'header' | 'fab'`, `inverted?: boolean`, `className?: string`).
    - `SIZE_CONFIG` per UI-SPEC §Component Rename Contract recalculated for 56:88 aspect: `message: { container: 'h-7 w-7 rounded-lg', icon: { width: 16, height: 25 } }`, `header: { container: 'h-9 w-9 rounded-lg', icon: { width: 20, height: 31 } }`, `fab: { container: 'h-14 w-14 rounded-full', icon: { width: 28, height: 44 } }`.
    - Export `function ZeremiAvatar({ size, inverted = false, className }: ZeremiAvatarProps)` — body identical to `QuibsAvatar` body but render `<ZeremiIcon ... />` instead of `<QuibsIcon ... />`. Gradient classes `bg-gradient-to-br from-primary to-[#14b8a6] text-white` and `bg-white text-primary` inverted variant are unchanged (color tokens carry over per D-04).
    - JSDoc updated to reference Zeremi Z mark (not Quibs Q-face).

    Do NOT delete `components/quibs/` in this task — the deletion happens in Task 3 after HeroMascot and app/page.tsx are updated.

    Commit: `feat(06.5-02): create ZeremiIcon + ZeremiAvatar components (D-04)`.
  </action>
  <verify>
    <automated>test -f components/zeremi/zeremi-icon.tsx &amp;&amp; test -f components/zeremi/zeremi-avatar.tsx &amp;&amp; grep -q 'viewBox="0 0 56 88"' components/zeremi/zeremi-icon.tsx &amp;&amp; grep -q 'fill="currentColor"' components/zeremi/zeremi-icon.tsx &amp;&amp; grep -q 'aria-hidden' components/zeremi/zeremi-icon.tsx &amp;&amp; ! grep -q 'linearGradient' components/zeremi/zeremi-icon.tsx &amp;&amp; grep -q "ZeremiIcon" components/zeremi/zeremi-avatar.tsx &amp;&amp; grep -q "SIZE_CONFIG" components/zeremi/zeremi-avatar.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `components/zeremi/zeremi-icon.tsx` exists.
    - Source assertion: `components/zeremi/zeremi-icon.tsx` contains `viewBox="0 0 56 88"` exactly once: `grep -c 'viewBox="0 0 56 88"' components/zeremi/zeremi-icon.tsx` equals 1.
    - Source assertion: `components/zeremi/zeremi-icon.tsx` contains `fill="currentColor"` on the `<svg>` element and does NOT contain `linearGradient`, `<defs>`, `url(#g)`, or `<stop`: `! grep -E 'linearGradient|<defs>|url\(#g\)|<stop' components/zeremi/zeremi-icon.tsx`.
    - Source assertion: `components/zeremi/zeremi-icon.tsx` contains exactly three `<path` JSX elements: `grep -c '<path' components/zeremi/zeremi-icon.tsx` equals 3.
    - Source assertion: `components/zeremi/zeremi-icon.tsx` contains `aria-hidden="true"`.
    - File assertion: `components/zeremi/zeremi-avatar.tsx` exists.
    - Source assertion: `components/zeremi/zeremi-avatar.tsx` imports `ZeremiIcon` from `@/components/zeremi/zeremi-icon`: `grep -q "from '@/components/zeremi/zeremi-icon'" components/zeremi/zeremi-avatar.tsx`.
    - Source assertion: `components/zeremi/zeremi-avatar.tsx` exports `function ZeremiAvatar`: `grep -q 'export function ZeremiAvatar' components/zeremi/zeremi-avatar.tsx`.
    - Source assertion: `SIZE_CONFIG` keys `message`, `header`, `fab` present: `grep -E 'message:|header:|fab:' components/zeremi/zeremi-avatar.tsx | wc -l` >= 3.
    - Source assertion: gradient class preserved: `grep -q 'bg-gradient-to-br from-primary to-\[#14b8a6\]' components/zeremi/zeremi-avatar.tsx`.
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `components/zeremi/zeremi-icon.tsx` exists as a 56×88 inline SVG component with three `<path>` elements (one Z body + two dot glyphs), `fill="currentColor"` on the `<svg>`, no gradient defs/stops, `aria-hidden="true"`, identical prop signature to `QuibsIcon`. `components/zeremi/zeremi-avatar.tsx` exists as a 1:1 port of `QuibsAvatar` with `ZeremiIcon` swapped in, SIZE_CONFIG recalculated for 56:88 aspect, inverted prop preserved, gradient classes unchanged. Both files type-check.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modify HeroMascot, rename WhyQuibly → WhyZeremi, update app/page.tsx import</name>
  <files>components/sections/hero-mascot.tsx, components/sections/why-zeremi.tsx, components/sections/why-quibly.tsx, app/page.tsx</files>
  <read_first>
    - components/sections/hero-mascot.tsx (current — import path + glyph dimensions 48×56)
    - components/sections/why-quibly.tsx (current — function name, heading copy "Why Quibly", DIFFERENTIATORS array structure)
    - app/page.tsx (current — `import { WhyQuibly } from '@/components/sections/why-quibly'` + `<WhyQuibly />` JSX usage)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§HeroMascot, §why-quibly→why-zeremi, §app/page.tsx — line-number transformations; note: app/page.tsx JSON-LD strings are NOT updated in this plan, only the import; JSON-LD edits are deferred to Wave 2 plan 03)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§HeroMascot Contract: `<ZeremiIcon width={36} height={56} />` inside unchanged 88×88 rounded-3xl teal-gradient wrapper)
  </read_first>
  <action>
    Modify `components/sections/hero-mascot.tsx`:
    - Replace import `import { QuibsIcon } from "@/components/quibs/quibs-icon"` with `import { ZeremiIcon } from "@/components/zeremi/zeremi-icon"`.
    - Replace the inner glyph `<QuibsIcon width={48} height={56} />` with `<ZeremiIcon width={36} height={56} />` (UI-SPEC §HeroMascot Contract — 36:56 ≈ 56:88 aspect; 88px container leaves ~13px margin).
    - JSDoc: replace "Quibs Q-face mascot" with "Zeremi Z mark"; replace "48×56 inner SVG" with "36×56 inner SVG".
    - Container line MUST remain `flex h-[88px] w-[88px] items-center justify-center rounded-3xl bg-gradient-to-br from-primary to-[#14b8a6] text-white` unchanged (D-04: container preserved, only glyph swapped).
    - The wrapping `aria-hidden="true"` attribute is preserved.

    Rename `components/sections/why-quibly.tsx` → `components/sections/why-zeremi.tsx`:
    - Use `git mv components/sections/why-quibly.tsx components/sections/why-zeremi.tsx` to preserve git history.
    - In the renamed file: rename `function WhyQuibly` (and any default export reference) to `function WhyZeremi`. Update JSDoc references to "Why Zeremi". Update heading copy literal `"Why Quibly"` → `"Why Zeremi"`. The DIFFERENTIATORS array structure is unchanged.

    Modify `app/page.tsx`:
    - Replace `import { WhyQuibly } from '@/components/sections/why-quibly'` with `import { WhyZeremi } from '@/components/sections/why-zeremi'`.
    - Replace JSX `<WhyQuibly />` with `<WhyZeremi />`.
    - Do NOT touch the JSON-LD blocks (organizationJsonLd, webSiteJsonLd) or any other Quibly strings in this file — those are updated in Wave 2 plan 03 as part of the broader string sweep. This task is import-swap only.

    Commit: `feat(06.5-02): swap HeroMascot inner glyph, rename WhyQuibly → WhyZeremi, update page import`.
  </action>
  <verify>
    <automated>grep -q "ZeremiIcon" components/sections/hero-mascot.tsx &amp;&amp; ! grep -q "QuibsIcon" components/sections/hero-mascot.tsx &amp;&amp; grep -q "width={36}" components/sections/hero-mascot.tsx &amp;&amp; grep -q "height={56}" components/sections/hero-mascot.tsx &amp;&amp; grep -q "flex h-\[88px\] w-\[88px\]" components/sections/hero-mascot.tsx &amp;&amp; test -f components/sections/why-zeremi.tsx &amp;&amp; ! test -f components/sections/why-quibly.tsx &amp;&amp; grep -q "WhyZeremi" components/sections/why-zeremi.tsx &amp;&amp; grep -q "WhyZeremi" app/page.tsx &amp;&amp; ! grep -q "WhyQuibly" app/page.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `components/sections/hero-mascot.tsx` imports `ZeremiIcon` from `@/components/zeremi/zeremi-icon` and contains no `QuibsIcon` reference: `! grep -q QuibsIcon components/sections/hero-mascot.tsx`.
    - Source assertion: HeroMascot renders `<ZeremiIcon width={36} height={56} />`: `grep -q 'ZeremiIcon width={36} height={56}' components/sections/hero-mascot.tsx`.
    - Source assertion: HeroMascot container className preserved: `grep -q 'h-\[88px\] w-\[88px\].*rounded-3xl.*bg-gradient-to-br from-primary to-\[#14b8a6\].*text-white' components/sections/hero-mascot.tsx`.
    - File assertion: `components/sections/why-zeremi.tsx` exists; `components/sections/why-quibly.tsx` does NOT exist: `test -f components/sections/why-zeremi.tsx && ! test -f components/sections/why-quibly.tsx`.
    - Source assertion: `components/sections/why-zeremi.tsx` exports `WhyZeremi`: `grep -q 'export function WhyZeremi' components/sections/why-zeremi.tsx` (or `export const WhyZeremi` if file used that form — verify with the symbol name `WhyZeremi`).
    - Source assertion: `components/sections/why-zeremi.tsx` heading copy `Why Zeremi` (not `Why Quibly`): `grep -q 'Why Zeremi' components/sections/why-zeremi.tsx && ! grep -q 'Why Quibly' components/sections/why-zeremi.tsx`.
    - Source assertion: `app/page.tsx` imports `WhyZeremi` from `@/components/sections/why-zeremi` and JSX uses `<WhyZeremi />`: both `grep -q "from '@/components/sections/why-zeremi'" app/page.tsx` and `grep -q '<WhyZeremi' app/page.tsx` exit 0; `! grep -q 'WhyQuibly' app/page.tsx`.
    - Git history preserved: `git log --follow --oneline components/sections/why-zeremi.tsx | head -1` shows a commit (rename detected).
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    HeroMascot uses ZeremiIcon at 36×56 inside the unchanged 88×88 teal-gradient container. `why-quibly.tsx` is renamed to `why-zeremi.tsx` via `git mv`; the function is `WhyZeremi`; heading reads "Why Zeremi". `app/page.tsx` imports `WhyZeremi` from the new path and the JSX uses `<WhyZeremi />`. Type-checks clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete components/quibs/ directory (CD-04)</name>
  <files>components/quibs/quibs-icon.tsx, components/quibs/quibs-avatar.tsx, components/quibs/</files>
  <read_first>
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md (CD-04 — Quibs assets and scripts to delete)
    - components/quibs/ directory listing (`ls components/quibs/`)
  </read_first>
  <action>
    Verify there are no remaining importers of `@/components/quibs/*` in the source tree before deletion:
    - Run `grep -r "components/quibs" app/ components/ lib/ emails/ scripts/ tests/ 2>/dev/null` — must return zero matches. If matches exist, stop and surface the offending file paths (they should have been updated in Task 2 or a previous task; do not auto-fix here).

    Use `git rm` to delete the directory cleanly (preserves git history for the deletion):
    - `git rm components/quibs/quibs-icon.tsx`
    - `git rm components/quibs/quibs-avatar.tsx`
    - The empty `components/quibs/` directory is removed by git automatically once both files are deleted.

    Commit: `chore(06.5-02): delete components/quibs/ directory (CD-04)`.
  </action>
  <verify>
    <automated>! test -d components/quibs &amp;&amp; ! test -f components/quibs/quibs-icon.tsx &amp;&amp; ! test -f components/quibs/quibs-avatar.tsx &amp;&amp; [ -z "$(grep -rl 'components/quibs' app/ components/ lib/ emails/ scripts/ tests/ 2>/dev/null)" ] &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Filesystem assertion: `components/quibs/` directory does not exist: `! test -d components/quibs`.
    - Source assertion: zero source files outside `.planning/` and `node_modules/` reference `components/quibs`: `grep -rl 'components/quibs' app/ components/ lib/ emails/ scripts/ tests/ 2>/dev/null | wc -l` equals 0.
    - Type check: `npx tsc --noEmit` exits 0.
    - Git assertion: the two files appear in `git status` as deleted (staged) before commit; after commit, `git log --diff-filter=D --name-only -1 | grep -E 'components/quibs/(quibs-icon|quibs-avatar)\.tsx'` shows them.
  </acceptance_criteria>
  <done>
    `components/quibs/` no longer exists in the working tree. Zero source files import from `components/quibs`. Deletion is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component prop API | ZeremiIcon/ZeremiAvatar must preserve the exact prop signatures of QuibsIcon/QuibsAvatar so any future call site (e.g. chat widget) remains compatible |
| inline SVG → DOM | Inline SVG with `fill='currentColor'` — gradient defs stripped to avoid ID collisions in multi-SVG pages (Pitfall 7) |
| filesystem deletion | `git rm` removes Quibs files; importer scan before deletion prevents broken builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-02-01 | Tampering | components/zeremi/zeremi-icon.tsx | mitigate | grep gate verifies `linearGradient`, `<defs>`, `url(#g)`, `<stop` absent — prevents Pitfall 7 (gradient ID collision in multi-SVG pages) and ensures `fill='currentColor'` drives color from parent text utility. |
| T-065-02-02 | Information Disclosure | Stale QuibsIcon import surviving in source | mitigate | Task 3 grep scan over `app/ components/ lib/ emails/ scripts/ tests/` blocks deletion if any importer remains; type-check confirms no dangling references. CI grep guard (Wave 4 plan 05) locks this permanently. |
| T-065-02-03 | Denial of Service | HeroMascot dimension regression degrading LCP | accept | UI-SPEC §Hero Layout Contract holds DOM-first `<h1>` LCP rule; 36×56 inner SVG is smaller than prior 48×56 (lower paint cost). Phase 2 D-03 LCP assertion re-runs in Wave 4 e2e. |
| T-065-02-04 | Tampering | Renamed file losing git history | mitigate | `git mv` (Task 2) and `git rm` (Task 3) preserve history; `git log --follow` verified in acceptance criteria. |
</threat_model>

<verification>
- `components/zeremi/` directory contains exactly two files (`zeremi-icon.tsx`, `zeremi-avatar.tsx`).
- `components/quibs/` directory removed; no source file imports from it.
- HeroMascot renders ZeremiIcon at 36×56 inside unchanged 88×88 container.
- `git mv` history preserved for `why-quibly.tsx` → `why-zeremi.tsx`.
- `npx tsc --noEmit` exits 0.
- `npm run dev` boots; landing page renders Z mark in hero (manual visual sanity).
</verification>

<success_criteria>
- ZeremiIcon + ZeremiAvatar exist with correct viewBox, fill, prop signatures, gradient classes.
- HeroMascot uses ZeremiIcon at 36×56; container unchanged.
- WhyQuibly renamed to WhyZeremi via `git mv`; app/page.tsx import updated.
- `components/quibs/` directory deleted; no dangling importers.
- Three atomic commits on `rebrand/zeremi` branch.
</success_criteria>

<output>
Create `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-02-SUMMARY.md` when done.
</output>
