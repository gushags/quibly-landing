---
phase: 06-production-deploy-cutover-runbook
plan: 04
type: execute
wave: 3
depends_on: [03, 05]
files_modified:
  - .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
  - .planning/phases/06-production-deploy-cutover-runbook/screenshots/
  - docs/cutover.md
requirements:
  - DEPLOY-09
autonomous: false
tags:
  - dry-run
  - cutover-rehearsal
  - manual-checkpoint
  - phase-6
user_setup:
  - service: vercel
    why: "Cross-project domain transfer dry-run on staging.useQuibly.com between quibly-landing and marketing-app projects (D-05/D-06/D-07)"
    env_vars: []
    dashboard_config:
      - task: "Confirm both quibly-landing and marketing-app Vercel projects exist on the same Vercel team and that the operator has Owner/Member permissions on both"
        location: "Vercel Dashboard → Team scope → Projects list"
      - task: "Add staging.useQuibly.com subdomain to quibly-landing project"
        location: "Vercel Dashboard → quibly-landing → Settings → Domains → Add Domain"
      - task: "Transfer staging.useQuibly.com to marketing-app via in-use prompt"
        location: "Vercel Dashboard → marketing-app → Settings → Domains → Add Domain → confirm in-use prompt"
      - task: "Transfer staging.useQuibly.com BACK to quibly-landing via in-use prompt"
        location: "Vercel Dashboard → quibly-landing → Settings → Domains → Add Domain → confirm in-use prompt"
  - service: dns-provider
    why: "If apex `useQuibly.com` is on external nameservers (NOT ns1/ns2.vercel-dns.com), staging CNAME must be added manually before Vercel will validate the domain (CD-05 / Pitfall 5)"
    env_vars: []
    dashboard_config:
      - task: "If `dig +short ns useQuibly.com` returns external nameservers: add CNAME record `staging` → `cname.vercel-dns.com` at the external DNS provider; wait 1–10 min for propagation"
        location: "External DNS provider (Cloudflare / NameSilo / registrar UI)"
must_haves:
  truths:
    - "staging.useQuibly.com subdomain bound to quibly-landing Vercel project at team level (auto-CNAME if Vercel NS, else manual CNAME at external provider per CD-05)"
    - "staging.useQuibly.com loads from quibly-landing with HTTP 200 (smoke 1)"
    - "staging.useQuibly.com transferred atomically to marketing-app project via Vercel in-use prompt (D-05/D-06)"
    - "staging.useQuibly.com loads from marketing-app with HTTP 200 (smoke 2)"
    - "staging.useQuibly.com transferred BACK to quibly-landing project (D-05)"
    - "staging.useQuibly.com loads from quibly-landing again (smoke 3)"
    - "All 4 dry-run screenshots captured and recorded in 06-UAT.md test 11"
    - "Production apex useQuibly.com unaffected by the dry-run (06-UAT.md test 12 pass)"
    - "Exact Vercel button label observed and recorded; if it differs from docs/cutover.md Step 5 prose, cutover.md is updated to match reality"
    - "D-07 scope respected: NO Resend writes during dry-run, NO separate staging sender configured"
  artifacts:
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-1-bound.png"
      provides: "Evidence: staging.useQuibly.com bound to quibly-landing, Valid Configuration"
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-2-prompt.png"
      provides: "Evidence: Vercel in-use prompt verbatim"
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-3-marketing-load.png"
      provides: "Evidence: marketing-app loading at staging.useQuibly.com"
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-4-back-to-landing.png"
      provides: "Evidence: quibly-landing serving at staging.useQuibly.com after transfer-back"
  key_links:
    - from: "Vercel UI in-use prompt"
      to: "docs/cutover.md Step 5 prose"
      via: "screenshot 2 + recorded button label; cutover.md updated if labels differ"
      pattern: "in-use|This domain is currently in use"
    - from: "06-UAT.md test 11"
      to: "screenshots/ directory artifacts"
      via: "4 PNG file path references in expected: block"
      pattern: "screenshots/06-uat-11-"
---

<objective>
Execute the cutover dry-run end-to-end on `staging.useQuibly.com` (DEPLOY-09 / D-05/D-06/D-07). Bind the staging subdomain to `quibly-landing`, smoke-test, transfer to `marketing-app` via the Vercel in-use prompt, smoke-test, transfer back, smoke-test. Capture 4 screenshots as evidence. Record actual button labels and propagation timing into `06-UAT.md` test 11; update `docs/cutover.md` Step 5 if observed UI labels diverge from runbook prose.

Purpose: 06-RESEARCH.md flagged the Vercel cross-project transfer UI as the single MEDIUM-confidence research item — UI flow specifics (prompt label, timing, edge-case behaviors) only manifest in a real attempt. This dry-run resolves that ambiguity BEFORE the real cutover happens with marketing-app at the apex. D-07 keeps scope tight: mechanics + smoke load only, no Resend writes.

Output: 4 screenshot artifacts under `.planning/phases/06-.../screenshots/`; `06-UAT.md` test 11 + test 12 flipped from `pending` to `pass` (or `fail` with documented gap); `docs/cutover.md` Step 5 button label updated if reality differs.

This plan is `autonomous: false` — Vercel UI flow + cross-project transfer requires founder hands on the dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md
@.planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md
@.planning/phases/06-production-deploy-cutover-runbook/06-PATTERNS.md
@.planning/phases/06-production-deploy-cutover-runbook/06-VALIDATION.md
@.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
@docs/cutover.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight DNS check + branch CD-05 sub-flow</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md (CD-05 staging-DNS branch)
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pattern 3 lines 289–306 (the two scenarios + dig command)
    - .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md test 11 expected: block
  </read_first>
  <action>
From a fresh terminal, run:

```bash
dig +short ns useQuibly.com
```

Branch the dry-run path based on the response:

- **Scenario A (Vercel-managed nameservers):** Response contains `ns1.vercel-dns.com.` and `ns2.vercel-dns.com.` → CD-05 sub-flow A applies. The `staging.useQuibly.com` CNAME will auto-create when the subdomain is added in the Vercel project. No external DNS provider action needed. Proceed directly to Task 2.

- **Scenario B (External nameservers):** Response shows external nameservers (Cloudflare / NameSilo / etc.) → CD-05 sub-flow B applies. BEFORE Task 2: log into the external DNS provider, add a CNAME record `staging` → `cname.vercel-dns.com`, save, wait 1–10 minutes for propagation. Confirm propagation with:
  ```bash
  dig +short cname staging.useQuibly.com
  ```
  Expected: a value containing `vercel-dns.com` once propagated. Only then proceed to Task 2.

Record the dig output verbatim and the branch (A or B) chosen into `06-UAT.md` as a brief note prepended to test 11's `note:` field — example format: `Pre-flight: dig +short ns useQuibly.com returned ns1.vercel-dns.com, ns2.vercel-dns.com → sub-flow A (auto-CNAME).`

DO NOT touch nameservers themselves (Pitfall 4). DO NOT add records other than the staging CNAME. DO NOT modify any apex DNS record (SPF/DKIM/DMARC/Return-Path).
  </action>
  <verify>
    <automated>dig +short ns useQuibly.com | head -3 && grep -q "Pre-flight" .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</automated>
  </verify>
  <acceptance_criteria>
- `dig +short ns useQuibly.com` produces non-empty output (DNS resolution works)
- 06-UAT.md contains the prefix string `Pre-flight:` somewhere in test 11's body or note (the dig output + sub-flow choice recorded)
- If sub-flow B was chosen: `dig +short cname staging.useQuibly.com` returns a `vercel-dns.com` host before Task 2 begins
- No DNS records other than staging CNAME (sub-flow B only) were modified
  </acceptance_criteria>
  <done>
Nameserver setup determined; sub-flow A or B chosen and recorded in 06-UAT.md. If B, the staging CNAME is propagated and verifiable via dig.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Bind staging.useQuibly.com to quibly-landing + screenshot 1</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-1-bound.png</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md lines 635–671 (verbatim PRE-FLIGHT step)
    - .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md test 11 expected: block
  </read_first>
  <what-built>
Task 1 has determined the nameserver setup; if sub-flow B, the external CNAME has been added and propagated. Now: bind the staging subdomain to the quibly-landing Vercel project.
  </what-built>
  <how-to-verify>
1. Open Vercel Dashboard → switch to the team scope owning quibly-landing.
2. Navigate to: quibly-landing → Settings → Domains.
3. Click "Add Domain" → enter exactly: `staging.useQuibly.com` → confirm.
4. Wait for the status indicator. Expected target state: "Valid Configuration" with green checkmark.
   - Sub-flow A (Vercel NS): completes in <60s.
   - Sub-flow B (external NS): completes after CNAME propagation (already verified in Task 1); typically <60s after that.
5. From a fresh terminal, smoke test:
   ```bash
   curl -sI https://staging.useQuibly.com | head -5
   ```
   Expected: HTTP/2 200 with an `x-vercel-id:` header in the response.
6. **Take screenshot 1** of the Vercel Dashboard showing `staging.useQuibly.com` listed under quibly-landing's domains with "Valid Configuration" status. Save at exactly:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-1-bound.png
   ```
   Create the `screenshots/` directory if it does not exist:
   ```bash
   mkdir -p .planning/phases/06-production-deploy-cutover-runbook/screenshots
   ```
7. Update 06-UAT.md test 11's `note:` field to record the curl output line and confirm the screenshot path.

If the Vercel Dashboard shows "Invalid Configuration" or "Pending" longer than 5 minutes after CNAME propagation: do NOT proceed. Re-check `dig +short cname staging.useQuibly.com` from a fresh resolver, escalate to a separate diagnostic session.

DO NOT add any other domains, env vars, or project settings during this step. Touch only the staging subdomain.
  </how-to-verify>
  <resume-signal>
Type "approved + screenshot saved" once: (a) `staging.useQuibly.com` shows Valid Configuration in Vercel, (b) `curl -sI https://staging.useQuibly.com` returns 200, (c) screenshot 1 is saved at the exact path. Or describe the failure mode (e.g., "Pending Configuration after 10 min — sub-flow B propagation not complete").
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-1-bound.png` returns success (file exists, non-zero bytes)
- `curl -sI https://staging.useQuibly.com | head -1` returns `HTTP/2 200` (or `HTTP/1.1 200`)
- 06-UAT.md test 11 note: contains the screenshot 1 reference and the curl output
- No other domain or project setting changed in Vercel (visual confirmation in Dashboard)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Atomic-transfer staging.useQuibly.com to marketing-app + screenshots 2 + 3</name>
  <files>
    .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-2-prompt.png,
    .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-3-marketing-load.png,
    docs/cutover.md
  </files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md lines 651–658 (verbatim TRANSFER STAGING step) and lines 261–266 (sub-flow A vs B)
    - docs/cutover.md Step 5 (current prose to compare against observed UI)
  </read_first>
  <what-built>
Task 2 confirmed staging.useQuibly.com loads from quibly-landing. Now: trigger the Vercel in-use prompt by adding the same domain to the marketing-app project. This is the actual cross-project transfer that resolves the MEDIUM-confidence research flag.
  </what-built>
  <how-to-verify>
1. In Vercel Dashboard, navigate to: marketing-app → Settings → Domains.
2. Click "Add Domain" → enter exactly: `staging.useQuibly.com` → confirm.
3. **Expected behavior:** Vercel detects the in-use state and surfaces a prompt with text along the lines of "This domain is currently in use by another project. Move it here?" (per 06-RESEARCH.md sub-flow A).
4. **BEFORE clicking confirm:** take screenshot 2 of the prompt verbatim. Save at exactly:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-2-prompt.png
   ```
5. Note the EXACT button label (e.g., "Move", "Transfer", "Continue", "Confirm"). Record the literal label text in 06-UAT.md test 11's `note:` field.
6. **Compare the observed prompt and button label to docs/cutover.md Step 5 prose.** If the prose says "in-use prompt" but the actual UI uses different wording: edit `docs/cutover.md` Step 5 to match reality. Use the Edit tool — touch ONLY Step 5 prose, do NOT restructure other sections. Re-run the line-count check (still 200–500 lines) afterward.
7. Confirm the prompt → atomic transfer.
8. From a fresh terminal, smoke test:
   ```bash
   curl -sI https://staging.useQuibly.com | head -5
   ```
   Expected: HTTP/2 200 from marketing-app's deployment. Verify by inspecting the response body or the `x-matched-path:` / `x-vercel-id:` header for marketing-app project markers.
9. **Take screenshot 3** showing marketing-app's content rendering at `https://staging.useQuibly.com`. Save at exactly:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-3-marketing-load.png
   ```
10. Record approximate transfer time (target <5s) and propagation observations in 06-UAT.md.

**Sub-flow B fallback (only if step 3 fails to surface the prompt):** Per 06-RESEARCH.md lines 263–266 — `vercel alias set <marketing-app-deployment-url> staging.useQuibly.com`, then remove from quibly-landing → Add Domain at marketing-app cleanly. Document this divergence in 06-UAT.md as a `severity: major` gap and update docs/cutover.md Step 5 to add a sub-flow B note. DO NOT delete from quibly-landing FIRST in any path — that creates a downtime gap.

D-07 scope reminder: do NOT submit a real signup against marketing-app at staging.useQuibly.com (no Resend writes during dry-run). The smoke test is a `curl` only.
  </how-to-verify>
  <resume-signal>
Type "approved + 2 screenshots saved" once: (a) screenshot 2 shows the in-use prompt, (b) screenshot 3 shows marketing-app loading at staging.useQuibly.com, (c) curl returns 200 from marketing-app, (d) docs/cutover.md Step 5 reflects observed UI labels (or no edit needed if labels match). Or describe the failure: "Prompt did NOT appear; falling back to sub-flow B" / "Transfer took >30s; documenting as gap."
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-2-prompt.png` returns success
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-3-marketing-load.png` returns success
- `curl -sI https://staging.useQuibly.com | head -1` returns `HTTP/2 200` (now from marketing-app)
- 06-UAT.md test 11 note: contains the EXACT button label observed (literal string, not paraphrased)
- 06-UAT.md test 11 note: contains the approximate transfer time (e.g., "<5s" or "8s observed")
- If the observed button label differs from docs/cutover.md Step 5: docs/cutover.md was edited and `wc -l docs/cutover.md` still returns between 200 and 500
- staging.useQuibly.com is no longer listed under quibly-landing → Settings → Domains (visual confirmation)
- staging.useQuibly.com IS listed under marketing-app → Settings → Domains (visual confirmation)
- No Resend audience writes occurred during this task (D-07 scope confirmed by Resend Dashboard → Audiences row count unchanged from pre-task)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Atomic-transfer staging.useQuibly.com BACK to quibly-landing + screenshot 4</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-4-back-to-landing.png</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md lines 659–663 (verbatim TRANSFER BACK step)
    - 06-UAT.md test 11 expected: block (subtest 3)
  </read_first>
  <what-built>
Task 3 transferred staging.useQuibly.com to marketing-app. Now: transfer back to quibly-landing to complete the dry-run round-trip. This validates that the same UI flow is reversible.
  </what-built>
  <how-to-verify>
1. In Vercel Dashboard, navigate to: quibly-landing → Settings → Domains.
2. Click "Add Domain" → enter exactly: `staging.useQuibly.com` → confirm.
3. The same in-use prompt should appear (this time saying the domain is in use by marketing-app).
4. Confirm → atomic transfer back to quibly-landing.
5. From a fresh terminal, smoke test:
   ```bash
   curl -sI https://staging.useQuibly.com | head -5
   ```
   Expected: HTTP/2 200 from quibly-landing.
6. **Take screenshot 4** showing quibly-landing's content rendering at `https://staging.useQuibly.com` (post transfer-back). Save at exactly:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-4-back-to-landing.png
   ```
7. Update 06-UAT.md test 11's `note:` field with the final state and full timeline (e.g., "T0: bound to landing; T+2m: transferred to marketing-app; T+4m: transferred back to landing; total dry-run duration N minutes").
8. Flip 06-UAT.md test 11 from `result: pending` to `result: pass` (assuming all 4 sub-steps PASS); if any sub-step failed or behaved unexpectedly, set `result: issue` and document the deviation as a YAML gap entry under `## Gaps` per the 05-UAT.md schema.
  </how-to-verify>
  <resume-signal>
Type "approved + dry-run complete" once: (a) screenshot 4 shows quibly-landing serving at staging.useQuibly.com, (b) curl returns 200 from quibly-landing, (c) 06-UAT.md test 11 result is set (pass or issue with gap entry), (d) all 4 screenshots are present at the documented paths.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-4-back-to-landing.png` returns success
- All 4 screenshots present: `ls .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-*.png | wc -l` returns 4
- `curl -sI https://staging.useQuibly.com | head -1` returns `HTTP/2 200` (back to quibly-landing)
- 06-UAT.md test 11 has `result: pass` OR `result: issue` (NOT `pending`)
- If `result: issue`: 06-UAT.md `## Gaps` section contains a YAML gap entry per 05-UAT.md schema (truth, status, reason, severity, test, root_cause, artifacts, missing, debug_session)
- staging.useQuibly.com IS listed under quibly-landing → Settings → Domains (visual confirmation)
- staging.useQuibly.com is no longer listed under marketing-app → Settings → Domains (visual confirmation)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Verify production apex untouched + flip 06-UAT.md test 12</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - 06-UAT.md test 12 expected: block
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pitfall 3 / Architecture Pattern 2 (DNS records + apex unaffected by subdomain transfers)
  </read_first>
  <action>
After Task 4 completes, verify the production apex was untouched by the staging-subdomain dry-run (different domain, different Vercel routing entry).

From a fresh terminal, run:

```bash
curl -sI https://useQuibly.com | head -1
# Expected: HTTP/2 200

curl -sI https://useQuibly.com | grep -i strict-transport
# Expected: strict-transport-security: max-age=300

curl -sI https://useQuibly.com | grep -iE "x-frame-options|x-content-type-options|referrer-policy|permissions-policy"
# Expected: 4 lines, all matching the values from D-11
```

Paste all command outputs verbatim into 06-UAT.md test 12's `note:` field.

Flip test 12 from `result: pending` to `result: pass` (assuming all checks PASS). If HSTS shows `preload` or `includeSubDomains`, set `result: fail` with `severity: blocker` — this would mean Plan 06-01's `next.config.ts` change regressed during deploy and must be debugged before any further go-live work.

Update 06-UAT.md `## Summary` block: increment `passed` for tests 11 and 12 (e.g., from `pending: 12` / `passed: 0` to `pending: 10` / `passed: 2`). DO NOT touch counts for tests 1–10 (those flip during Plan 06-05).

Update the frontmatter `updated:` field to the current ISO timestamp.
  </action>
  <verify>
    <automated>curl -sI https://useQuibly.com | head -1 | grep -q "200" && curl -sI https://useQuibly.com | grep -i strict-transport | grep -q "max-age=300" && curl -sI https://useQuibly.com | grep -i strict-transport | grep -vqE "preload|includeSubDomains" && grep -A2 '^### 12' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md | grep -qE "result: (pass|fail|issue)"</automated>
  </verify>
  <acceptance_criteria>
- `curl -sI https://useQuibly.com | head -1` returns `HTTP/2 200`
- `curl -sI https://useQuibly.com | grep -i strict-transport` returns a line containing `max-age=300`
- `curl -sI https://useQuibly.com | grep -i strict-transport` does NOT contain `preload` or `includeSubDomains`
- `curl -sI https://useQuibly.com` emits all 4 other hardening headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy)
- 06-UAT.md test 12 has `result: pass` (or `result: fail` if HSTS regressed; in that case, halt Phase 6 and trigger debug session)
- 06-UAT.md `## Summary` block reflects 2 tests passed (tests 11 + 12)
- 06-UAT.md frontmatter `updated:` timestamp is current
  </acceptance_criteria>
  <done>
Production apex is unaffected by the dry-run. The 5 hardening headers still emit correctly with HSTS literal `max-age=300`. 06-UAT.md tests 11 and 12 are flipped from pending to pass (or issue/fail with documented gap). The dry-run is complete; the Vercel cross-project transfer flow is validated for the future real cutover.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Vercel UI | Manual UI operations across two projects in the same team; trust the platform but verify each transfer with a smoke curl |
| staging.useQuibly.com → DNS resolvers | Subdomain DNS state propagation may lag |
| Production apex → dry-run scope | The dry-run subject is staging.* — the apex must remain untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-V14-DRYRUN-APEX-LEAK | Tampering (apex affected by subdomain dry-run) | useQuibly.com production routing | mitigate | Task 5 verifies apex still serves quibly-landing with 5 hardening headers intact post-dry-run. ASVS V14.x. |
| T-06-V14-RESEND-WRITE-LEAK | Information Disclosure (D-07 violation; Resend writes during dry-run) | Resend audience contacts | mitigate | D-07 explicit; Task 3 acceptance criteria require Resend Audience row count unchanged from pre-task baseline. |
| T-06-V9-DOWNTIME-GAP | Denial of Service (visitor-facing downtime during transfer) | staging.useQuibly.com | mitigate | Atomic-transfer flow is documented zero-downtime per Vercel; Task 3 records observed transfer time. If >30s gap observed, document as gap (informs cutover.md Step 5 with realistic propagation expectation). |
| T-06-V9-PROMPT-DIVERGENCE | Repudiation (cutover.md prose stale relative to UI) | docs/cutover.md Step 5 | mitigate | Task 3 mandates editing docs/cutover.md Step 5 if observed labels differ from prose. ASVS V14.x (config doc accuracy). |
| T-06-V14-DNS-MUTATION | Denial of Service (apex DNS records mutated during dry-run) | apex DNS zone | mitigate | Task 1 explicit constraint (Pitfall 4 — DO NOT touch nameservers); Task 5 verifies SPF/DKIM/DMARC unaffected (could add separate dig probes if needed; covered by Plan 06-05's exhaustive DNS verification). |
</threat_model>

<verification>
1. Pre-flight nameserver check completes; sub-flow A or B chosen (Task 1).
2. staging.useQuibly.com bound to quibly-landing, screenshot 1 saved (Task 2).
3. Atomic transfer to marketing-app via in-use prompt; screenshots 2 + 3 saved; observed button label recorded; docs/cutover.md Step 5 updated if needed (Task 3).
4. Atomic transfer back to quibly-landing; screenshot 4 saved; test 11 result flipped (Task 4).
5. Production apex unaffected; test 12 result flipped (Task 5).
6. 4 screenshots present; 2 06-UAT.md tests transitioned from pending to pass.
</verification>

<success_criteria>
- All 4 dry-run screenshots saved at the documented paths.
- 06-UAT.md test 11 result transitioned from `pending` to `pass` (or `issue` with gap entry).
- 06-UAT.md test 12 result transitioned from `pending` to `pass`.
- docs/cutover.md Step 5 prose accurately reflects the observed Vercel UI button label.
- Production apex `https://useQuibly.com` is unaffected; 5 hardening headers still emit; HSTS literal value remains `max-age=300`.
- staging.useQuibly.com final state: bound to quibly-landing, serves 200.
- No Resend audience writes occurred during the dry-run (D-07 scope respected).
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-deploy-cutover-runbook/06-04-SUMMARY.md` documenting:
- Sub-flow chosen (A or B) and dig output
- Observed Vercel button label (literal string)
- Approximate per-transfer duration
- 4 screenshot paths
- Any docs/cutover.md Step 5 edits made (paste diff)
- 06-UAT.md tests 11 + 12 final result values
- Any gaps documented in `## Gaps`
</output>
