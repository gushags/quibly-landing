---
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
plan: 05
type: execute
wave: 4
depends_on:
  - 06.5-03
  - 06.5-04
files_modified:
  - scripts/check-no-quibly.mjs
  - .github/workflows/test.yml
  - tests/branding.test.ts
  - tests/e2e/brand-paint.spec.ts
  - playwright.config.ts
  - package.json
autonomous: true
requirements:
  - D-03
  - CD-05
  - SC-5
must_haves:
  truths:
    - "D-03: CI grep guard step in .github/workflows/test.yml fails the build if any of /Quibly|Quibs|useQuibly\\.com/ reappear in source (excluding .planning/, node_modules/, generated files); the script form (per CD-05) mirrors scripts/check-no-trackers.mjs"
    - "scripts/check-no-quibly.mjs exists, mirrors scripts/check-no-trackers.mjs, and exits 0 on the current repo state (after Wave 2 + plan 04 have completed)"
    - "scripts/check-no-quibly.mjs exits 1 if any of /Quibly|Quibs|useQuibly\\.com|usequibly\\.com|Quicksand/i appears in source under app/, components/, lib/, scripts/, tests/, emails/, public/ (excluding .planning/, node_modules/, .next/, dist/, playwright-report/, coverage/, and the script itself)"
    - "Vitest tests/branding.test.ts asserts no brand string appears in rendered RSC HTML of layout, page, sitemap, robots, privacy, terms (VALIDATION.md Wave 0 requirement)"
    - "Playwright tests/e2e/brand-paint.spec.ts asserts Bree Serif loads in the network panel, Quicksand does NOT, and the gradient Z mark renders in the hero (VALIDATION.md Wave 0)"
    - ".github/workflows/test.yml runs node scripts/check-no-quibly.mjs as a step that fails the build on regression"
  artifacts:
    - path: scripts/check-no-quibly.mjs
      provides: "Repo-wide grep guard; recursive walk of source dirs; regex denylist; exit 1 on match"
      contains: "DENYLIST_PATTERNS"
      min_lines: 50
    - path: tests/branding.test.ts
      provides: "Vitest RSC string assertions for layout metadata, sitemap, robots, legal page bodies"
      contains: "Zeremi"
    - path: tests/e2e/brand-paint.spec.ts
      provides: "Playwright check: Bree Serif font request observed; Quicksand absent; Z mark visible"
      contains: "Bree Serif"
    - path: .github/workflows/test.yml
      provides: "CI step invoking node scripts/check-no-quibly.mjs"
      contains: "check-no-quibly"
  key_links:
    - from: .github/workflows/test.yml
      to: scripts/check-no-quibly.mjs
      via: "shell step `node scripts/check-no-quibly.mjs`"
      pattern: "node scripts/check-no-quibly\\.mjs"
    - from: tests/e2e/brand-paint.spec.ts
      to: app/layout.tsx + components/sections/hero-mascot.tsx
      via: "Playwright network capture + DOM screenshot/visibility assertion"
      pattern: "bree-serif"
---

<objective>
Wave 4: lock the rebrand mechanically. Create `scripts/check-no-quibly.mjs` (CD-05 — standalone Node script mirroring `scripts/check-no-trackers.mjs`), add the VALIDATION.md Wave 0 tests (`tests/branding.test.ts` Vitest + `tests/e2e/brand-paint.spec.ts` Playwright), and wire the grep guard as a CI step in `.github/workflows/test.yml`.

This is the LAST plan before the human-gated infra wave (plan 06) — it depends on plan 03 AND plan 04 because the grep guard MUST find zero violations on its first run. Activating the guard before the sweep completes would block every intermediate commit (RESEARCH §Pitfall 1, §Anti-Patterns).

Per D-03 the script is the load-bearing enforcement; pre-commit-only would be bypassable with `--no-verify`. Per CD-05 the script form is preferred over an inline `git grep` step (auditable, re-runnable, matches the Phase 5 `check-no-trackers.mjs` pattern).

Purpose: Permanently prevent rebrand drift in source. After this plan, any commit re-introducing `Quibly|Quibs|useQuibly.com|Quicksand` (outside `.planning/`) fails CI.

Output: Grep guard script + two Wave 0 tests + CI wiring; all run green on the current rebrand branch state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-VALIDATION.md
@scripts/check-no-trackers.mjs
@.github/workflows/test.yml
@playwright.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/check-no-quibly.mjs (CD-05 — mirror check-no-trackers.mjs pattern)</name>
  <files>scripts/check-no-quibly.mjs, package.json</files>
  <read_first>
    - scripts/check-no-trackers.mjs (analog — error message + denylist + exit pattern; ~94 lines)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§scripts/check-no-quibly.mjs + §Pattern C — script skeleton + workflow step)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pitfall 1 — activation sequencing; §Anti-pattern — must exclude `.planning/` to avoid false positive on the phase slug)
  </read_first>
  <action>
    Create `scripts/check-no-quibly.mjs` (ESM, Node stdlib only):
    - `#!/usr/bin/env node` shebang.
    - Imports: `import { readFileSync, readdirSync, statSync } from 'node:fs'; import { join, relative } from 'node:path'`.
    - Constants:
      - `DENYLIST_PATTERNS = [/Quibly/i, /Quibs/i, /useQuibly\.com/i, /usequibly\.com/i, /Quicksand/i]`.
      - `INCLUDE_DIRS = ['app', 'components', 'emails', 'lib', 'scripts', 'tests', 'public']` (relative to repo root).
      - `EXCLUDE_DIRS = new Set(['.planning', 'node_modules', '.next', 'dist', 'playwright-report', 'coverage', '.git', 'brand'])` — `.planning` is explicitly excluded (RESEARCH Anti-pattern: the phase slug `06.5-quibly-zeremi-...` would false-positive); `brand` excludes the brand asset directory naming under `public/`.
      - `EXCLUDE_FILES = new Set(['scripts/check-no-quibly.mjs'])` — this script mentions the patterns; must self-exclude.
      - `BINARY_EXTENSIONS = new Set(['.png', '.jpg', '.jpeg', '.webp', '.gif', '.ico', '.woff', '.woff2', '.ttf', '.otf', '.eot', '.mp4', '.pdf'])` — skip binary files to avoid false matches on decoded bytes.
    - Recursive walk of each `INCLUDE_DIRS` entry: for each file, skip if any path component is in `EXCLUDE_DIRS`, skip if the relative path is in `EXCLUDE_FILES`, skip if extension in `BINARY_EXTENSIONS`. Read file as UTF-8, test each `DENYLIST_PATTERNS` regex against the content. Record violations as `{ file, pattern, lineNumber, lineContent }`.
    - On violations: print `Phase 6.5 rebrand violation: prohibited brand strings found in source:`, then per-violation lines `  ${file}:${lineNumber}: matches ${pattern} — ${lineContent.trim()}`, then `process.exit(1)`.
    - On no violations: print `Phase 6.5: no prohibited brand strings (Quibly|Quibs|useQuibly.com|Quicksand) in source`, `process.exit(0)`.

    Add npm script: `"check:no-quibly": "node scripts/check-no-quibly.mjs"` in `package.json`.

    Run the script LOCALLY before committing: `node scripts/check-no-quibly.mjs` MUST exit 0. If it does not, stop — plans 03/04 missed something and must be fixed before this plan can land (per Pitfall 1 sequencing rule).

    Commit: `feat(06.5-05): add scripts/check-no-quibly.mjs CI grep guard (D-03, CD-05)`.
  </action>
  <verify>
    <automated>test -f scripts/check-no-quibly.mjs &amp;&amp; grep -q "DENYLIST_PATTERNS" scripts/check-no-quibly.mjs &amp;&amp; grep -q "EXCLUDE_DIRS" scripts/check-no-quibly.mjs &amp;&amp; grep -q "'.planning'" scripts/check-no-quibly.mjs &amp;&amp; node scripts/check-no-quibly.mjs &amp;&amp; grep -q '"check:no-quibly"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `scripts/check-no-quibly.mjs` exists.
    - Source assertion: `DENYLIST_PATTERNS` array references all five tokens — `grep -E 'Quibly|Quibs|useQuibly|usequibly|Quicksand' scripts/check-no-quibly.mjs | wc -l` >= 5.
    - Source assertion: `EXCLUDE_DIRS` set contains `'.planning'`: `grep -q "'.planning'" scripts/check-no-quibly.mjs`.
    - Source assertion: the script self-excludes — contains `'scripts/check-no-quibly.mjs'` in `EXCLUDE_FILES`: `grep -q "'scripts/check-no-quibly.mjs'" scripts/check-no-quibly.mjs`.
    - Behavior assertion: `node scripts/check-no-quibly.mjs` exits 0 on the current repo state (the load-bearing gate — if it fails, plan 03 or 04 has a residual violation).
    - Regression smoke: temporarily inject `// Quibly` into a scratch file under `app/` and re-run; verify exit 1; restore the file. (Executor-verified, not auto-asserted.)
    - package.json assertion: `grep -q '"check:no-quibly"' package.json`.
  </acceptance_criteria>
  <done>
    `scripts/check-no-quibly.mjs` exists with 5 denylist patterns, `.planning` + self-exclusion, recursive walk over app/components/emails/lib/scripts/tests/public. Running it on the current branch exits 0. `npm run check:no-quibly` is wired. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/branding.test.ts (Vitest) and tests/e2e/brand-paint.spec.ts (Playwright) — VALIDATION.md Wave 0</name>
  <files>tests/branding.test.ts, tests/e2e/brand-paint.spec.ts, playwright.config.ts</files>
  <read_first>
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-VALIDATION.md (§Wave 0 Requirements — exact contracts for both files)
    - tests/seo.spec.ts (analog — Playwright spec structure, page.goto, expect(text).toContain patterns)
    - tests/legal.spec.ts (analog — Playwright Locator + visibility assertion)
    - playwright.config.ts (current — projects array; check whether `tests/e2e/brand-paint.spec.ts` is matched by an existing testMatch glob)
    - tests/unit/disposable-domains.test.ts (analog — Vitest describe/it/expect structure)
  </read_first>
  <action>
    Create `tests/branding.test.ts` (Vitest):
    - Import `describe, it, expect` from `vitest`; `readFileSync` from `node:fs`.
    - Test group 1 (rendered source files contain no brand strings):
      - For each of: `app/layout.tsx`, `app/page.tsx`, `app/sitemap.ts`, `app/robots.ts`, `app/(legal)/privacy/page.tsx`, `app/(legal)/terms/page.tsx`, `components/sections/hero.tsx`, `components/sections/footer.tsx`, `components/sections/waitlist-form-section.tsx`, `components/sections/why-zeremi.tsx`, `components/sections/hero-mascot.tsx` — assert content does NOT match `/Quibly|Quibs|useQuibly\.com|usequibly\.com|Quicksand/i`.
    - Test group 2 (positive brand presence):
      - `app/layout.tsx` matches `/Bree_Serif/` AND `/zeremi\.app/`.
      - `app/sitemap.ts` matches `/zeremi\.app/`.
      - `app/(legal)/privacy/page.tsx` matches `/Name change notice/` AND `/zeremi\.app/`.
      - `components/zeremi/zeremi-icon.tsx` exists and matches `/viewBox="0 0 56 88"/`.

    Create `tests/e2e/brand-paint.spec.ts` (Playwright):
    - Import `test, expect` from `@playwright/test`.
    - Test 1 — Bree Serif loads, Quicksand does not:
      - Collect network request URLs via `page.on('request', req => urls.push(req.url()))`. Navigate to `/` and wait for `networkidle`.
      - Assert at least one URL matches `/bree.serif/i` (Google Fonts CSS or WOFF/WOFF2).
      - Assert NO URL matches `/quicksand/i`.
    - Test 2 — gradient Z mark visible in hero:
      - `await page.locator('h1').first()` is visible (LCP-as-H1 Phase 2 D-03 carries forward).
      - The hero mascot SVG with `viewBox="0 0 56 88"` is visible: `page.locator('svg[viewBox="0 0 56 88"]').first()` is visible.
    - Test 3 — no brand string in rendered DOM:
      - `const html = await page.content()`; `expect(html).not.toMatch(/Quibly|Quibs|useQuibly\.com|usequibly\.com/i)`.

    Update `playwright.config.ts` if necessary — verify by reading the `projects` array; if its `testMatch` glob already covers `tests/e2e/**/*.spec.ts`, no edit needed.

    Run the suites locally to verify they pass: `npm run test:unit -- --run tests/branding.test.ts` AND the Playwright invocation that matches the project (e.g. `npm run test:e2e -- tests/e2e/brand-paint.spec.ts`).

    Commit: `test(06.5-05): add branding.test.ts + brand-paint.spec.ts Wave 0 guards`.
  </action>
  <verify>
    <automated>test -f tests/branding.test.ts &amp;&amp; test -f tests/e2e/brand-paint.spec.ts &amp;&amp; grep -q "Bree_Serif" tests/branding.test.ts &amp;&amp; grep -q "Name change notice" tests/branding.test.ts &amp;&amp; grep -qE "bree.serif|bree-serif|Bree Serif" tests/e2e/brand-paint.spec.ts &amp;&amp; grep -q 'viewBox="0 0 56 88"' tests/e2e/brand-paint.spec.ts &amp;&amp; npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `tests/branding.test.ts` exists; `tests/e2e/brand-paint.spec.ts` exists.
    - Source assertion: `tests/branding.test.ts` references at least 11 source-file paths from Test Group 1 (each via `readFileSync`): `grep -c 'readFileSync' tests/branding.test.ts` >= 11.
    - Source assertion: `tests/branding.test.ts` asserts presence of `Bree_Serif`, `zeremi.app`, `Name change notice`, and `viewBox="0 0 56 88"`.
    - Source assertion: `tests/e2e/brand-paint.spec.ts` references both `bree.serif|bree-serif|Bree Serif` AND `viewBox="0 0 56 88"` AND a negative `quicksand` assertion: `grep -qi 'quicksand' tests/e2e/brand-paint.spec.ts` (the string appears as the negative-match pattern).
    - Behavior assertion: `npm run test:unit -- --run` exits 0 (full unit suite passes including the new `branding.test.ts`).
    - Behavior assertion (manual / preview): Playwright `brand-paint.spec.ts` passes against a local `npm run dev` server — recorded in the SUMMARY.
  </acceptance_criteria>
  <done>
    Both Wave 0 test files exist and pass locally. Vitest unit suite is green including the new branding asserts. Playwright e2e includes the brand-paint spec. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire scripts/check-no-quibly.mjs into .github/workflows/test.yml as a CI step</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml (current — job structure; vitest job lines ~19-31 per PATTERNS)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§Pattern C — workflow step format; §`.github/workflows/test.yml` — add step after `npm ci`)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pitfall 1 — activate AFTER rename sweep; the dependency on plans 03/04 in frontmatter encodes this)
  </read_first>
  <action>
    Edit `.github/workflows/test.yml`:
    - Locate the existing `vitest` job (or whichever job runs after `npm ci` and before the typecheck/lint steps).
    - Insert a new step IMMEDIATELY after `npm ci` (so a regression aborts CI before more expensive steps run):
      ```yaml
      - name: Check no Quibly/Quibs/useQuibly.com/Quicksand strings in source
        run: node scripts/check-no-quibly.mjs
      ```
    - Verify the YAML indentation matches the surrounding steps exactly (2-space step indent under `steps:`).
    - Do NOT remove or reorder any existing step.
    - Verify YAML parses correctly: `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/test.yml','utf8'))"` exits 0 — if `js-yaml` isn't a dep, fall back to `python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` or just run `npm ci` locally and inspect the syntax.

    Commit: `ci(06.5-05): wire check-no-quibly.mjs into test.yml (D-03)`.

    NOTE: This commit MUST come AFTER the script + tests are confirmed clean locally — if CI runs and the grep guard fails on the very first push, the activation was premature (per Pitfall 1).
  </action>
  <verify>
    <automated>grep -q "node scripts/check-no-quibly.mjs" .github/workflows/test.yml &amp;&amp; grep -q "Check no Quibly" .github/workflows/test.yml &amp;&amp; node scripts/check-no-quibly.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `.github/workflows/test.yml` contains `node scripts/check-no-quibly.mjs` exactly once.
    - Source assertion: `.github/workflows/test.yml` contains the step name `Check no Quibly` (descriptive name visible in GitHub UI).
    - Behavior assertion: `node scripts/check-no-quibly.mjs` still exits 0 (the wiring step did not introduce any brand string into the workflow file itself — verify no accidental literal `useQuibly.com` etc.).
    - YAML validity (best-effort): the file parses as valid YAML (run `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/test.yml'))"` if Python is available; otherwise rely on CI for the parse check).
  </acceptance_criteria>
  <done>
    `.github/workflows/test.yml` has the `Check no Quibly...` step running `node scripts/check-no-quibly.mjs` immediately after `npm ci`. The script still exits 0 against the current repo state. Change is committed. Subsequent commits on the rebrand branch and on `main` are now permanently gated against reintroduction of the brand strings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future commits → main branch | CI grep guard is the load-bearing post-rebrand defense; pre-commit hooks are bypassable, CI is not |
| Test suite → rendered output | Wave 0 branding tests assert both presence (Zeremi/Bree Serif) and absence (Quibly/Quicksand) — closes the false-negative gap where a partial cleanup silently passes |
| Phase slug (.planning/) vs source | The phase slug contains "quibly" by design — guard MUST exclude `.planning/` to avoid eternal CI failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-05-01 | Tampering | Reintroduction of Quibly string via copy-paste from prior phase docs | mitigate | scripts/check-no-quibly.mjs runs in CI on every PR + push; exits 1 with file:line on any match (per Task 1 design). |
| T-065-05-02 | Denial of Service | Guard incorrectly fails on legitimate .planning/ phase slug | mitigate | EXCLUDE_DIRS contains `'.planning'` literal — verified by acceptance criterion `grep -q "'.planning'" scripts/check-no-quibly.mjs`. |
| T-065-05-03 | Tampering | Guard activated before sweep completes — every intermediate commit fails CI | mitigate | Pitfall 1 sequencing: this plan's `depends_on: [06.5-03, 06.5-04]` makes the dependency explicit; Task 1 instructions require `node scripts/check-no-quibly.mjs` to exit 0 LOCALLY before committing the script. |
| T-065-05-04 | Spoofing | Test suite silently green while UI still leaks Quibly (RSC vs file-based assertions) | mitigate | Wave 0 splits into two layers: branding.test.ts (file source assertions, fast) + brand-paint.spec.ts (Playwright DOM + network panel, end-to-end). Both must pass. |
| T-065-05-05 | Information Disclosure | A new file added in a future phase silently violates the guard if not in INCLUDE_DIRS | accept | INCLUDE_DIRS covers all standard project dirs (app, components, emails, lib, scripts, tests, public); a new top-level dir is a deliberate project structure change that triggers fresh review. Documented in script comments. |
</threat_model>

<verification>
- `node scripts/check-no-quibly.mjs` exits 0 against current repo state.
- `npm run test:unit -- --run` exits 0 (includes new `tests/branding.test.ts`).
- Playwright `tests/e2e/brand-paint.spec.ts` passes against `npm run dev` (executor verifies locally).
- `.github/workflows/test.yml` includes the new step; YAML parses cleanly.
- Three atomic commits on `rebrand/zeremi` branch.
</verification>

<success_criteria>
- D-03 satisfied: CI grep guard active in `.github/workflows/test.yml`, fails on any future Quibly/Quibs/useQuibly.com/Quicksand reintroduction.
- VALIDATION.md Wave 0 closed: `tests/branding.test.ts` + `tests/e2e/brand-paint.spec.ts` exist and pass.
- SC #5 mechanically enforced going forward.
</success_criteria>

<output>
Create `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-05-SUMMARY.md` when done.
</output>
