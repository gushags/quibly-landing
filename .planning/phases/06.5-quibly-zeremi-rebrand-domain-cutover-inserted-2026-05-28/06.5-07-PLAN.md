---
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
plan: 07
type: execute
wave: 6
depends_on:
  - 06.5-05
  - 06.5-06
files_modified:
  - .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md
  - .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md
  - .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
autonomous: true
requirements:
  - D-02
  - D-05
  - D-06
  - D-07
  - CD-07
  - SC-3
  - SC-5
must_haves:
  truths:
    - "D-02: Phase 6 plans 06-04-PLAN.md and 06-05-PLAN.md are edited IN PLACE on the rebrand/zeremi branch (no SUPERSEDED headers, no parallel 06.5-04/06.5-05 plans, no plan ID fork); 06-UAT.md is updated in lockstep"
    - "D-05: useQuibly.com is released entirely after zeremi.app is bound — no 301 redirect, no 'we moved' static page, no SEO carry-over; the new 06-05-PLAN.md Vercel-unbind step removes the apex from project domains"
    - "Phase 6 plan 06-04-PLAN.md targets staging.zeremi.app throughout (every staging.useQuibly.com replaced); UAT screenshot expectations updated to match the new domain in their captions/descriptions"
    - "Phase 6 plan 06-05-PLAN.md targets zeremi.app throughout; references to Quibly/Quibly Waitlist/hello@useQuibly.com/privacy@useQuibly.com replaced with Zeremi equivalents"
    - "06-05-PLAN.md contains two NEW step entries: (a) Resend Dashboard — delete useQuibly.com sender domain (gated by mail-tester ≥9/10 from zeremi.app, D-07); (b) Vercel Dashboard — remove useQuibly.com from project domains (D-06, CD-07: UI step with screenshot)"
    - "06-UAT.md (if present) has all 12 numbered tests updated to reference zeremi.app / Zeremi / privacy@zeremi.app / Zeremi Waitlist as appropriate"
    - "All edited files contain zero Quibly/useQuibly.com/Quibs strings after this plan (per CI grep guard from Wave 4 plan 05 — but note .planning/ is excluded; we sweep for hygiene anyway)"
  artifacts:
    - path: .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md
      provides: "Retargeted staging dry-run plan: staging.zeremi.app + Zeremi audience references"
      contains: "staging.zeremi.app"
    - path: .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md
      provides: "Retargeted production go-live plan: zeremi.app + 2 new useQuibly.com unbind/delete tasks"
      contains: "zeremi.app"
    - path: .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
      provides: "Updated UAT checklist referencing zeremi.app and Zeremi audience names"
      contains: "zeremi.app"
  key_links:
    - from: .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md (new task)
      to: Resend Dashboard delete sender step
      via: "D-07: delete useQuibly.com sender after mail-tester ≥9/10 from zeremi.app verified in Phase 6.5 plan 06"
      pattern: "delete.*useQuibly\\.com.*sender"
    - from: .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md (new task)
      to: Vercel Dashboard unbind step
      via: "D-06: remove useQuibly.com from quibly-landing project domains"
      pattern: "remove.*useQuibly\\.com"
---

<objective>
Wave 6: edit Phase 6 plans 06-04 and 06-05 (and 06-UAT.md) in place on the `rebrand/zeremi` branch per D-02 — do NOT fork them, do NOT add a SUPERSEDED header, do NOT create parallel 06.5-04 / 06.5-05 plans. The intent is that after the Phase 6.5 merge, Phase 6 plans 06-04 and 06-05 ARE the natural execution surface for the staging dry-run and production go-live, just retargeted to `zeremi.app` / `staging.zeremi.app`.

Two NEW steps must be ADDED to `06-05-PLAN.md` (per D-06 + D-07 + CD-07):
- Step (a): Vercel Dashboard — remove `useQuibly.com` from the `quibly-landing` project's bound domains (UI-driven step with a screenshot, mirroring Phase 6's existing screenshot convention).
- Step (b): Resend Dashboard — delete the `useQuibly.com` sender domain (gated by Phase 6.5 plan 06's mail-tester ≥9/10 confirmation).

These two new steps slot AFTER the existing mail-tester / real-signup smoke task (currently Task 6 in 06-05) and BEFORE the no-Service-Worker check (currently Task 7) — i.e. they become the new Task 6a/6b or 7/8 depending on numbering preference. They MUST be scheduled to run only AFTER the production go-live has already confirmed `zeremi.app` is serving and emails are flowing, so the deletions remove only the now-redundant old identity.

This plan does NOT touch Phase 6 plans 06-01, 06-02, 06-03 (already complete; do not modify in-place per D-02 — the rename references in those SUMMARYs are historical artifacts).

This plan is the FINAL plan in Phase 6.5. After this plan's commit, the rebrand branch is ready to merge to main as a merge commit (D-01).

Output: 06-04-PLAN.md, 06-05-PLAN.md, 06-UAT.md updated in place; commit on the rebrand branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md
@.planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md
@.planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md
@.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retarget 06-04-PLAN.md (staging dry-run plan) to staging.zeremi.app</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md (current — Read in full so every Quibly literal is identified; per RESEARCH §06-04-PLAN.md edits summary the host `staging.useQuibly.com` appears ~20+ times in task steps, acceptance criteria, dig commands)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Phase 6 Plan Edits Strategy + §06-04-PLAN.md edits summary)
  </read_first>
  <action>
    Apply the following replacements throughout `06-04-PLAN.md`:
    - `staging.useQuibly.com` → `staging.zeremi.app` (every occurrence — task bodies, acceptance criteria, `dig +short` invocations, frontmatter `files_modified`/`user_setup` entries).
    - `useQuibly.com` (when used in DNS pre-flight check task, e.g. `dig +short ns useQuibly.com`) → `zeremi.app`.
    - Any `Quibly` brand references in task descriptions or rendered-content checks → `Zeremi`.
    - Any references to the `Quibly Waitlist` audience name in user-facing prose → `Zeremi Waitlist`.

    Do NOT rename the screenshot evidence paths (`06-uat-11-1-bound.png`, `06-uat-11-2-prompt.png`, `06-uat-11-3-marketing-load.png`, `06-uat-11-4-back-to-landing.png`) — they're brand-neutral.

    Do NOT restructure tasks, change wave numbers, or modify the frontmatter schema beyond the string replacements above.

    Sanity-check by running the Wave 4 grep guard against this file path (note the guard excludes `.planning/` by default, but we can run a manual grep): `grep -ciE 'useQuibly|Quibly' .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md` should report 0.

    Commit: `docs(06.5-07): retarget 06-04-PLAN.md to staging.zeremi.app (D-02)`.
  </action>
  <verify>
    <automated>! grep -iE 'useQuibly|usequibly' .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md &amp;&amp; grep -q "staging.zeremi.app" .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md &amp;&amp; [ "$(grep -ic Quibly .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `06-04-PLAN.md` contains `staging.zeremi.app` at least 5 times: `grep -c staging.zeremi.app .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md` >= 5.
    - Source assertion: `06-04-PLAN.md` contains zero `useQuibly|usequibly|Quibly` matches (case-insensitive): `grep -ciE 'useQuibly|usequibly|Quibly' .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md` equals 0.
    - Source assertion: existing screenshot paths preserved (brand-neutral): `grep -c '06-uat-11-' .planning/phases/06-production-deploy-cutover-runbook/06-04-PLAN.md` is unchanged from pre-edit count.
    - Structure assertion: frontmatter `plan: 04` and `wave: 3` (or whichever wave was set originally) UNCHANGED — verified by reading the frontmatter block.
  </acceptance_criteria>
  <done>
    `06-04-PLAN.md` references only `staging.zeremi.app`; no Quibly/useQuibly strings remain. Screenshot evidence paths unchanged. Task structure preserved. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Retarget 06-05-PLAN.md and ADD useQuibly.com unbind + Resend sender delete steps (D-06, D-07, CD-07)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md (current — the existing 10-task production go-live plan; per RESEARCH §06-05-PLAN.md edits summary `useQuibly.com` appears ~60 times, `Quibly`, `Quibly Waitlist`, `hello@useQuibly.com`, `privacy@useQuibly.com`)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md (D-06 — Vercel unbind happens INSIDE this plan; D-07 — Resend sender delete gated by Phase 6.5 plan 06's mail-tester ≥9/10; CD-07 — UI step with screenshot)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Phase 6 Plan Edits Strategy + §06-05-PLAN.md edits summary — itemizes the two new steps to add)
  </read_first>
  <action>
    Part A: String replacements throughout `06-05-PLAN.md`:
    - `useQuibly.com` → `zeremi.app` (every occurrence — task bodies, curl commands, acceptance criteria, DNS dig, frontmatter).
    - `Quibly` → `Zeremi` in copy-content checks (e.g., `curl -s https://useQuibly.com | grep -c Quibly` → `... grep -c Zeremi`).
    - `Quibly Waitlist` → `Zeremi Waitlist` in audience references (D-08 audience name).
    - `hello@useQuibly.com` → `hello@zeremi.app` in email round-trip check.
    - `privacy@useQuibly.com` → `privacy@zeremi.app` in the Task 1 mailbox-provisioning HARD launch-gate.
    - `From: renders as 'Quibly <hello@useQuibly.com>'` → `From: renders as 'Jeff at Zeremi <hello@zeremi.app>'` (preserve the test-assertion shape from app/actions/join-waitlist.ts plan 04).

    Part B: ADD two NEW task entries to the task list (D-06 + D-07 + CD-07). Insert these AFTER the existing Task 6 (real-signup smoke / mail-tester verification — whichever currently comes last among the deliverability tasks) and BEFORE the no-Service-Worker check. Use the existing task-numbering convention (`<task type="checkpoint:human-action" gate="blocking">` or whatever the surrounding tasks use).

    New Task A (Vercel `useQuibly.com` unbind, CD-07 UI step):
    - Type: `checkpoint:human-action` (gated by the preceding mail-tester success).
    - Name: "Vercel: remove useQuibly.com from quibly-landing project domains (D-06)".
    - Body:
      - Prerequisite: Phase 6.5 plan 06 confirmed mail-tester ≥9/10 from `hello@zeremi.app` AND this Phase 6 plan's preceding tasks confirmed `https://zeremi.app` is serving the production deploy AND a real signup round-trip succeeded.
      - Steps:
        1. Open Vercel Dashboard → quibly-landing Project → Settings → Domains.
        2. Locate `useQuibly.com` (and the `www.useQuibly.com` variant if present). Click "Remove".
        3. Confirm the removal dialog. Vercel stops serving `useQuibly.com` immediately.
        4. Verify: `curl -sI https://useQuibly.com` no longer returns a Vercel response (expect connection failure or a generic 404/error from the registrar's parking page). The registrar still holds the domain — D-05 invariant — but Vercel is no longer bound.
        5. Take a screenshot of the Vercel Domains panel showing useQuibly.com is no longer listed → save to `.planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-useQuibly-unbound.png`.
      - Resume signal: "unbound" with the screenshot path.

    New Task B (Resend `useQuibly.com` sender delete, D-07):
    - Type: `checkpoint:human-action` (gated by Task A AND the prior mail-tester pass).
    - Name: "Resend: delete useQuibly.com sender domain (D-07)".
    - Body:
      - Prerequisite: Phase 6.5 plan 06 confirmed `zeremi.app` sender verified + mail-tester ≥9/10. Task A above completed.
      - Steps:
        1. Open Resend Dashboard → Domains.
        2. Locate `useQuibly.com`. Open its settings.
        3. Click "Delete Domain". Confirm. Resend removes the sender; no future sends from `hello@useQuibly.com` are possible.
        4. Verify via API: `curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains | jq '.data[].name'` no longer lists `useQuibly.com`.
        5. Take a screenshot of the Resend Domains list showing only `zeremi.app` → save to `.planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-resend-useQuibly-deleted.png`.
      - Resume signal: "deleted" with the screenshot path.

    Update the plan's `must_haves.truths` (in frontmatter) to include the two new invariants:
    - "useQuibly.com is no longer bound to the quibly-landing Vercel project after this plan ships (D-06)"
    - "useQuibly.com is no longer a verified Resend sender after this plan ships (D-07)"

    Sanity-check: `grep -ciE 'useQuibly|usequibly|Quibly Waitlist' .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md` reports 0 (all old references replaced). Note: the two new tasks intentionally mention `useQuibly.com` in their REMOVAL instructions — these are NOT brand drift, they're explicit unbind/delete instructions. Use a more specific grep that excludes lines mentioning "remove", "delete", "unbind" if needed.

    Commit: `docs(06.5-07): retarget 06-05-PLAN.md to zeremi.app + add useQuibly.com unbind/delete steps (D-02, D-06, D-07)`.
  </action>
  <verify>
    <automated>grep -q "zeremi.app" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md &amp;&amp; grep -q "hello@zeremi.app" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md &amp;&amp; grep -q "privacy@zeremi.app" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md &amp;&amp; grep -q "Zeremi Waitlist" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md &amp;&amp; grep -qi "remove.*useQuibly\.com" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md &amp;&amp; grep -qi "delete.*useQuibly\.com" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: positive Zeremi references present — `zeremi.app`, `hello@zeremi.app`, `privacy@zeremi.app`, `Zeremi Waitlist` (each `grep -q`).
    - Source assertion: `06-05-PLAN.md` contains an explicit "remove useQuibly.com" instruction (Vercel unbind, D-06): `grep -qi "remove.*useQuibly\.com" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md`.
    - Source assertion: `06-05-PLAN.md` contains an explicit "delete useQuibly.com" instruction (Resend sender, D-07): `grep -qi "delete.*useQuibly\.com" .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md`.
    - Cleanup assertion: outside the two unbind/delete task bodies (which legitimately mention the old domain), there should be no stray `Quibly Waitlist|hello@useQuibly|privacy@useQuibly` references. Spot-check: `grep -c 'Quibly Waitlist' .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md` equals 0; `grep -c 'hello@useQuibly' .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md` equals 0; `grep -c 'privacy@useQuibly' .planning/phases/06-production-deploy-cutover-runbook/06-05-PLAN.md` equals 0.
    - Structure assertion: task count increased by exactly 2 (two new checkpoint tasks added) vs the original; verify by counting `<task type=` occurrences before/after if pre-edit count was captured (or trust the manual review).
  </acceptance_criteria>
  <done>
    `06-05-PLAN.md` targets zeremi.app throughout; contains the new Vercel-unbind and Resend-sender-delete tasks gated on Phase 6.5 plan 06's mail-tester pass; `must_haves` updated; outdated Quibly references removed. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update 06-UAT.md (if present) and commit Phase 6.5 close-phase ROADMAP/STATE updates</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md (current 12-test checklist; identify every Quibly literal)
    - .planning/ROADMAP.md (current Phase 6.5 entry — note the Goal references useQuibly.com and zeremi.app; ROADMAP entries themselves DO NOT need rewriting since the phase is now "done" and the Goal is historical — see PROJECT close-phase note)
    - .planning/STATE.md (current — last_activity, accumulated decisions; per CONTEXT this close should note the rename)
    - .planning/PROJECT.md (current — per CONTEXT §Specifics "PROJECT.md and CLAUDE.md still call this project 'Quibly Landing'... naming is internal and acceptable as project identity")
  </read_first>
  <action>
    Edit `06-UAT.md`:
    - Replace `useQuibly.com` → `zeremi.app` throughout the 12 numbered tests (curl commands, expected response strings, screenshot captions where applicable).
    - Replace `hello@useQuibly.com` → `hello@zeremi.app`.
    - Replace `privacy@useQuibly.com` → `privacy@zeremi.app`.
    - Replace `Quibly Waitlist` → `Zeremi Waitlist` in audience-related tests.
    - Replace `Quibly` → `Zeremi` in copy-content checks.

    Edit `.planning/STATE.md`:
    - Update `last_activity` to a date-stamped entry: `2026-05-28 -- Phase 6.5 (Quibly → Zeremi rebrand) ready to merge to main`.
    - In `Accumulated Context > Decisions`, append: `[Phase 6.5]: Quibly → Zeremi rebrand and domain cutover; new apex zeremi.app, new sender hello@zeremi.app, audience renamed in lockstep (D-08); useQuibly.com Vercel unbind + Resend sender delete deferred to edited 06-05 plan (D-06/D-07).`
    - Update `stopped_at` to `Phase 6.5 plans 1–7 complete, ready for merge to main`.

    Edit `.planning/PROJECT.md`:
    - Per CONTEXT §Specifics: "PROJECT.md... should be updated at Phase 6.5 close to reference Zeremi / zeremi.app. Treat that as part of the phase close, not a separate plan."
    - Update the Domain constraint from `useQuibly.com` to `zeremi.app`.
    - Update the project name from "Quibly Landing" to "Zeremi Landing" in the title block. The repo / GSD phase-dir slug stays `quibly-landing` / `06.5-quibly-zeremi-...` (per CONTEXT §Specifics — internal identifiers; not visible in production source).
    - Update any other Quibly-vs-Zeremi copy in PROJECT.md to reflect the post-rebrand reality (e.g., references to the brand name in the "Core Value" line).
    - Note: `CLAUDE.md` is excluded from this pass — it lives in the repo root and would be caught by the CI grep guard if it contained source-relevant brand strings, but it's a developer-facing instruction file and ITS own "Project" section header can stay as "Quibly Landing" (repo identifier) per CONTEXT — leave CLAUDE.md untouched.

    The ROADMAP.md Phase 6.5 entry stays as-is (historical record of the phase; the Goal/Success Criteria narrative is appropriate as a "what we did" entry post-merge).

    After this commit, the rebrand branch is ready for merge. The merge to main is a separate developer action — NOT a task in this plan. Document the next step in the SUMMARY: `git checkout main && git merge --no-ff rebrand/zeremi -m "merge: Quibly → Zeremi rebrand & domain cutover (Phase 6.5)"` (D-01: no-ff merge commit, no rebase, no squash).

    Commit: `docs(06.5-07): update 06-UAT.md, STATE.md, PROJECT.md for Zeremi rebrand close-phase`.
  </action>
  <verify>
    <automated>! grep -iE 'useQuibly|usequibly' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md &amp;&amp; grep -q "zeremi.app" .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md &amp;&amp; grep -q "Phase 6.5" .planning/STATE.md &amp;&amp; grep -q "Zeremi" .planning/STATE.md &amp;&amp; grep -q "zeremi.app" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `06-UAT.md` contains zero `useQuibly|usequibly|Quibly Waitlist|hello@useQuibly|privacy@useQuibly` matches (case-insensitive). Note `06-UAT.md` may still mention "Quibly" in historical / footnote context — verify there are zero RUNTIME assertions referencing the old domain.
    - Source assertion: `STATE.md` updated — `grep -q "Phase 6.5" .planning/STATE.md` AND `grep -q "Zeremi" .planning/STATE.md` AND `grep -q "2026-05-28" .planning/STATE.md`.
    - Source assertion: `PROJECT.md` updated — `grep -q "zeremi.app" .planning/PROJECT.md` AND the title block reflects "Zeremi" identity.
    - Branch readiness assertion: `git log --oneline rebrand/zeremi -1` shows this plan's commit; `git status` is clean (no uncommitted changes).
    - SUMMARY documents the merge command: the post-task SUMMARY file `.planning/phases/06.5-.../06.5-07-SUMMARY.md` includes the literal merge invocation `git merge --no-ff rebrand/zeremi`.
  </acceptance_criteria>
  <done>
    06-UAT.md retargeted to zeremi.app; STATE.md records the rebrand completion; PROJECT.md reflects Zeremi identity. The rebrand branch is committed and ready for the developer to execute the merge to main (`git merge --no-ff rebrand/zeremi ...`) per D-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 6 plan documents → execute-phase workflow | After merge, Phase 6 plan 06-04 / 06-05 become the live execution surface; their content directly drives the staging dry-run + production go-live |
| Phase 6.5 → Phase 6 dependency | D-02 mandates in-place edits (no plan ID forking) — STATE/ROADMAP continuity depends on the edits being clean |
| useQuibly.com lifecycle | Vercel unbind + Resend sender delete are intentionally deferred to 06-05 so they happen ONLY after zeremi.app has been live-verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-07-01 | Tampering | Stray `staging.useQuibly.com` host string in 06-04-PLAN.md causes staging dry-run to bind the wrong host | mitigate | Task 1 acceptance criterion: zero `useQuibly|usequibly|Quibly` matches in 06-04-PLAN.md. |
| T-065-07-02 | Spoofing | 06-05-PLAN.md tests reading old `Quibly` content from a Zeremi page silently pass (false negative) | mitigate | Task 2 sweeps content-grep assertions: `grep -c Quibly` → `grep -c Zeremi`. |
| T-065-07-03 | Information Disclosure | useQuibly.com remains Vercel-bound post-launch, both domains serve the same deploy → duplicate content / brand confusion | mitigate | Task 2 adds explicit Vercel-unbind step (D-06, CD-07) gated on the Phase 6.5 plan 06 verification chain; UI screenshot evidence required. |
| T-065-07-04 | Spoofing | Stale `useQuibly.com` Resend sender allows future accidental sends from the old domain post-rename | mitigate | Task 2 adds explicit Resend sender-delete step (D-07) gated on mail-tester ≥9/10 from zeremi.app + Vercel unbind complete. |
| T-065-07-05 | Repudiation | STATE.md doesn't reflect the rebrand → future agents don't know the project's true identity | mitigate | Task 3 updates STATE.md `last_activity` and Accumulated Decisions with the rebrand entry; PROJECT.md domain constraint updated. |
| T-065-07-06 | Tampering | CI grep guard from plan 05 false-positive-fails on the legitimate "remove useQuibly.com" instructions in 06-05 | accept | Plan 05's guard excludes `.planning/` (RESEARCH §Anti-pattern); `.planning/phases/06-.../06-05-PLAN.md` is under .planning/ and is not scanned. |
</threat_model>

<verification>
- 06-04-PLAN.md: zero Quibly/useQuibly matches; >=5 `staging.zeremi.app` references.
- 06-05-PLAN.md: positive Zeremi references present; explicit `remove useQuibly.com` and `delete useQuibly.com` instructions present (legitimate unbind/delete commands).
- 06-UAT.md: retargeted to zeremi.app.
- STATE.md: rebrand entry recorded.
- PROJECT.md: zeremi.app and Zeremi identity reflected.
- `git status` clean on `rebrand/zeremi` branch after the three commits.
</verification>

<success_criteria>
- D-02 satisfied: 06-04 and 06-05 plans edited in-place; no parallel 06.5-04/05 plans created.
- D-06 + D-07 + CD-07 captured: 06-05 contains the two new gated checkpoint tasks for Vercel unbind and Resend delete.
- STATE.md / PROJECT.md updated as Phase 6.5 close artifacts.
- Rebrand branch is ready for `git merge --no-ff rebrand/zeremi` to main per D-01.
</success_criteria>

<output>
Create `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-07-SUMMARY.md` when done. The SUMMARY MUST include the literal merge command `git checkout main && git merge --no-ff rebrand/zeremi -m "merge: Quibly → Zeremi rebrand & domain cutover (Phase 6.5)"` so the developer can execute it directly. The merge itself is intentionally NOT a task in this plan — it is the developer's atomic flip per D-01.
</output>
