---
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
plan: 03
type: execute
wave: 2
depends_on:
  - 06.5-01
  - 06.5-02
files_modified:
  - app/page.tsx
  - app/sitemap.ts
  - app/robots.ts
  - app/icon.tsx
  - app/apple-icon.tsx
  - app/opengraph-image.tsx
  - app/unsubscribe/route.ts
  - app/(legal)/privacy/page.tsx
  - app/(legal)/terms/page.tsx
  - components/sections/hero.tsx
  - components/sections/footer.tsx
  - components/sections/waitlist-form-section.tsx
  - lib/env.ts
  - lib/rate-limit.ts
  - scripts/check-no-trackers.mjs
  - public/quibs-icon.svg
  - public/quibs-icon.png
  - public/quibs-avatar.png
  - public/quibs-avatar-sm.png
  - public/fonts/Quicksand-Bold.woff
  - public/fonts/Quicksand-Bold.ttf
  - public/fonts/Quicksand-OFL.txt
  - scripts/rasterize-mascot.mjs
  - scripts/export-avatar.mjs
autonomous: true
requirements:
  - D-04
  - D-09
  - CD-04
  - CD-08
  - SC-1
  - SC-3
must_haves:
  truths:
    - "Sitemap host, robots host, OG image, favicon, apple-icon, legal pages, sections, env errors, rate-limit prefixes, and JSON-LD Organization/WebSite all reference Zeremi/zeremi.app — no Quibly/useQuibly.com/Quibs/Quicksand strings remain in the listed app/components/lib/scripts files (excluding tests/ and emails/ which are owned by plan 04)"
    - "D-09: editing app/(legal)/privacy/page.tsx mints a new consent_version SHA automatically via the existing Phase 4 git-SHA pipeline; this plan does NOT modify join-waitlist.ts consent_version derivation, and does NOT backfill existing contacts"
    - "Privacy policy contains a visible 'Name change notice (2026-05-28)' paragraph (per UI-SPEC §Privacy Policy Rename-Disclosure Contract) and the Last updated date reflects the rebrand"
    - "app/icon.tsx and app/apple-icon.tsx render the pre-composited Zeremi square PNGs from public/brand/zeremi/png/"
    - "app/opengraph-image.tsx references public/brand/zeremi/png/zeremi-mark-512.png and a Bree Serif WOFF buffer (weight 400)"
    - "Stale Quibs raster assets and Quicksand font files are deleted from public/; scripts/rasterize-mascot.mjs and scripts/export-avatar.mjs are deleted (CD-04)"
  artifacts:
    - path: app/(legal)/privacy/page.tsx
      provides: "Name-change notice paragraph; updated Last-updated date; all Quibly/useQuibly references replaced with Zeremi/zeremi.app; DSAR mailbox privacy@zeremi.app"
      contains: "Name change notice"
    - path: app/sitemap.ts
      provides: "BASE_URL = 'https://zeremi.app'"
      contains: "zeremi.app"
    - path: app/robots.ts
      provides: "Sitemap URL https://zeremi.app/sitemap.xml"
      contains: "https://zeremi.app/sitemap.xml"
    - path: app/opengraph-image.tsx
      provides: "Bree Serif Satori font + zeremi-mark-512.png composition + Zeremi alt/tagline/zeremi.app subtitle"
      contains: "zeremi-mark-512.png"
  key_links:
    - from: app/icon.tsx
      to: public/brand/zeremi/png/zeremi-icon-square-32.png
      via: "readFile() of pre-composited 32×32 PNG"
      pattern: "zeremi-icon-square-32\\.png"
    - from: app/apple-icon.tsx
      to: public/brand/zeremi/png/zeremi-icon-square-180.png
      via: "readFile() of pre-composited 180×180 PNG"
      pattern: "zeremi-icon-square-180\\.png"
    - from: app/opengraph-image.tsx
      to: public/brand/zeremi/png/zeremi-mark-512.png
      via: "readFile() composited via ImageResponse"
      pattern: "zeremi-mark-512\\.png"
    - from: app/page.tsx
      to: schema.org JSON-LD
      via: "Organization + WebSite name='Zeremi', url='https://zeremi.app'"
      pattern: "https://zeremi\\.app"
---

<objective>
Wave 2 string sweep: replace every Quibly/Quibs/useQuibly.com/Quicksand reference in `app/`, `components/sections/`, `lib/`, and the non-tracker scripts with Zeremi/zeremi.app, rewire icon/apple-icon/OG image routes to the pre-composited Zeremi PNGs from `public/brand/zeremi/png/`, edit the privacy policy to add the rename-disclosure paragraph (D-09: this edit naturally mints a new consent_version git SHA — no code change to `join-waitlist.ts`), and delete the stale Quibs raster assets, Quicksand font files, and Quibs-specific export scripts (CD-04).

This plan does NOT touch: `app/actions/join-waitlist.ts`, `emails/WelcomeEmail.tsx`, `scripts/generate-wordmark.mjs`, `tests/**`, `.github/workflows/test.yml`, or `scripts/check-no-quibly.mjs` — all of those are owned by plan 04 (email + tests) and plan 05 (CI guard activation).

Purpose: Land the bulk of the surface-area string sweep in one atomic plan, so the visible web surface (page, sitemap, robots, OG image, favicon, legal) flips to Zeremi in one commit. Sequencing matters: this must complete BEFORE plan 04 updates tests (the email change in plan 04 depends on consent_version semantics that flow from this plan's privacy edit per D-09).

Output: All listed source files swept; legal pages contain rename notice; favicon/OG/Apple icons render Zeremi PNGs; stale assets deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md
@app/page.tsx
@app/sitemap.ts
@app/robots.ts
@app/icon.tsx
@app/apple-icon.tsx
@app/opengraph-image.tsx
@app/unsubscribe/route.ts
@app/(legal)/privacy/page.tsx
@app/(legal)/terms/page.tsx
@components/sections/hero.tsx
@components/sections/footer.tsx
@components/sections/waitlist-form-section.tsx
@lib/env.ts
@lib/rate-limit.ts
@scripts/check-no-trackers.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep app/page.tsx, sitemap, robots, sections (hero, footer, waitlist-form-section), lib/env.ts, lib/rate-limit.ts, scripts/check-no-trackers.mjs</name>
  <files>app/page.tsx, app/sitemap.ts, app/robots.ts, components/sections/hero.tsx, components/sections/footer.tsx, components/sections/waitlist-form-section.tsx, lib/env.ts, lib/rate-limit.ts, scripts/check-no-trackers.mjs</files>
  <read_first>
    - All files in this task's `<files>` list — read current content to identify every Quibly/useQuibly literal.
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§app/page.tsx, §app/sitemap.ts and robots.ts, §lib/env.ts, §lib/rate-limit.ts, §scripts/check-no-trackers.mjs, §components/sections/hero/footer/waitlist-form-section)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§Copywriting Contract: exact before/after string mapping for page title, hero headline, footer wordmark, etc.)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Complete Quibly/Quibs/useQuibly.com/Quicksand String Inventory — verified grep audit)
  </read_first>
  <action>
    `app/page.tsx`:
    - Update Organization JSON-LD: `name: 'Quibly'` → `'Zeremi'`; `url: 'https://useQuibly.com'` → `'https://zeremi.app'`; `logo: 'https://useQuibly.com/quibs-icon.svg'` → `'https://zeremi.app/brand/zeremi/png/zeremi-mark-512.png'`.
    - Update WebSite JSON-LD: `name: 'Quibly'` → `'Zeremi'`; `url: 'https://useQuibly.com'` → `'https://zeremi.app'`.
    - Update any JSDoc / comment / inline copy referencing Quibly.
    - The `<WhyZeremi />` JSX and its import are already correct from plan 02 — do not touch.

    `app/sitemap.ts`: replace `const BASE_URL = 'https://useQuibly.com'` with `'https://zeremi.app'` (single line per PATTERNS).

    `app/robots.ts`: replace `sitemap: 'https://useQuibly.com/sitemap.xml'` with `'https://zeremi.app/sitemap.xml'`.

    `components/sections/hero.tsx`:
    - JSDoc line referencing "Quibly is the strategy-first..." → "Zeremi is the strategy-first..." (preserve the rest of the prose).
    - `<h1>` text "You know your business. Quibly knows how to market it." → "You know your business. Zeremi knows how to market it."

    `components/sections/footer.tsx`:
    - Replace `<span ...>Quibly</span>` wordmark with `<span className="font-heading text-base font-bold text-primary">zeremi</span>` (lowercase per UI-SPEC §Wordmark Rendering CD-06). Preserve any surrounding classes and structure.
    - Update copyright line "© 2026 Quibly" → "© 2026 Zeremi" (UI-SPEC §Copywriting Contract).

    `components/sections/waitlist-form-section.tsx`:
    - Replace all "Quibly" in `<h2>` copy, sub-copy, and JSDoc with "Zeremi".

    `lib/env.ts`:
    - Update the Zod error message from `'RESEND_AUDIENCE_ID is required (Resend Dashboard -> Audiences -> "Quibly Waitlist")'` to `'... "Zeremi Waitlist")'` (D-08 audience rename — AUDIENCE_ID value unchanged, only the human-readable name in the error message updates).
    - Sweep any other "Quibly" string in the file.

    `lib/rate-limit.ts`:
    - Replace `prefix: '@quibly/ratelimit/min'` → `'@zeremi/ratelimit/min'`.
    - Replace `prefix: '@quibly/ratelimit/day'` → `'@zeremi/ratelimit/day'`.
    - (Per RESEARCH §Runtime State Inventory, in-flight Upstash entries expire within 60s/1d; no data migration needed.)

    `scripts/check-no-trackers.mjs`:
    - Replace error message string `'Quibly Landing is cookieless by design. ...'` with `'Zeremi Landing is cookieless by design. ...'` (preserve the rest of the prose).

    Commit: `feat(06.5-03): sweep Quibly→Zeremi in page/sitemap/robots/sections/lib/scripts`.
  </action>
  <verify>
    <automated>! grep -iE 'Quibly|useQuibly|@quibly' app/page.tsx app/sitemap.ts app/robots.ts components/sections/hero.tsx components/sections/footer.tsx components/sections/waitlist-form-section.tsx lib/env.ts lib/rate-limit.ts scripts/check-no-trackers.mjs &amp;&amp; grep -q "https://zeremi.app" app/sitemap.ts &amp;&amp; grep -q "https://zeremi.app/sitemap.xml" app/robots.ts &amp;&amp; grep -q "@zeremi/ratelimit/min" lib/rate-limit.ts &amp;&amp; grep -q "@zeremi/ratelimit/day" lib/rate-limit.ts &amp;&amp; grep -q "Zeremi Waitlist" lib/env.ts &amp;&amp; grep -q '>zeremi<' components/sections/footer.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero brand strings: `grep -ilE 'Quibly|useQuibly|@quibly|Quibs|Quicksand' app/page.tsx app/sitemap.ts app/robots.ts components/sections/hero.tsx components/sections/footer.tsx components/sections/waitlist-form-section.tsx lib/env.ts lib/rate-limit.ts scripts/check-no-trackers.mjs | wc -l` equals 0.
    - Source assertion: `app/sitemap.ts` BASE_URL is `https://zeremi.app`: `grep -q "BASE_URL.*https://zeremi.app" app/sitemap.ts` (or `grep -q "'https://zeremi.app'" app/sitemap.ts`).
    - Source assertion: `app/robots.ts` sitemap URL is `https://zeremi.app/sitemap.xml`.
    - Source assertion: `app/page.tsx` Organization JSON-LD has `name: 'Zeremi'` and `url: 'https://zeremi.app'`: `grep -q "name: 'Zeremi'" app/page.tsx && grep -q "url: 'https://zeremi.app'" app/page.tsx`.
    - Source assertion: `lib/rate-limit.ts` both prefixes use `@zeremi/`: `grep -c '@zeremi/ratelimit/' lib/rate-limit.ts` equals 2.
    - Source assertion: `lib/env.ts` Zod error contains `Zeremi Waitlist`.
    - Source assertion: `components/sections/footer.tsx` wordmark is lowercase `zeremi` rendered with `font-heading`: `grep -q 'font-heading.*>zeremi<' components/sections/footer.tsx` (allowing other classes between).
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    All nine files swept; no Quibly/useQuibly/Quibs/@quibly/Quicksand strings remain. JSON-LD on `app/page.tsx` advertises Zeremi at https://zeremi.app. Sitemap + robots reference the new host. Footer wordmark is lowercase `zeremi` in Bree Serif. Rate-limit Redis prefixes use `@zeremi/`. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire app/icon.tsx, app/apple-icon.tsx, app/opengraph-image.tsx to Zeremi PNGs + Bree Serif WOFF for Satori; sweep app/unsubscribe/route.ts inline HTML</name>
  <files>app/icon.tsx, app/apple-icon.tsx, app/opengraph-image.tsx, app/unsubscribe/route.ts, public/fonts/BreeSerif-Regular.woff2 (or @fontsource/bree-serif devDep if needed), package.json</files>
  <read_first>
    - app/icon.tsx (current — readFile path `public/quibs-icon.png` + ImageResponse composition)
    - app/apple-icon.tsx (current — same pattern at 180×180)
    - app/opengraph-image.tsx (current — readFile of Quicksand-Bold.woff + quibs-icon.png + Satori font config; line numbers per PATTERNS §app/opengraph-image.tsx)
    - app/unsubscribe/route.ts (current — THREE inline HTML blocks at approximately lines 109/197/287, each with a Google Fonts `<link>` for Quicksand and a CSS `font-family: Quicksand` rule; mailto strings unsubscribe@usequibly.com at lines 181/269; brand strings at lines 269/359/361)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§app/icon.tsx and apple-icon.tsx, §app/opengraph-image.tsx, §app/unsubscribe/route.ts — exact line-numbered transformations)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pitfall 2 — Bree Serif weight 400 only in Satori; §Pitfall 3 — unsubscribe route has 3 identical HTML blocks; §Pitfall 5 — favicon double-compositing risk; §Pitfall 6 + Open Question 1 — Bree Serif WOFF source for Satori)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§OG Image Contract: WOFF resolution path priority — check marketing-app/public/fonts/ first, else add @fontsource/bree-serif)
    - /Users/jeff/repos/marketing-app/public/fonts/ (directory listing — check whether a Bree Serif WOFF exists for direct copy)
  </read_first>
  <action>
    Resolve Bree Serif WOFF for Satori (Open Question 1, UI-SPEC §OG Image Contract resolution priority):
    1. Check `/Users/jeff/repos/marketing-app/public/fonts/` for an existing `BreeSerif-*.woff` or `bree-serif-*.woff2` file. If present: copy it to `quibly-landing/public/fonts/BreeSerif-Regular.woff2` (or `.woff` matching the source extension) and use the local file in opengraph-image.tsx.
    2. If not present: add `@fontsource/bree-serif` as a devDependency (`npm install -D @fontsource/bree-serif`). The WOFF2 path is `node_modules/@fontsource/bree-serif/files/bree-serif-latin-400-normal.woff2` (consistent with the existing `@fontsource/quicksand` pattern used by `scripts/generate-wordmark.mjs`). Note: the actual file path inside the package MAY differ; the implementer must verify by running `ls node_modules/@fontsource/bree-serif/files/` after install. If the package layout differs, fall back to Google Fonts CDN download at build time. Document the resolution in the commit message.

    `app/opengraph-image.tsx` (CD-08):
    - Replace `readFile(join(process.cwd(), 'public/fonts/Quicksand-Bold.woff'))` with a read of the Bree Serif WOFF resolved above. Variable name `quicksandBold` → `breeSerifRegular`.
    - Replace `readFile(join(process.cwd(), 'public/quibs-icon.png'))` with `readFile(join(process.cwd(), 'public/brand/zeremi/png/zeremi-mark-512.png'))`. Variable name `mascotPng` → `markPng`.
    - Update `alt` text from "Quibly — You know your business. Quibly knows how to market it." to the Zeremi equivalent.
    - Update inline tagline body text "Quibly" → "Zeremi".
    - Update inline subtitle `useQuibly.com` → `zeremi.app`.
    - Update `fontFamily: 'Quicksand'` → `'Bree Serif'`.
    - Update `fontWeight: 700` → `400` (RESEARCH Pitfall 2 — Bree Serif weight 400 only; requesting 700 silently falls back or errors).
    - Update Satori `fonts:` array entry from `{ name: 'Quicksand', data: quicksandBold, ..., weight: 700 }` to `{ name: 'Bree Serif', data: breeSerifRegular, ..., weight: 400 }`.
    - OG dimensions 1200×630 unchanged.

    `app/icon.tsx` (per UI-SPEC §Favicon / App Icon Contract option 2 — keep ImageResponse but simplify):
    - Replace `readFile(join(process.cwd(), 'public/quibs-icon.png'))` with `readFile(join(process.cwd(), 'public/brand/zeremi/png/zeremi-icon-square-32.png'))`. Variable name `mascotPng` → `iconPng`.
    - Remove any teal-gradient background div (per UI-SPEC: the brand PNGs are already pre-composited with the gradient baked in — double-compositing would render a teal square on a teal square). Render the data URI directly as `<img>` inside a transparent ImageResponse, OR replace the entire ImageResponse with `NextResponse` streaming the raw PNG bytes if the implementer judges that cleaner.
    - Output dimensions remain 32×32.
    - Update JSDoc: "Quibs mascot" → "Zeremi Z mark".

    `app/apple-icon.tsx`:
    - Same transformation as `icon.tsx` but for `public/brand/zeremi/png/zeremi-icon-square-180.png` at 180×180.

    `app/unsubscribe/route.ts` (RESEARCH Pitfall 3 — three identical inline HTML blocks; use a full-file replacement strategy):
    - Replace all three `<title>Unsubscribed — Quibly</title>` with `<title>Unsubscribed — Zeremi</title>`.
    - In each of the three Google Fonts `<link>` tags, drop `Quicksand:wght@500;600` from the `family=` query string and add `Bree+Serif` (single weight). Final family list: `family=Figtree:wght@400;600&family=Bree+Serif&display=swap` (preserve Figtree weights).
    - In each of the three embedded `<style>` blocks, replace `font-family: Quicksand, ...` with `font-family: "Bree Serif", Georgia, serif`.
    - Replace `unsubscribe@usequibly.com` (mailto + visible text, two locations) with `unsubscribe@zeremi.app`.
    - Replace "any Quibly email" / "any further Quibly emails" with the Zeremi equivalents.
    - Replace `<a href="https://usequibly.com">useQuibly.com</a>` with `<a href="https://zeremi.app">zeremi.app</a>`.

    Commit: `feat(06.5-03): rewire icon/apple-icon/og-image to Zeremi PNGs + Bree Serif Satori font; sweep unsubscribe route inline HTML`.
  </action>
  <verify>
    <automated>grep -q "zeremi-icon-square-32.png" app/icon.tsx &amp;&amp; grep -q "zeremi-icon-square-180.png" app/apple-icon.tsx &amp;&amp; grep -q "zeremi-mark-512.png" app/opengraph-image.tsx &amp;&amp; grep -q "Bree Serif" app/opengraph-image.tsx &amp;&amp; ! grep -iE 'Quibly|useQuibly|usequibly|Quicksand|quibs-icon' app/icon.tsx app/apple-icon.tsx app/opengraph-image.tsx app/unsubscribe/route.ts &amp;&amp; [ "$(grep -c '<title>Unsubscribed — Zeremi</title>' app/unsubscribe/route.ts)" -eq 3 ] &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/icon.tsx` references `public/brand/zeremi/png/zeremi-icon-square-32.png` and does NOT reference `quibs-icon.png` or `quibs-icon.svg`: `grep -q 'zeremi-icon-square-32.png' app/icon.tsx && ! grep -q 'quibs-' app/icon.tsx`.
    - Source assertion: `app/apple-icon.tsx` references `public/brand/zeremi/png/zeremi-icon-square-180.png`.
    - Source assertion: `app/opengraph-image.tsx` reads `public/brand/zeremi/png/zeremi-mark-512.png` AND `fontFamily: 'Bree Serif'` AND `fontWeight: 400` AND Satori `fonts:` array contains `name: 'Bree Serif'` with `weight: 400`.
    - Source assertion: `app/opengraph-image.tsx` does NOT contain `Quicksand`, `quibs-icon`, or `weight: 700`: `! grep -E 'Quicksand|quibs-icon|weight: 700' app/opengraph-image.tsx`.
    - Source assertion: `app/unsubscribe/route.ts` contains exactly three `<title>Unsubscribed — Zeremi</title>` occurrences (the three response branches): `grep -c '<title>Unsubscribed — Zeremi</title>' app/unsubscribe/route.ts` equals 3.
    - Source assertion: `app/unsubscribe/route.ts` does NOT contain `Quicksand`, `Quibly`, `useQuibly`, or `usequibly`: `! grep -iE 'Quicksand|Quibly|useQuibly|usequibly' app/unsubscribe/route.ts`.
    - Source assertion: `app/unsubscribe/route.ts` contains `mailto:unsubscribe@zeremi.app`: `grep -q 'mailto:unsubscribe@zeremi.app' app/unsubscribe/route.ts`.
    - Bree Serif WOFF available: either `test -f public/fonts/BreeSerif-Regular.woff2` (or `.woff`) OR `test -d node_modules/@fontsource/bree-serif` (the resolution path chosen is documented in the commit message).
    - Type check: `npx tsc --noEmit` exits 0.
    - Behavior smoke: `curl -s http://localhost:3000/opengraph-image` (with `npm run dev` running) returns HTTP 200 and a 1200×630 PNG (manual sanity if dev server is available; if not, this is verified during Wave 4 e2e — flag as deferred).
  </acceptance_criteria>
  <done>
    `app/icon.tsx`, `app/apple-icon.tsx`, and `app/opengraph-image.tsx` consume the Zeremi PNGs from `public/brand/zeremi/png/`. The OG image uses Bree Serif weight 400 as the Satori font. The unsubscribe route's three inline HTML blocks reference Bree Serif (not Quicksand) and unsubscribe@zeremi.app (not usequibly.com). Bree Serif WOFF source is resolved (local copy or @fontsource/bree-serif). Type checks clean. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sweep app/(legal)/privacy/page.tsx, app/(legal)/terms/page.tsx (add rename-disclosure paragraph per D-09); delete stale Quibs/Quicksand public assets and Quibs scripts (CD-04)</name>
  <files>app/(legal)/privacy/page.tsx, app/(legal)/terms/page.tsx, public/quibs-icon.svg, public/quibs-icon.png, public/quibs-avatar.png, public/quibs-avatar-sm.png, public/fonts/Quicksand-Bold.woff, public/fonts/Quicksand-Bold.ttf, public/fonts/Quicksand-OFL.txt, scripts/rasterize-mascot.mjs, scripts/export-avatar.mjs</files>
  <read_first>
    - app/(legal)/privacy/page.tsx (current — identifies every Quibly/useQuibly/privacy@useQuibly.com occurrence and "Last updated" date)
    - app/(legal)/terms/page.tsx (current — same)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§Privacy Policy Rename-Disclosure Contract — exact prose for the name-change notice paragraph)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md (D-09 — privacy edit naturally mints new consent_version git SHA via existing Phase 4 pipeline; no code change to join-waitlist.ts)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pitfall — terms/privacy "Last updated" dates must stay in sync per tests/legal.spec.ts LEGAL-10 invariant)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§app/(legal)/privacy/page.tsx and terms/page.tsx — line-numbered transformations + Deleted files section)
  </read_first>
  <action>
    `app/(legal)/privacy/page.tsx`:
    - Replace all "Quibly" with "Zeremi" in metadata description, body prose, and headings.
    - Replace all `useQuibly.com` with `zeremi.app`.
    - Replace all `privacy@useQuibly.com` mailto hrefs and visible link text with `privacy@zeremi.app`.
    - Insert a "Name change notice" paragraph near the top of the policy body (after the introduction, before the data-collection section), with prose (per UI-SPEC §Privacy Policy Rename-Disclosure Contract):
      **Name change notice (2026-05-28):** This product was previously known as Quibly and operated at useQuibly.com. It has been renamed to Zeremi and now operates at zeremi.app. The data controller, contact details, and your rights remain unchanged.
    - Update "Last updated:" date to `2026-05-28` (or the actual rebrand merge date, whichever is later — set to `2026-05-28` for the plan execution).

    `app/(legal)/terms/page.tsx`:
    - Replace all "Quibly" → "Zeremi" in metadata description, body, headings.
    - Replace all `useQuibly.com` → `zeremi.app`.
    - Replace `privacy@useQuibly.com` → `privacy@zeremi.app` in any mailto and link text.
    - Update "Last updated:" date to MATCH the privacy policy date exactly (`2026-05-28`) — `tests/legal.spec.ts` LEGAL-10 invariant requires both dates to match.

    Delete stale assets and scripts (CD-04) using `git rm`:
    - `git rm public/quibs-icon.svg public/quibs-icon.png public/quibs-avatar.png public/quibs-avatar-sm.png`
    - `git rm public/fonts/Quicksand-Bold.woff public/fonts/Quicksand-Bold.ttf public/fonts/Quicksand-OFL.txt`
    - `git rm scripts/rasterize-mascot.mjs scripts/export-avatar.mjs`

    Before deletion, sanity-check that no source file references the deleted paths:
    - `grep -rE 'quibs-icon\.(svg|png)|quibs-avatar(-sm)?\.png|Quicksand-(Bold|OFL)' app/ components/ lib/ emails/ scripts/ tests/ 2>/dev/null` — must return zero matches (Task 1 + Task 2 already removed these references; if any remain, stop and surface them).

    Commit: `feat(06.5-03): sweep legal pages with rename notice (D-09); delete stale Quibs/Quicksand assets (CD-04)`.
  </action>
  <verify>
    <automated>! grep -iE 'Quibly|useQuibly|usequibly' app/\(legal\)/privacy/page.tsx app/\(legal\)/terms/page.tsx &amp;&amp; grep -q "Name change notice" app/\(legal\)/privacy/page.tsx &amp;&amp; grep -q "2026-05-28" app/\(legal\)/privacy/page.tsx &amp;&amp; grep -q "2026-05-28" app/\(legal\)/terms/page.tsx &amp;&amp; grep -q "privacy@zeremi.app" app/\(legal\)/privacy/page.tsx &amp;&amp; grep -q "privacy@zeremi.app" app/\(legal\)/terms/page.tsx &amp;&amp; ! test -f public/quibs-icon.svg &amp;&amp; ! test -f public/quibs-icon.png &amp;&amp; ! test -f public/fonts/Quicksand-Bold.woff &amp;&amp; ! test -f scripts/rasterize-mascot.mjs &amp;&amp; ! test -f scripts/export-avatar.mjs &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/(legal)/privacy/page.tsx` contains the literal `Name change notice` and the date `2026-05-28` AND zero `Quibly|useQuibly|usequibly` matches (case-insensitive): `grep -q "Name change notice" 'app/(legal)/privacy/page.tsx' && ! grep -iE 'Quibly|useQuibly|usequibly' 'app/(legal)/privacy/page.tsx'`.
    - Source assertion: `app/(legal)/terms/page.tsx` "Last updated" date matches privacy page date (both `2026-05-28`): both files have `grep -q "2026-05-28"`.
    - Source assertion: both legal pages contain `privacy@zeremi.app` and contain no `privacy@useQuibly.com`.
    - Filesystem assertion: all six deleted public assets and two deleted scripts are absent: `! test -f public/quibs-icon.svg && ! test -f public/quibs-icon.png && ! test -f public/quibs-avatar.png && ! test -f public/quibs-avatar-sm.png && ! test -f public/fonts/Quicksand-Bold.woff && ! test -f public/fonts/Quicksand-Bold.ttf && ! test -f public/fonts/Quicksand-OFL.txt && ! test -f scripts/rasterize-mascot.mjs && ! test -f scripts/export-avatar.mjs`.
    - Source assertion: no remaining references to the deleted paths in source: `grep -rE 'quibs-(icon|avatar)|Quicksand-(Bold|OFL)|rasterize-mascot|export-avatar' app/ components/ lib/ emails/ scripts/ tests/ 2>/dev/null | wc -l` equals 0.
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Privacy policy contains the visible rename-disclosure paragraph dated 2026-05-28; terms page date matches. Both legal pages use `privacy@zeremi.app` and contain no Quibly references. The seven stale public assets and two Quibs-specific scripts are removed from the working tree via `git rm`. Type checks clean. Change is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → metadata routes (icon, apple-icon, opengraph-image) | Routes must serve Zeremi-branded assets; cached old Quibs icons in CDN/browser are an information-disclosure window |
| User → privacy policy | The rename notice is a legal disclosure — must be visible in the policy body, not hidden in a comment |
| Build pipeline → Satori font | Bree Serif WOFF resolution must be deterministic (either local file or pinned @fontsource version) — silent fallback to system fonts would degrade the OG image brand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-03-01 | Information Disclosure | Stale Quibly references in JSON-LD / sitemap / robots | mitigate | Grep gates in Task 1 acceptance criteria assert zero brand-string matches across 9 files; sitemap and robots host strings cross-checked. |
| T-065-03-02 | Spoofing | Stale `privacy@useQuibly.com` mailto on live legal pages → phishing anchor | mitigate | Task 3 grep asserts both legal pages contain `privacy@zeremi.app` and zero `usequibly.com` references; rename disclosure paragraph dated 2026-05-28 cited per UI-SPEC §Privacy Policy Rename-Disclosure Contract. |
| T-065-03-03 | Information Disclosure | OG image silently falling back to system font (lost brand integrity in social previews) | mitigate | Bree Serif WOFF resolution required: either local copy from marketing-app or @fontsource/bree-serif install; verified by filesystem/test-d assertion. Pitfall 2 (weight 700 fallback) blocked by `! grep weight: 700` assertion. |
| T-065-03-04 | Tampering | Favicon double-compositing (Pitfall 5) | mitigate | Task 2 instructions remove gradient background div from icon.tsx; brand PNGs are already pre-composited per UI-SPEC §Asset Inventory. |
| T-065-03-05 | Repudiation | consent_version SHA doesn't update post-rebrand (legal audit risk) | mitigate | D-09 — privacy/page.tsx edit naturally re-baselines the SHA via the existing Phase 4 git-SHA pipeline. The acceptance criterion `grep -q "Name change notice"` confirms the policy body content changed. |
| T-065-03-06 | Tampering | Deleted Quibs script still referenced by package.json or husky | mitigate | Task 3 pre-deletion grep over `app/ components/ lib/ emails/ scripts/ tests/`. (package.json must not have a script entry that runs export-avatar/rasterize-mascot — verify by `grep -E 'export-avatar|rasterize-mascot' package.json` returns nothing.) |
</threat_model>

<verification>
- Zero `Quibly|useQuibly|usequibly|Quibs|Quicksand` matches across all 14 modified source files (case-insensitive, after the three tasks land).
- Privacy policy has rename notice + 2026-05-28 date; terms page date matches.
- Stale Quibs assets and Quicksand fonts deleted from `public/`; export-avatar.mjs and rasterize-mascot.mjs deleted from `scripts/`.
- `app/icon.tsx`/`app/apple-icon.tsx`/`app/opengraph-image.tsx` consume Zeremi PNGs.
- Bree Serif WOFF available for Satori (resolution path documented in commit).
- `npx tsc --noEmit` exits 0.
- `npm run dev` boots; `curl -s http://localhost:3000/sitemap.xml` shows `https://zeremi.app/sitemap.xml` (manual sanity).
</verification>

<success_criteria>
- All bullet items in `<verification>` met.
- Three atomic commits on `rebrand/zeremi` branch.
- Wave 2 leaves the visible web surface flipped to Zeremi (the email path is finished in Wave 3 plan 04; the CI guard is activated in Wave 4 plan 05).
</success_criteria>

<output>
Create `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-03-SUMMARY.md` when done.
</output>
