---
phase: 04-resend-wiring-bot-protection-welcome-email
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - emails/WelcomeEmail.tsx
autonomous: true
requirements: [EMAIL-04, EMAIL-05, EMAIL-06, EMAIL-07]
tags: [phase-4, react-email, welcome-email, brand]

must_haves:
  truths:
    - "WelcomeEmail component renders with `unsubscribeUrl` and `postalAddress` props"
    - "Body copy is the D-01 locked draft verbatim — no paraphrasing"
    - "Footer contains the unsubscribe link AND the postal address (CAN-SPAM EMAIL-04/05)"
    - "Subject line is `You're on the Quibly list` and Preview text is the locked preheader from D-01 / 04-UI-SPEC.md"
    - "All styles are inline hex colors (no oklch, no Tailwind classes — email-client-safe)"
    - "Default export is named `WelcomeEmail`"
  artifacts:
    - path: "emails/WelcomeEmail.tsx"
      provides: "React Email JSX template (EMAIL-07)"
      exports: ["WelcomeEmail (default + named)"]
      contains: "List-Unsubscribe"
  key_links:
    - from: "emails/WelcomeEmail.tsx"
      to: "@react-email/components"
      via: "Html/Head/Body/Container/Section/Hr/Link/Preview imports"
      pattern: "from '@react-email/components'"
    - from: "emails/WelcomeEmail.tsx"
      to: "Plan 05 action"
      via: "props interface { unsubscribeUrl, postalAddress }"
      pattern: "WelcomeEmailProps"
---

<objective>
Ship the React Email JSX template (`emails/WelcomeEmail.tsx`) that Plan 05's action body will pass to `resend.emails.send({ react: WelcomeEmail({...}) })`. The voice and substance are LOCKED by D-01 (CONTEXT.md) — Claude polishes JSX rendering only; the founder edits the final string in PR before merge.

Purpose: Sending the welcome email (Plan 05) requires this React component to exist. Layout adapts the `marketing-app/emails/WelcomeEmail.tsx` structure (teal `#0D9488` header strip, Container, Hr-separated footer) to the D-01 founder voice. Inline styles only (hex colors, system font stack) — email clients strip external stylesheets and don't support `oklch()` / Tailwind classes.

Output: One new TSX file under `emails/`. No tests in this plan — the rendered output is verified visually via Resend's React Email preview tooling and via Playwright snapshot in Plan 05's downstream UAT, plus the EMAIL-03 inbox checkpoint in Plan 07. Component-level testing of email JSX isn't tractable (happy-dom doesn't reproduce email-client rendering).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-CONTEXT.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-RESEARCH.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-PATTERNS.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-UI-SPEC.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-01-SUMMARY.md

<interfaces>
<!-- Existing surface this plan binds to. Verified via PATTERNS.md and Context7. -->

From `@react-email/components@^1.0.12` (Plan 01 installed):
```typescript
// Used in this plan:
export function Html(props: { children: ReactNode }): JSX.Element
export function Head(props): JSX.Element
export function Body(props: { children, style? }): JSX.Element
export function Container(props: { children, style? }): JSX.Element
export function Section(props: { children, style? }): JSX.Element
export function Hr(props: { style? }): JSX.Element
export function Link(props: { href, children, style? }): JSX.Element
export function Preview(props: { children: string }): JSX.Element  // <-- preheader text
export function Text(props: { children, style? }): JSX.Element
```

From `marketing-app/emails/WelcomeEmail.tsx` (analog, lines 1-238 — structural reference):
- Imports from `@react-email/components` listed alphabetically
- Header strip: 48px tall, teal `#0D9488` background
- Container: `maxWidth: '600px', margin: '0 auto'`
- Content section: `padding: '32px 24px'`
- Hr separator: `borderColor: '#e5e5e5', margin: '24px 0'`
- Inline-style objects with `React.CSSProperties` types
- `WelcomeEmail.PreviewProps = {...}` for React Email dev preview

D-01 locked body copy (do NOT paraphrase — UI-SPEC.md §"Voice & Copy"):
```
Hey —

Thanks for joining the Quibly waitlist. I'm Jeff —
a solopreneur building Quibly for other solopreneurs
and small operators who are experts at what they
make but not necessarily at marketing it.

I'm deep in a strategy-first AI marketing tool that
learns your business and runs the marketing loop
with you (not at you). I'll send one more email when
I open it up — no spam, no product-launch hype.

In the meantime, hit reply if there's a marketing
problem you wish someone would just solve. I read
everything.

— Jeff
```

D-01 subject: `You're on the Quibly list`
D-01 / UI-SPEC preview text: `Welcome to the Quibly waitlist — I'll be in touch.`

UI-SPEC.md §"Email Template" color palette (hex only):
- Header strip background: `#0D9488` (matches `--primary` hex)
- Header text: `#ffffff`
- Body background: `#ffffff`
- Body text primary: `#404040`
- Body text secondary (footer/legal): `#737373`
- Divider/border: `#e5e5e5`
- Unsubscribe link: `#737373` underlined

UI-SPEC.md §"Email Template Spacing":
- Container max-width: 600px
- Header strip height: 48px
- Content padding: 32px top/bottom, 24px left/right
- Paragraph margin-bottom: 16px
- HR margin: 24px top/bottom

UI-SPEC.md §"Typography (Email Template)":
- Header wordmark: 16px, weight 600
- Body paragraph: 14px, weight 400, line-height 1.5
- Footer/legal: 12px, weight 400, line-height 1.5
- Font stack: `-apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create emails/ directory and WelcomeEmail.tsx</name>
  <files>emails/WelcomeEmail.tsx</files>
  <read_first>
    - /Users/jeff/repos/quibly-landing/.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-PATTERNS.md (full emails/WelcomeEmail.tsx section — JSX structure + style table)
    - /Users/jeff/repos/quibly-landing/.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-UI-SPEC.md (full file — copy + color + typography contract)
    - /Users/jeff/repos/quibly-landing/.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-CONTEXT.md (D-01 locked draft — verbatim source of truth)
    - /Users/jeff/repos/marketing-app/emails/WelcomeEmail.tsx (analog — copy structural pattern; do NOT copy body copy or buttons — adapt to waitlist voice)
  </read_first>
  <action>
    Verify the `emails/` directory exists at the repo root (create it if missing — same level as `app/`, `lib/`, `components/`).

    Write `emails/WelcomeEmail.tsx` exactly as below. The body copy MUST be the D-01 locked draft verbatim. The four body paragraphs are HTML-entity-escaped (`I&apos;m`, `&mdash;` or `—`) so JSX/React doesn't choke. Keep em-dashes as literal `—` (Unicode U+2014) — they render correctly in modern email clients and match the D-01 locked draft punctuation.

    Style objects come AFTER the component (file convention from `marketing-app/emails/WelcomeEmail.tsx`). Use named export `WelcomeEmail` AND default export — `resend.emails.send({ react: WelcomeEmail({...}) })` works with either, but `import WelcomeEmail from '@/emails/WelcomeEmail'` (default) is the pattern Plan 05 uses.

    ```tsx
    import {
      Body,
      Container,
      Head,
      Hr,
      Html,
      Link,
      Preview,
      Section,
      Text,
    } from '@react-email/components'
    import * as React from 'react'

    /**
     * Phase 4 — Welcome email (EMAIL-07).
     *
     * Voice & substance are locked by D-01 — DO NOT paraphrase the body copy.
     * Founder edits the final string in PR before merge; Claude polishes JSX
     * rendering only (typography, spacing, brand strip).
     *
     * Layout (top → bottom, per UI-SPEC.md §"Email Template Component Contract"):
     *   1. Teal header strip (#0D9488) — 48px tall, white "Quibly" wordmark
     *   2. Content zone — 32px/24px padding, body paragraphs
     *   3. Hr divider — #e5e5e5, 24px margin
     *   4. Footer zone — postal address (EMAIL-05 CAN-SPAM), unsubscribe link (EMAIL-04)
     *
     * Inline styles only — email clients strip external stylesheets and don't
     * support oklch() or Tailwind classes (UI-SPEC.md §Color "no oklch").
     *
     * The `unsubscribeUrl` prop is the HMAC-signed token URL produced by
     * `generateToken(email)` from `lib/unsubscribe-token.ts` (Plan 02 / CD-02).
     * The `postalAddress` prop reads from `env.RESEND_FROM_POSTAL_ADDRESS`
     * (Plan 01 / D-10) — placeholder in dev, real address before production merge.
     */

    export interface WelcomeEmailProps {
      unsubscribeUrl: string
      postalAddress: string
    }

    export function WelcomeEmail({
      unsubscribeUrl,
      postalAddress,
    }: WelcomeEmailProps) {
      return (
        <Html>
          <Head />
          <Preview>
            Welcome to the Quibly waitlist — I&apos;ll be in touch.
          </Preview>
          <Body style={main}>
            <Container style={container}>
              {/* Teal header strip — UI-SPEC #0D9488, 48px height */}
              <Section style={header}>
                <Text style={headerText}>Quibly</Text>
              </Section>

              {/* D-01 locked body copy — DO NOT paraphrase */}
              <Section style={content}>
                <Text style={paragraph}>Hey —</Text>
                <Text style={paragraph}>
                  Thanks for joining the Quibly waitlist. I&apos;m Jeff —
                  a solopreneur building Quibly for other solopreneurs
                  and small operators who are experts at what they
                  make but not necessarily at marketing it.
                </Text>
                <Text style={paragraph}>
                  I&apos;m deep in a strategy-first AI marketing tool that
                  learns your business and runs the marketing loop
                  with you (not at you). I&apos;ll send one more email when
                  I open it up — no spam, no product-launch hype.
                </Text>
                <Text style={paragraph}>
                  In the meantime, hit reply if there&apos;s a marketing
                  problem you wish someone would just solve. I read
                  everything.
                </Text>
                <Text style={paragraph}>— Jeff</Text>
              </Section>

              <Hr style={hr} />

              {/* Footer: unsubscribe (EMAIL-04) + postal address (EMAIL-05 CAN-SPAM) */}
              <Section style={content}>
                <Text style={footer}>
                  <Link href={unsubscribeUrl} style={footerLink}>
                    Unsubscribe
                  </Link>
                  {' · '}
                  {postalAddress}
                </Text>
                <Text style={footer}>
                  You&apos;re receiving this because you signed up for the Quibly waitlist.
                </Text>
              </Section>
            </Container>
          </Body>
        </Html>
      )
    }

    /**
     * React Email dev preview props — visible in `npm run email:dev` (not wired in
     * Phase 4; postal address placeholder kept benign for the preview UI).
     */
    WelcomeEmail.PreviewProps = {
      unsubscribeUrl: 'https://useQuibly.com/unsubscribe?t=preview_token',
      postalAddress: '123 Main St, Anytown, CA 90210',
    } satisfies WelcomeEmailProps

    export default WelcomeEmail

    // ─── Inline styles (hex colors only — UI-SPEC.md §Color) ────────────────────

    const main: React.CSSProperties = {
      backgroundColor: '#ffffff',
      fontFamily:
        '-apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',
      margin: 0,
      padding: 0,
    }

    const container: React.CSSProperties = {
      maxWidth: '600px',
      margin: '0 auto',
    }

    const header: React.CSSProperties = {
      height: '48px',
      padding: '0 24px',
      backgroundColor: '#0D9488',
      textAlign: 'center',
    }

    const headerText: React.CSSProperties = {
      color: '#ffffff',
      fontSize: '16px',
      fontWeight: 600,
      lineHeight: '48px',
      margin: 0,
    }

    const content: React.CSSProperties = {
      padding: '32px 24px',
    }

    const paragraph: React.CSSProperties = {
      color: '#404040',
      fontSize: '14px',
      lineHeight: 1.5,
      margin: '0 0 16px 0',
    }

    const hr: React.CSSProperties = {
      borderColor: '#e5e5e5',
      margin: '24px 0',
    }

    const footer: React.CSSProperties = {
      color: '#737373',
      fontSize: '12px',
      lineHeight: 1.5,
      margin: '0 0 4px 0',
    }

    const footerLink: React.CSSProperties = {
      color: '#737373',
      textDecoration: 'underline',
    }
    ```

    Notes for the executor:
    - The header `Section` uses `lineHeight: '48px'` on `headerText` to vertically-center the wordmark inside the 48px header strip — email clients don't honor flexbox reliably, but `line-height === height` is the universal vertical-center pattern.
    - Do NOT add a CTA button — UI-SPEC.md §Color is explicit: "No teal CTA button in the waitlist welcome email."
    - Do NOT add `--primary` or any `oklch()` reference — email clients reject these. Hex `#0D9488` is the locked equivalent.
    - The `‘ · ’` separator in the footer is a literal middle-dot character (U+00B7) flanked by spaces.
    - Subject line and `from:` header are NOT in this file — they are passed by Plan 05 in the `resend.emails.send({ from, subject, react })` call.
  </action>
  <verify>
    <automated>cd /Users/jeff/repos/quibly-landing && npm run check 2>&1 | tail -10 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `emails/WelcomeEmail.tsx` exists
    - File imports `Body, Container, Head, Hr, Html, Link, Preview, Section, Text` from `@react-email/components`
    - File contains `export interface WelcomeEmailProps` with both `unsubscribeUrl: string` and `postalAddress: string` properties
    - File contains `export function WelcomeEmail(`
    - File contains `export default WelcomeEmail`
    - Body copy contains all four locked phrases (each verifiable via grep):
      - `grep -c "Thanks for joining the Quibly waitlist" emails/WelcomeEmail.tsx` returns 1
      - `grep -c "I&apos;m Jeff" emails/WelcomeEmail.tsx` returns 1
      - `grep -c "strategy-first AI marketing tool" emails/WelcomeEmail.tsx` returns 1
      - `grep -c "hit reply if there" emails/WelcomeEmail.tsx` returns 1
    - Preview text matches D-01 / UI-SPEC: `grep -c "Welcome to the Quibly waitlist" emails/WelcomeEmail.tsx` returns 1
    - Header background is the literal hex `#0D9488` — `grep -c "#0D9488" emails/WelcomeEmail.tsx` returns 1
    - File contains `<Hr style={hr} />` and the `hr` style object with `borderColor: '#e5e5e5'`
    - Footer renders the unsubscribe Link AND `{postalAddress}` — `grep -c "{postalAddress}" emails/WelcomeEmail.tsx` returns 1
    - File does NOT contain `oklch(` — `grep -c "oklch" emails/WelcomeEmail.tsx` returns 0
    - File does NOT contain `className=` (no Tailwind) — `grep -c "className=" emails/WelcomeEmail.tsx` returns 0
    - File does NOT contain a `<Button` import or component (no CTA button per UI-SPEC) — `grep -c "<Button" emails/WelcomeEmail.tsx` returns 0
    - File contains `WelcomeEmail.PreviewProps = {`
    - `npm run check` exits 0 (TS happy with the JSX + style types)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>EMAIL-07 React Email template exists with D-01 locked body copy, EMAIL-04 unsubscribe Link, EMAIL-05 postal address slot, brand-locked teal header strip; ready for Plan 05's action body to render via Resend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05 action → WelcomeEmail props | `unsubscribeUrl` is HMAC-signed (Plan 02 generateToken); `postalAddress` is from validated env (Plan 01 RESEND_FROM_POSTAL_ADDRESS) |
| WelcomeEmail JSX → email client renderer | All inputs are typed strings; no user-supplied HTML; no XSS surface within the email body |
| Recipient email client → unsubscribe URL | URL contains base64url-encoded email + HMAC; verified server-side by Plan 06 unsubscribe route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Tampering | Body copy paraphrasing | mitigate | D-01 acceptance criteria pin the four locked phrases via grep — any paraphrase fails the criteria. Founder edits final voice in PR review (acceptable per CONTEXT — Claude polishes JSX rendering only). |
| T-04-19 | Information Disclosure | Postal address in email | accept | EMAIL-05 / CAN-SPAM REQUIRES the physical address in the email. D-10 mandates a registered agent / PO box / CMRA — NOT a personal home address — to bound disclosure. The address being in every welcome email is the regulatory requirement, not a leak. |
| T-04-20 | Spoofing | Unsubscribe URL forgery | mitigate (Plan 02) | The URL is generated by `generateToken(email)` from `lib/unsubscribe-token.ts` which signs with `RESEND_WEBHOOK_SECRET`. WelcomeEmail itself is a presentation surface — the cryptographic mitigation lives in Plan 02. |
| T-04-21 | Information Disclosure | DKIM coverage of List-Unsubscribe-Post | mitigate (Plan 05) | Resend's verified-domain DKIM signs all custom headers automatically (Pitfall 1 / D-02). WelcomeEmail.tsx does not set headers — Plan 05's `resend.emails.send({ headers: {...} })` does. Plan 07 has the Gmail "Show Original" inbox checkpoint to confirm DKIM coverage. |
| T-04-22 | Tampering | Inline-style email-client compatibility | accept | Any future style change must remain hex/inline; UI-SPEC.md §Color is explicit. Acceptance criteria reject `oklch(` and `className=` to enforce. |
</threat_model>

<verification>
After the template ships:
1. `npm run check` exits 0 — TypeScript compiles the JSX with style-prop types
2. `npm run lint` exits 0 — no ESLint errors (the `emails/` path is not in the custom rule scope per `eslint.config.mjs`, but the global rules from next-vitals/typescript still apply)
3. `npm run test:unit` exits 0 — no new tests in this plan but pre-existing suites must remain green
4. Manual sanity (informational, not gating): in dev, `import WelcomeEmail from '@/emails/WelcomeEmail'` from a Server Component or test file imports cleanly with no missing-types complaint
5. Optional follow-up (informational, not blocking): rendering the email in `react-email`'s dev preview (`npx react-email dev` if installed; otherwise deferred to Plan 07 inbox checkpoint) shows the teal header, locked body copy, Hr, and footer in the expected layout
</verification>

<success_criteria>
- `emails/WelcomeEmail.tsx` exists with the D-01 locked body copy verbatim
- WelcomeEmailProps requires both `unsubscribeUrl` (HMAC-signed URL from Plan 02) and `postalAddress` (env from Plan 01)
- All styles are inline hex colors / system font stack — no `oklch()`, no Tailwind classes, no React-DOM-only style props
- Default export `WelcomeEmail` matches the import pattern Plan 05 uses
- `npm run check && npm run lint` both exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-04-SUMMARY.md` documenting:
- Final line count of the file
- Confirmation that all four D-01 body phrases survived verbatim
- Whether `header` style uses `lineHeight: '48px'` (vertical-center pattern) or `display: 'flex'` (less email-client-safe — flag if used)
- Whether `WelcomeEmail.PreviewProps` is wired (informational; not gating)
- Note that the email subject `You're on the Quibly list` and `from:` (`Jeff @ Quibly <hello@useQuibly.com>`) are NOT in this file — Plan 05 sets them when calling `resend.emails.send`
</output>
