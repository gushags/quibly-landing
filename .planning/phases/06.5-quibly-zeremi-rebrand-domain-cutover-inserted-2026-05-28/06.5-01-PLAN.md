---
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/globals.css
  - scripts/import-zeremi-brand.mjs
  - public/brand/zeremi/
  - package.json
autonomous: true
requirements:
  - D-01
  - D-04
  - CD-02
  - CD-03
  - SC-1
  - SC-2
must_haves:
  truths:
    - "D-01: `rebrand/zeremi` branch exists and Task 1 commit lands on it; all subsequent plan commits will land on this branch up to the merge-commit flip (no rebase, no squash)"
    - "The dev server boots with no Quicksand `next/font/google` import in app/layout.tsx (Quicksand fully removed)"
    - "Bree Serif font registers as a CSS variable `--font-bree-serif` and `--font-heading` resolves to it via app/globals.css"
    - "public/brand/zeremi/src/ and public/brand/zeremi/png/ exist and contain the assets from marketing-app/public/brand/zeremi/"
    - "The page renders with Bree Serif applied to all `font-heading` consumers (verified visually after Wave 2 swaps the components)"
  artifacts:
    - path: app/layout.tsx
      provides: "Bree_Serif + Figtree imports; metadataBase + title/siteName updated to zeremi.app/Zeremi"
      contains: "Bree_Serif"
    - path: app/globals.css
      provides: "--font-heading rebound to var(--font-bree-serif), serif"
      contains: "--font-bree-serif"
    - path: scripts/import-zeremi-brand.mjs
      provides: "Re-runnable copy script for marketing-app/public/brand/zeremi/ → public/brand/zeremi/"
    - path: public/brand/zeremi/png/zeremi-icon-square-32.png
      provides: "Favicon source (consumed by Wave 2 plan 03)"
    - path: public/brand/zeremi/png/zeremi-mark-512.png
      provides: "OG overlay mark (consumed by Wave 2 plan 03)"
    - path: public/brand/zeremi/src/zeremi-mark.svg
      provides: "Z mark SVG source (consumed by Wave 1 plan 02 for inline ZeremiIcon)"
  key_links:
    - from: app/layout.tsx
      to: app/globals.css
      via: "${breeSerif.variable} CSS variable wiring on <html>"
      pattern: "breeSerif.variable"
    - from: app/globals.css
      to: Tailwind font-heading utility
      via: "--font-heading: var(--font-bree-serif), serif"
      pattern: "var\\(--font-bree-serif\\)"
---

<objective>
Wave 1 foundation A: swap the heading font from Quicksand to Bree Serif, collapse Figtree weights to 400/600 only (per UI-SPEC §Typography Contract), and import the Zeremi brand assets from `marketing-app/public/brand/zeremi/` into `public/brand/zeremi/`. After this plan, the dev server compiles with the Quicksand import fully removed and the brand PNGs/SVGs are available for downstream plans (Wave 1 plan 02 inlines `zeremi-mark.svg`; Wave 2 plan 03 references the PNGs from icon/apple-icon/OG image routes).

Purpose: Lock D-04 (Bree Serif as `--font-heading`) and CD-03 (re-runnable asset copy script) before any component rename or string sweep. Per CONTEXT D-01 this lands as a per-plan atomic commit on the `rebrand/zeremi` branch.

Output: `app/layout.tsx` swapped, `app/globals.css` single-line CSS variable rebound, `scripts/import-zeremi-brand.mjs` created and executed, `public/brand/zeremi/{src,png}/` populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-VALIDATION.md
@app/layout.tsx
@app/globals.css
@/Users/jeff/repos/marketing-app/app/layout.tsx
@/Users/jeff/repos/marketing-app/app/globals.css
@/Users/jeff/repos/marketing-app/public/brand/zeremi/README.md
@scripts/export-avatar.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create rebrand/zeremi branch + scripts/import-zeremi-brand.mjs + run asset import</name>
  <files>scripts/import-zeremi-brand.mjs, public/brand/zeremi/src/zeremi-mark.svg, public/brand/zeremi/src/zeremi-icon-square.svg, public/brand/zeremi/src/zeremi-icon-circle.svg, public/brand/zeremi/png/*.png, public/brand/zeremi/README.md</files>
  <read_first>
    - scripts/export-avatar.mjs (analog — node:fs/promises pattern with absolute SRC + relative OUT_DIR)
    - /Users/jeff/repos/marketing-app/public/brand/zeremi/README.md (asset inventory, gradient definition, file-to-use mapping)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§"scripts/import-zeremi-brand.mjs" — analog details)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§Asset Inventory — exact filenames + sizes to expect)
  </read_first>
  <action>
    First: create and switch to the `rebrand/zeremi` branch from `main` (per D-01) using `git checkout -b rebrand/zeremi`. All Phase 6.5 plan commits land on this branch.

    Then create `scripts/import-zeremi-brand.mjs` (mirrors `scripts/export-avatar.mjs` pattern per PATTERNS §"scripts/import-zeremi-brand.mjs", CD-03):
    - ESM module using `node:fs/promises` (`mkdir`, `readdir`, `copyFile`, `stat`).
    - Constants: `SRC_DIR = '/Users/jeff/repos/marketing-app/public/brand/zeremi'`, `OUT_DIR = join(process.cwd(), 'public/brand/zeremi')`.
    - Recursively copy `src/`, `png/`, and the top-level `README.md` from SRC_DIR to OUT_DIR. Create OUT_DIR subdirs with `mkdir({ recursive: true })`.
    - Print one line per copied file; exit with `process.exit(1)` on error with the caught error printed via `console.error`.
    - No external dependencies — pure stdlib.

    Add an npm script `"import:brand": "node scripts/import-zeremi-brand.mjs"` to `package.json` so the import is re-runnable.

    Execute `node scripts/import-zeremi-brand.mjs` to populate `public/brand/zeremi/`. Per UI-SPEC §Asset Inventory the expected PNG set includes (at minimum): `zeremi-icon-square-{16,32,48,180,192,512}.png`, `zeremi-icon-circle-{96,128}.png`, `zeremi-mark-{256,512}.png`. SVG sources: `zeremi-mark.svg` (viewBox 0 0 56 88), `zeremi-icon-square.svg`, `zeremi-icon-circle.svg`.

    Do NOT symlink (PATTERNS Anti-pattern: marketing-app is a separate repo). Do NOT modify any file under marketing-app.

    Commit: `feat(06.5-01): import zeremi brand assets + add import-zeremi-brand.mjs`.
  </action>
  <verify>
    <automated>node scripts/import-zeremi-brand.mjs &amp;&amp; test -f public/brand/zeremi/src/zeremi-mark.svg &amp;&amp; test -f public/brand/zeremi/png/zeremi-icon-square-32.png &amp;&amp; test -f public/brand/zeremi/png/zeremi-icon-square-180.png &amp;&amp; test -f public/brand/zeremi/png/zeremi-mark-512.png &amp;&amp; test -f public/brand/zeremi/README.md &amp;&amp; grep -q '"import:brand"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `scripts/import-zeremi-brand.mjs` exists and imports from `node:fs/promises` (no external deps): `grep -q "node:fs/promises" scripts/import-zeremi-brand.mjs` exits 0.
    - Source assertion: `package.json` `scripts` contains key `import:brand`: `node -e "process.exit(JSON.parse(require('fs').readFileSync('package.json')).scripts['import:brand'] ? 0 : 1)"` exits 0.
    - Filesystem assertion: `public/brand/zeremi/src/zeremi-mark.svg` exists AND contains `viewBox="0 0 56 88"` (grep): `grep -q 'viewBox="0 0 56 88"' public/brand/zeremi/src/zeremi-mark.svg` exits 0.
    - Filesystem assertion: All six required PNGs exist — `zeremi-icon-square-{16,32,48,180}.png`, `zeremi-mark-{256,512}.png` — verified with `test -f` for each.
    - Branch assertion: current git branch equals `rebrand/zeremi`: `[[ $(git rev-parse --abbrev-ref HEAD) == "rebrand/zeremi" ]]`.
    - Re-runnability: re-executing `node scripts/import-zeremi-brand.mjs` exits 0 without error (idempotent).
  </acceptance_criteria>
  <done>
    The `rebrand/zeremi` branch exists and is checked out. `scripts/import-zeremi-brand.mjs` exists, is re-runnable, and has populated `public/brand/zeremi/{src,png}/` with the marketing-app brand assets including the 56×88 `zeremi-mark.svg` and the favicon/Apple/OG PNGs that Wave 2 plan 03 consumes. The asset import is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap Quicksand → Bree Serif in app/layout.tsx + rebind --font-heading in app/globals.css</name>
  <files>app/layout.tsx, app/globals.css</files>
  <read_first>
    - app/layout.tsx (current Quicksand import + html className + metadata)
    - app/globals.css (current --font-heading binding)
    - /Users/jeff/repos/marketing-app/app/layout.tsx (authoritative Bree_Serif config — verified per RESEARCH §Standard Stack)
    - /Users/jeff/repos/marketing-app/app/globals.css (line 14: `--font-heading: var(--font-bree-serif), serif`)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§Typography Contract: weight ['400'] Bree Serif; Figtree weights collapsed to ['400','600'])
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pattern 1 + Anti-pattern: requesting weights other than 400 for Bree Serif errors)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§app/layout.tsx — line-numbered transformation list)
  </read_first>
  <action>
    Edit `app/layout.tsx`:
    - Replace import `import { Quicksand, Figtree } from "next/font/google"` with `import { Bree_Serif, Figtree } from "next/font/google"`.
    - Delete the `const quicksand = Quicksand({ ... })` block.
    - Add `const breeSerif = Bree_Serif({ subsets: ["latin"], weight: ["400"], variable: "--font-bree-serif", display: "swap" })` (single-axis font; per RESEARCH Anti-pattern do NOT request any other weight).
    - Update the `const figtree = Figtree({ ... })` block to set `weight: ["400", "600"]` (UI-SPEC §Typography Contract collapses from 4 weights to 2).
    - Replace `${quicksand.variable}` with `${breeSerif.variable}` in the `<html>` className.
    - Update `metadataBase: new URL("https://useQuibly.com")` → `new URL("https://zeremi.app")`.
    - Update title metadata: `template: "%s | Quibly"` → `"%s | Zeremi"`; `default: "Quibly — AI Marketing..."` → `"Zeremi — AI Marketing..."` (preserve the rest of the tagline verbatim).
    - Update openGraph block: `url: "https://useQuibly.com"` → `"https://zeremi.app"`; `siteName: "Quibly"` → `"Zeremi"`.
    - Update description and any other Quibly literal in this file to Zeremi.

    Edit `app/globals.css`:
    - Replace the line `--font-heading: var(--font-quicksand);` with `--font-heading: var(--font-bree-serif), serif;` (matches marketing-app/app/globals.css line 14 exactly).
    - Do NOT touch any other line in this file (per RESEARCH Anti-pattern: no oklch / radius / sidebar token changes — the teal palette is identical pre/post-rebrand).

    Commit: `feat(06.5-01): swap Quicksand → Bree Serif in layout + globals (D-04)`.
  </action>
  <verify>
    <automated>grep -q "Bree_Serif" app/layout.tsx &amp;&amp; ! grep -q "Quicksand" app/layout.tsx &amp;&amp; grep -q "var(--font-bree-serif), serif" app/globals.css &amp;&amp; ! grep "var(--font-quicksand)" app/globals.css &amp;&amp; grep -q "https://zeremi.app" app/layout.tsx &amp;&amp; ! grep "useQuibly.com" app/layout.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/layout.tsx` contains `Bree_Serif` and does NOT contain `Quicksand`: `grep -q Bree_Serif app/layout.tsx` exits 0 AND `! grep -q Quicksand app/layout.tsx`.
    - Source assertion: `app/layout.tsx` Bree Serif config has `weight: ["400"]` — single string in array (no other weights): `grep -E 'Bree_Serif\(' -A 5 app/layout.tsx | grep -q 'weight: \["400"\]'`.
    - Source assertion: `app/layout.tsx` Figtree config has `weight: ["400", "600"]` (no 500 or 700): `grep -E 'Figtree\(' -A 5 app/layout.tsx | grep -q 'weight: \["400", "600"\]'`.
    - Source assertion: `app/layout.tsx` contains `breeSerif.variable` and does NOT contain `quicksand.variable`.
    - Source assertion: `app/layout.tsx` `metadataBase` is `new URL("https://zeremi.app")`: `grep -q 'metadataBase: new URL("https://zeremi.app")' app/layout.tsx`.
    - Source assertion: `app/layout.tsx` contains no occurrences of `Quibly`, `useQuibly`, `quibs` (case-insensitive): `! grep -iE 'Quibly|useQuibly|Quibs' app/layout.tsx`.
    - Source assertion: `app/globals.css` line for `--font-heading` matches exactly `--font-heading: var(--font-bree-serif), serif;`: `grep -q 'var(--font-bree-serif), serif' app/globals.css` exits 0 AND `! grep -q 'var(--font-quicksand)' app/globals.css`.
    - Type check: `npx tsc --noEmit` exits 0 (no type errors introduced).
  </acceptance_criteria>
  <done>
    `app/layout.tsx` imports Bree Serif weight 400 only (Quicksand fully removed), Figtree is collapsed to weights 400/600, the `<html>` `className` uses `breeSerif.variable`, `metadataBase` points to `https://zeremi.app`, and every Quibly/useQuibly string in this file is replaced with Zeremi/zeremi.app. `app/globals.css` `--font-heading` resolves to `var(--font-bree-serif), serif`. All other CSS tokens (oklch, radius, sidebar) are unchanged. The file passes `tsc --noEmit`. Change is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → public/ asset directory | Asset copy from marketing-app — must not pull arbitrary files outside the documented brand directory |
| build pipeline → next/font/google network | Font fetch at build time — must not silently degrade if Bree Serif weight 700 is requested (would degrade brand integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-01-01 | Tampering | scripts/import-zeremi-brand.mjs | mitigate | SRC_DIR is hardcoded to `/Users/jeff/repos/marketing-app/public/brand/zeremi`; no user-input path traversal accepted. Pure stdlib (no external deps). |
| T-065-01-02 | Information Disclosure | app/layout.tsx metadataBase leak | mitigate | metadataBase updated to `https://zeremi.app` in same commit as font swap; verified by grep assertion in acceptance criteria. |
| T-065-01-03 | Denial of Service | Bree Serif font request | mitigate | weight constrained to `["400"]` only — single string verified by grep on `'weight: ["400"]'` (per RESEARCH §Anti-pattern weight 700 errors at build). |
| T-065-01-04 | Tampering | Stale Quicksand reference in layout | mitigate | grep assertion `! grep -iE 'Quibly|useQuibly|Quibs' app/layout.tsx` ensures no residual brand string. CI grep guard (Wave 4 plan 05) locks this permanently. |
</threat_model>

<verification>
- `node scripts/import-zeremi-brand.mjs` exits 0; re-runnable.
- Six required PNGs present in `public/brand/zeremi/png/`.
- `app/layout.tsx`: zero `Quicksand|Quibly|useQuibly|Quibs` matches (case-insensitive grep).
- `app/globals.css`: `--font-heading` points to `var(--font-bree-serif), serif`; no `--font-quicksand` reference.
- `npx tsc --noEmit` clean.
- `npm run dev` boots the server with no compile error attributable to the font swap (manual sanity).
</verification>

<success_criteria>
- Branch `rebrand/zeremi` exists and is checked out.
- `scripts/import-zeremi-brand.mjs` exists and has been executed; `public/brand/zeremi/{src,png}/` populated with marketing-app assets.
- Bree Serif is the resolved `--font-heading`; Quicksand is fully removed from `app/layout.tsx`.
- `metadataBase` + title/siteName/url in `app/layout.tsx` all reference `zeremi.app`/`Zeremi`.
- All work landed as two atomic commits on the `rebrand/zeremi` branch (per D-01 per-plan commit convention).
</success_criteria>

<output>
Create `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-01-SUMMARY.md` when done.
</output>
