---
phase: 06-production-deploy-cutover-runbook
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cutover.md
requirements:
  - DEPLOY-08
autonomous: true
tags:
  - cutover-runbook
  - documentation
  - phase-6
must_haves:
  truths:
    - "docs/cutover.md exists at repo root in newly-created docs/ directory (CD-06)"
    - "Document is FUTURE-oriented (the eventual handoff to marketing-app); it is NOT today's launch checklist (D-03)"
    - "Step-numbered runbook with `## Step N: <Action>` headings (CD-02)"
    - "Each step has imperative `**What to do:**`, `**What to verify:**`, and `**What could break:**` sub-blocks (CD-02 / Pattern 4)"
    - "Step 5 includes the verbatim Vercel cross-project transfer UI flow (Settings → Domains → Add Domain → in-use prompt → confirm) per 06-RESEARCH.md Pattern 2 sub-flow A"
    - "Step 2 (CSV export) references the empirical Pitfall 6 / A1 verification — if consent_version column is missing in the dashboard CSV, document the API-fallback snapshot path"
    - "Step 4 broadcast timing is PRE-cutover from quibly-landing's hello@useQuibly.com (D-08) using Resend Broadcasts UI (D-09) — no custom send script"
    - "Step 7 verifies legacy routes resolve at marketing-app (`/`, `/privacy`, `/terms`, `/unsubscribe`, `/sitemap.xml`, `/robots.txt`) — verification only, NO pre-scripted redirect rules (D-04)"
    - "Step 9 enumerates decommission steps that DO NOT delete the repo, Resend domain, or audience"
    - "Rollback Plan section treats reverting the apex transfer as a cold-storage emergency only (D-01); HSTS max-age=300 is the documented safety net"
    - "Length is between 200 and 500 lines (CD-02)"
    - "Document contains zero pre-scripted Next.js `redirect()` rules, zero custom Resend broadcast scripts, zero today's-launch-checklist content (anti-pattern table)"
  artifacts:
    - path: "docs/cutover.md"
      provides: "Future-oriented cutover runbook for the eventual atomic apex transfer to marketing-app"
      min_lines: 200
      contains: "Atomic Vercel cross-project domain transfer"
  key_links:
    - from: "docs/cutover.md Step 5"
      to: "Vercel UI cross-project transfer flow"
      via: "verbatim UI navigation steps + screenshot prompts"
      pattern: "Settings → Domains"
    - from: "docs/cutover.md Step 2"
      to: "Resend Audience CSV export"
      via: "Dashboard manual workflow (NO custom code)"
      pattern: "Audiences →.*Export"
    - from: "docs/cutover.md Rollback Plan"
      to: "HSTS max-age=300 safety net"
      via: "documented 5-min reversibility window from DEPLOY-06"
      pattern: "max-age=300"
---

<objective>
Author `docs/cutover.md` — the single FUTURE-oriented runbook this phase produces. Tone is "me 6 months from now, when marketing-app is finally ready to take over the apex." Step-numbered, with inline "what to verify" and "what could break" callouts per step. Founder edits the prose tone in PR; planner ships the structure + technical content.

Purpose: When the marketing-app is ready to inherit `useQuibly.com`, the cutover must be a 30-minute checklist execution, not a research project. Phase 6 produces this runbook BEFORE the dry-run (Plan 06-04) and BEFORE production go-live (Plan 06-05); the dry-run validates Step 5's UI flow specifics in real time and may write minor corrections back into this document.

Output: `docs/cutover.md` in a newly-created `docs/` directory. 200–500 lines per CD-02. No code changes outside this file. No app-tier code. No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md
@.planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md
@.planning/phases/06-production-deploy-cutover-runbook/06-PATTERNS.md
@/Users/jeff/repos/marketing-app/docs/PRODUCTION-CUTOVER-REMOVE-CLIPROXYAPI.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/cutover.md with all 9 step sections + rollback + checklist</name>
  <files>docs/cutover.md</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md (D-01 rollback posture, D-03 doc split, D-04 verification-only redirects, D-08 broadcast timing, D-09 Resend Broadcasts UI, CD-02 voice/structure, CD-06 location)
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pattern 2 (lines 249–287, Vercel atomic transfer UI flow), §Pattern 4 (lines 308–339, runbook skeleton), §Code Examples lines 587–630 (CSV export workflow + fallback) and lines 631–671 (verbatim Vercel UI flow), §Pitfall 1 lines 379–387 (HSTS rationale), §Pitfall 3 lines 399–409 (env vars don't transfer cross-project — pre-condition for Step 1)
    - .planning/phases/06-production-deploy-cutover-runbook/06-PATTERNS.md lines 81–167 (cutover.md skeleton + analog excerpts + voice + length target)
    - /Users/jeff/repos/marketing-app/docs/PRODUCTION-CUTOVER-REMOVE-CLIPROXYAPI.md (structural analog — copy step-numbered shape, "What could break" callouts, GSD command callouts; do NOT copy content — that doc is code-removal, this is domain-swap)
  </read_first>
  <action>
Create the directory `docs/` (does not yet exist) and write `docs/cutover.md` with the following exact section skeleton (per Pattern 4 / 06-PATTERNS.md line 145–161). Each `## Step N` section MUST contain three labeled sub-blocks: `**What to do:**`, `**What to verify:**`, and `**What could break:**`. Voice is second-person founder-direct ("You're transferring…", "Once you've confirmed…"). Length target: 200–500 lines.

Section structure (use these exact headings, in this exact order):

```markdown
# Cutover Runbook: useQuibly.com → marketing-app

## When to use this runbook
[1 paragraph — when marketing-app is feature-ready and you want the apex to switch from this waitlist to the full marketing site. NOT for emergency rollback (see Rollback Plan); NOT for the launch-day go-live of the waitlist itself (that's 06-UAT.md).]

## Prerequisites (what must be true before starting)
[Bulleted list — at minimum:
  - `marketing-app` Vercel project deployed at preview, healthy
  - `marketing-app` Vercel project owns its own production env vars (`RESEND_API_KEY`, `RESEND_AUDIENCE_ID`, `RESEND_WEBHOOK_SECRET`, `UPSTASH_*`, `RESEND_FROM_POSTAL_ADDRESS`, `NEXT_PUBLIC_SITE_URL`) — Vercel does NOT transfer env vars across projects (Pitfall 3 / A2)
  - Same Vercel team owns both projects
  - Operator has Owner/Member permissions on both projects
  - Resend "Quibly Waitlist" production audience non-empty and current
  - DNS provider for useQuibly.com unchanged from production setup (DO NOT touch nameservers — Pitfall 4)
  - Empty inbox for `hello@useQuibly.com` to compose broadcast (D-09)
  - Welcome email + unsubscribe round-trip currently passing on production
  - You have ~60 minutes uninterrupted; cutover-day-minus-1 dry-run completed]

## Step 1: Verify marketing-app is ready
**What to do:** [list — env vars set in destination project; production build succeeds; key routes return 200 in preview; team membership confirmed]
**What to verify:** [list — one curl per critical route; one Vercel Dashboard glance at the env-vars panel for marketing-app]
**What could break:** [callout — env vars missing in destination project means the form 500s the moment apex flips. Pitfall 3 / A2.]

## Step 2: Export Resend Audience as CSV (snapshot for emergency)
**What to do:** [Resend Dashboard → Audiences → Quibly Waitlist → Export Contacts → download CSV; if audience ≥1000 contacts, an emailed link arrives within minutes (link expires 7 days)]
**What to verify:** [open CSV → confirm columns include id, email, created_at, unsubscribed, **and `consent_version`** — see Pitfall 6 below; row count matches Resend Dashboard's contact count]
**What could break:** [callout — the dashboard CSV export may drop the `consent_version` custom property (Pitfall 6 / A1). If that column is MISSING in the CSV: do NOT proceed with cutover until you have a `consent_version` snapshot via API. Add a one-time `lib/audience-snapshot.ts` script using `resend.contacts.list({ audienceId, limit: 100, after })` to paginate and serialize properties manually. Reference 06-RESEARCH.md §Code Examples lines 609–626 for the snippet.]

## Step 3: Compose pre-cutover launch broadcast in Resend Dashboard
**What to do:** [Resend Dashboard → Broadcasts → New Broadcast → select "Quibly Waitlist" audience → set From: `Quibly <hello@useQuibly.com>` (same sender as welcome email) → write subject + body in Resend's editor → preview → save as draft (DO NOT send yet — Step 4 picks the moment)]
**What to verify:** [preview renders correctly across desktop + mobile previewer; List-Unsubscribe and List-Unsubscribe-Post headers are present (Resend handles these automatically — confirm in the preview's "headers" view); audience selection shows correct contact count]
**What could break:** [callout — DO NOT write a custom send-broadcast.ts script (D-09 forbids). Resend Broadcasts UI handles RFC 8058 one-click unsubscribe, suppression list, click tracking; a hand-rolled loop will subtly miss one and torch sender reputation at the worst possible moment.]

## Step 4: Send broadcast (timing — SAME DAY as cutover, BEFORE the transfer)
**What to do:** [click Send in Resend; broadcast goes out from quibly-landing's still-active apex with hello@useQuibly.com sender; subscribers see "Quibly is live!" with a link to useQuibly.com]
**What to verify:** [Resend Broadcasts dashboard shows "Sent" status with delivery counts; spot-check 2-3 inboxes (Gmail / iCloud / Outlook) for arrival within 60s; click-tracking starts populating]
**What could break:** [callout — if you send AFTER the cutover transfer, the broadcast goes from marketing-app's sender setup which has no inbox-placement reputation history with these recipients yet. Pre-cutover preserves the reputation anchor (D-08).]

## Step 5: Atomic Vercel cross-project domain transfer
**What to do:** [paste the verbatim UI flow from 06-RESEARCH.md lines 635–671:
  1. Vercel Dashboard → marketing-app → Settings → Domains → Add Domain → enter `useQuibly.com`
  2. Vercel detects the in-use state and surfaces the prompt: "This domain is currently in use by another project. Move it here?"
  3. Confirm → atomic transfer
  4. Note the EXACT button label seen in dry-run (record discrepancies between this doc and reality during dry-run; update if needed)]
**What to verify:** [`curl -sI https://useQuibly.com | head -5` returns 200 within ~5s; response body shows marketing-app content (not the waitlist hero); HSTS header still emits with `max-age=300` (see 06-PATTERNS.md verification commands)]
**What could break:** [callout — if the in-use prompt does NOT appear (Vercel UI version variance), use sub-flow B: `vercel alias set <marketing-app-deployment-url> useQuibly.com`, then remove from quibly-landing → Add Domain at marketing-app cleanly. DO NOT delete the domain from quibly-landing FIRST — that creates a downtime gap. Reference 06-RESEARCH.md lines 263–266.]

## Step 6: Post-flip smoke test (curl + browser + signup)
**What to do:** [Per D-01: (1) `curl -sI https://useQuibly.com | head -1` returns 200; (2) browser-load the apex; (3) submit a real signup against marketing-app's form; (4) verify Resend audience row + welcome email; (5) `curl -s https://useQuibly.com/opengraph-image | head -1` 200; (6) `curl -s https://useQuibly.com/sitemap.xml` valid XML; (7) `curl -s https://useQuibly.com/robots.txt` expected output]
**What to verify:** [all six checks PASS — if any fails, jump immediately to Rollback Plan (cold storage)]
**What could break:** [callout — a propagation lag on a single resolver may show stale content for up to ~5 min (HSTS=300 bounds this). DO NOT panic on the first single-resolver inconsistency; re-test from a different DNS resolver and wait 5 min.]

## Step 7: Walk marketing-app's route map (verify legacy routes resolve)
**What to do:** [walk the route map; verify `/`, `/privacy`, `/terms`, `/unsubscribe`, `/sitemap.xml`, `/robots.txt` resolve at marketing-app's apex without 404]
**What to verify:** [each route returns 200 OR a documented redirect to a marketing-app-native URL]
**What could break:** [callout — DO NOT pre-script `redirect()` rules in this runbook (D-04). marketing-app's actual route map may differ; verify and add redirects in marketing-app's own repo if any 404 is found.]

## Step 8: Wait 10 min and re-check propagation
**What to do:** [set a timer for 10 min; afterward, re-run all of Step 6 from a fresh terminal + fresh incognito browser; check from a 2nd resolver (e.g., `dig @8.8.8.8 useQuibly.com` and `dig @1.1.1.1 useQuibly.com`)]
**What to verify:** [both resolvers return matching IPs; `curl` from both responds 200 from marketing-app]
**What could break:** [callout — if any resolver still serves stale content after 10 min: do NOT change DNS records. The HSTS max-age=300 cache means individual user agents may take up to 5 min beyond DNS to refresh. Wait the additional 5 min, then escalate.]

## Step 9: Decommission steps (do NOT delete)
**What to do:** [
  - DO NOT delete the `quibly-landing` Vercel project — keep it on a non-apex preview-only URL for cold-storage rollback access for ≥30 days
  - DO NOT delete the Resend "Quibly Waitlist" audience — marketing-app may continue reading from it OR re-import via CSV
  - DO NOT delete the verified Resend domain `useQuibly.com` — DNS records survive transfers (Pitfall 3); marketing-app inherits sender capability without re-verification
  - DO NOT touch DNS nameservers (Pitfall 4)
  - You MAY: archive the GitHub repo (read-only); set quibly-landing's Vercel deployment status to "paused"
]
**What to verify:** [`quibly-landing` still resolves on its `*.vercel.app` URL (rollback path A available); Resend Audience export from Step 2 still readable on disk]
**What could break:** [callout — deleting the Resend domain restarts a 72-hour DNS verification cycle. Deleting the audience loses contact history. Touching nameservers resets every DNS record on the apex. None of these is recoverable in <1 day.]

## Rollback Plan (cold storage emergency only)
[per D-01 — only invoke if Step 6 hard checks fail. Steps:
  1. Vercel Dashboard → quibly-landing → Settings → Domains → Add Domain → `useQuibly.com` → in-use prompt → confirm
  2. Smoke test: `curl -sI https://useQuibly.com | head -1` → 200 from quibly-landing
  3. HSTS max-age=300 means client-side reversibility within ~5 min
  4. `/gsd-debug` to investigate the marketing-app failure that triggered rollback]

**GSD command for rollback investigation:**
```
/gsd-debug Cutover rollback — apex flipped back to quibly-landing because <reason>. Investigate <hypothesis>; check <files>; verify <env>.
```

## Summary Checklist
[GitHub task-list — exhaustive `- [ ]` covering Steps 1–9 + Rollback rehearsal]
```

Constraints (per locked decisions, NON-NEGOTIABLE):
- DO NOT pre-script Next.js `redirect()` rules anywhere (D-04). Step 7 is verification-only.
- DO NOT include a custom send-broadcast.ts script (D-09). Use Resend Broadcasts UI.
- DO NOT include today's launch-go-live items here (D-03). Those live in 06-UAT.md (Plan 06-03).
- DO NOT include `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` examples (Pitfall 1). Reference HSTS only as `max-age=300` per DEPLOY-06.
- DO NOT include any `redirect`, `rewrite`, or middleware code samples for marketing-app (D-04).
- DO NOT include code that requires running anything outside Vercel/Resend dashboards EXCEPT the optional `lib/audience-snapshot.ts` fallback path documented inline (Pitfall 6 contingency only).
- The CSV export step MUST cite Pitfall 6 / A1 with the API-fallback escape hatch reference (06-RESEARCH.md lines 609–626).
- The Step 5 transfer flow MUST cite both sub-flow A (in-use prompt) and sub-flow B (`vercel alias set` fallback) per 06-RESEARCH.md lines 261–266.

Tone enforcement (CD-02):
- Second-person ("You're…", "Once you've confirmed…", "If you see X, do Y")
- Imperative voice in step titles ("Verify…", "Export…", "Compose…", "Send…", "Atomic…", "Walk…", "Wait…", "Decommission…")
- Each `**What could break:**` callout is a single paragraph stating the failure mode + the recovery action

Length: 200 ≤ wc -l ≤ 500.
  </action>
  <verify>
    <automated>test -f docs/cutover.md && wc -l docs/cutover.md | awk '{ if ($1 >= 200 && $1 <= 500) exit 0; else exit 1 }' && grep -c '^## ' docs/cutover.md | awk '{ if ($1 >= 12) exit 0; else exit 1 }' && grep -q 'When to use this runbook' docs/cutover.md && grep -q 'Prerequisites' docs/cutover.md && grep -q 'Step 1: Verify marketing-app is ready' docs/cutover.md && grep -q 'Step 2: Export Resend Audience as CSV' docs/cutover.md && grep -q 'Step 3: Compose pre-cutover launch broadcast' docs/cutover.md && grep -q 'Step 4: Send broadcast' docs/cutover.md && grep -q 'Step 5: Atomic Vercel cross-project domain transfer' docs/cutover.md && grep -q 'Step 6: Post-flip smoke test' docs/cutover.md && grep -q 'Step 7: Walk marketing-app' docs/cutover.md && grep -q 'Step 8: Wait 10 min' docs/cutover.md && grep -q 'Step 9: Decommission' docs/cutover.md && grep -q 'Rollback Plan' docs/cutover.md && grep -q 'Summary Checklist' docs/cutover.md && grep -q 'max-age=300' docs/cutover.md && ! grep -E 'preload|includeSubDomains' docs/cutover.md</automated>
  </verify>
  <acceptance_criteria>
- `test -f docs/cutover.md` returns success (file created)
- `test -d docs` returns success (new directory created)
- `wc -l docs/cutover.md` returns a count between 200 and 500 inclusive (CD-02 length target)
- `grep -c '^## ' docs/cutover.md` returns ≥12 (cover heading + 9 steps + rollback + checklist + prerequisites + when-to-use ≥ 12 H2s)
- `grep -q 'Step 1: Verify marketing-app is ready' docs/cutover.md` → matches
- `grep -q 'Step 2: Export Resend Audience as CSV' docs/cutover.md` → matches
- `grep -q 'Step 3: Compose pre-cutover launch broadcast' docs/cutover.md` → matches
- `grep -q 'Step 4: Send broadcast' docs/cutover.md` → matches
- `grep -q 'Step 5: Atomic Vercel cross-project domain transfer' docs/cutover.md` → matches
- `grep -q 'Step 6: Post-flip smoke test' docs/cutover.md` → matches
- `grep -q 'Step 7: Walk marketing-app' docs/cutover.md` → matches
- `grep -q 'Step 8: Wait 10 min' docs/cutover.md` → matches
- `grep -q 'Step 9: Decommission' docs/cutover.md` → matches
- `grep -q 'Rollback Plan' docs/cutover.md` → matches
- `grep -q 'Summary Checklist' docs/cutover.md` → matches
- `grep -c 'What to do:' docs/cutover.md` returns ≥9 (one per step)
- `grep -c 'What to verify:' docs/cutover.md` returns ≥9 (one per step)
- `grep -c 'What could break:' docs/cutover.md` returns ≥7 (one per technical step)
- `grep -q 'max-age=300' docs/cutover.md` → matches (HSTS safety-net narrative)
- `! grep -E 'preload|includeSubDomains' docs/cutover.md` → success (FORBIDDEN — Pitfall 1)
- `grep -q 'Settings → Domains' docs/cutover.md` → matches (Vercel UI nav present)
- `grep -q 'in-use' docs/cutover.md` → matches (in-use prompt cited per Pattern 2 sub-flow A)
- `grep -q 'consent_version' docs/cutover.md` → matches (Pitfall 6 / A1 escape hatch documented)
- `grep -q 'Resend Broadcasts' docs/cutover.md` → matches (D-09 mechanism cited)
- `grep -q 'hello@useQuibly.com' docs/cutover.md` → matches (D-08 sender continuity narrative)
- `grep -q 'DO NOT delete' docs/cutover.md` → matches (Step 9 decommission constraint)
- `! grep -E 'redirect\\(' docs/cutover.md` → success (D-04 — no pre-scripted Next.js redirect() rules)
- `! grep -E 'rewrites?\\s*:' docs/cutover.md` → success (no rewrite rules)
- `! grep -i 'launch checklist for today' docs/cutover.md` → success (D-03 — no today's-launch content)
- `! grep -E 'send-broadcast\\.ts' docs/cutover.md` → success (D-09 — no custom broadcast script reference except in escape-hatch narrative)
- `git diff --stat docs/cutover.md` shows file is new and non-empty
- No other files modified except docs/cutover.md (`git diff --name-only` shows only docs/cutover.md)
- No new dependencies in package.json (DIFF-check: package.json unchanged)
  </acceptance_criteria>
  <done>
docs/cutover.md exists at repo root in newly-created docs/ directory. File is 200–500 lines, contains all 9 Steps + Rollback + Checklist + Prerequisites, includes the verbatim Vercel UI flow at Step 5, cites the Pitfall 6 / consent_version escape hatch at Step 2, and contains zero anti-pattern content (no preload, no pre-scripted redirects, no custom broadcast script, no today's-launch checklist). Founder may edit prose tone in PR; structure and technical content are locked by this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → operator (future me) | The runbook is the trust artifact carrying cutover knowledge across time; staleness is the threat |
| Operator → Vercel/Resend dashboards | Cross-platform manual operations — runbook is the only checkpointing layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-V14-RUNBOOK-STALE | Information Disclosure (operator misled by stale doc) | docs/cutover.md | mitigate | Step 5 explicitly notes "record discrepancies during dry-run; update if needed" — Plan 06-04's dry-run validates and corrects this doc. ASVS V14.x. |
| T-06-V8-AUDIT-TRAIL | Information Disclosure (GDPR audit trail loss on cutover) | Step 2 CSV export | mitigate | Step 2 cites Pitfall 6 / A1 — if `consent_version` column missing in dashboard CSV, escape-hatch documents `lib/audience-snapshot.ts` API path. ASVS V8.x. |
| T-06-V9-CSP-DEFER | Cross-Site Scripting | (CSP omission persists past cutover) | accept | D-11 defers; runbook does not introduce CSP. Future spike post-launch. |
| T-06-V14-PRELOAD-LEAK | Configuration | Operator copy-pastes a "stronger HSTS" example into next.config.ts mid-cutover | mitigate | Runbook narrative ONLY references `max-age=300`. Acceptance criteria reject any line containing `preload` or `includeSubDomains` in the doc. |
| T-06-V8-DSAR | Information Disclosure | DSAR mailbox unreachable post-cutover | accept | DSAR mailbox `privacy@useQuibly.com` provisioning is verified pre-cutover by Plan 06-05 (06-UAT.md hard launch-gate). Cutover does NOT touch DNS, so MX records survive (Pitfall 4 reinforces). |
</threat_model>

<verification>
1. `test -f docs/cutover.md` — file exists.
2. `wc -l docs/cutover.md` — line count between 200 and 500.
3. `grep -c '^## '` — ≥12 H2 sections.
4. All 9 Step headings present (verified by individual `grep -q` per the acceptance criteria).
5. Rollback Plan + Summary Checklist + Prerequisites + When-to-use sections present.
6. Forbidden tokens absent: `preload`, `includeSubDomains`, `redirect()`, `rewrites:`, "launch checklist for today".
7. Required tokens present: `max-age=300`, `Settings → Domains`, `in-use`, `consent_version`, `Resend Broadcasts`, `hello@useQuibly.com`, `DO NOT delete`.
</verification>

<success_criteria>
- docs/cutover.md exists at repo root, 200–500 lines, with all required Step / Rollback / Checklist / Prerequisites sections in the locked order.
- Zero anti-pattern content (no HSTS preload, no pre-scripted Next.js redirects, no custom broadcast script reference outside Pitfall 6 escape hatch, no today's-launch checklist).
- Step 5 contains the verbatim Vercel cross-project transfer UI navigation.
- Step 2 contains the Pitfall 6 / consent_version escape hatch with API-fallback reference.
- Plan 06-04 (dry-run) may add screenshot references and minor corrections to this doc; the structure must remain stable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-deploy-cutover-runbook/06-02-SUMMARY.md` documenting:
- Final docs/cutover.md line count
- The 9 Step headings + Rollback + Checklist (paste the H2 list)
- Confirmation that anti-pattern tokens are absent (paste each `! grep` result)
- Confirmation that no other files changed
</output>
