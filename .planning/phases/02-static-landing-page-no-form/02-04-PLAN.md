---
phase: 02-static-landing-page-no-form
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - app/page.tsx
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - HERO-06
  - HERO-07
  - MOB-01
  - MOB-03
  - MOB-04
  - FOLD-01
  - FOLD-02
  - FOLD-03
  - FOLD-04
tags:
  - page-composition
  - rsc
  - viewport-verification
  - human-verify

must_haves:
  truths:
    - "Visiting `/` (home) renders the full marketing surface in section order: Hero → PlaceholderFormSection → WhyQuibly → FounderVoice → SecondaryCTA → Footer."
    - "On a 320×568 viewport, the headline + sub-headline + mascot + CTA + microcopy all fit above the fold (no vertical scroll required to see them)."
    - "Layout scales cleanly across 320 / 375 / 768 / 1440 viewports without horizontal scroll, broken text wrapping, or clipped components."
    - "The hero `<h1>` text is the LCP candidate — Lighthouse-DevTools `largest-contentful-paint-element` audit reports the H1 selector, not the mascot div."
    - "Clicking the hero CTA `<a href=\"#waitlist\">` smoothly scrolls to the PlaceholderFormSection; with macOS Reduce Motion ON it jumps instantly."
  artifacts:
    - path: "app/page.tsx"
      provides: "Thin RSC composition rendering all six sections in D-16 order"
      exports: ["default function HomePage"]
      contains: "<Hero />"
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/hero.tsx, placeholder-form-section.tsx, why-quibly.tsx, founder-voice.tsx, secondary-cta.tsx, footer.tsx (Plans 02-02 + 02-03)"
      via: "named imports + JSX composition"
      pattern: "from \"@/components/sections/"
  decision_refs:
    - "D-16: section order on the page top-to-bottom — Hero → PlaceholderFormSection → WhyQuibly → FounderVoice → SecondaryCTA → Footer (locked, exact)"
    - "D-26: app/page.tsx becomes a thin composition with no business logic and no inline copy strings — copy lives in each section component"
    - "D-27: Footer is imported by app/page.tsx directly in Phase 2 (Phase 5 may hoist into a route-group layout.tsx — that decision belongs to Phase 5)"
---

<objective>
Replace Phase 1's smoke-test `app/page.tsx` with the real Phase 2 page composition: a thin RSC default export that renders the six sections in the locked D-16 order, wrapped in `<main>` for layout structure, with `<Footer>` outside `<main>` per UI-SPEC § Layout & Interaction Contract.

Then execute the manual viewport sweep that the LHCI plan (02-05) cannot fully automate: verify above-fold composition at 320×568, scaling at 375 / 768 / 1440, the LCP-element identity, and the `prefers-reduced-motion` smooth-scroll guard.

Purpose:
- Wires every component built in Plans 02-02 and 02-03 into the live `/` route.
- Satisfies all 14 requirement IDs covering visual composition: HERO-01..07, MOB-01/03/04, FOLD-01..04. (MOB-02 is covered by Plans 02-01 + 02-03's tap-target classes; PERF-01..03 land in Plan 02-05.)
- The manual checkpoint catches deviations LHCI cannot — content correctness, the LCP-element-identity report, and the `prefers-reduced-motion` UX.

Output: One modified file (`app/page.tsx`) + a developer-confirmed checkpoint that the page renders correctly across viewports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-landing-page-no-form/02-CONTEXT.md
@.planning/phases/02-static-landing-page-no-form/02-RESEARCH.md
@.planning/phases/02-static-landing-page-no-form/02-UI-SPEC.md
@.planning/phases/02-static-landing-page-no-form/02-PATTERNS.md
@.planning/phases/02-static-landing-page-no-form/02-VALIDATION.md
@.planning/phases/02-static-landing-page-no-form/02-01-SUMMARY.md
@.planning/phases/02-static-landing-page-no-form/02-02-SUMMARY.md
@.planning/phases/02-static-landing-page-no-form/02-03-SUMMARY.md
@CLAUDE.md
@app/page.tsx
@app/layout.tsx
@components/sections/hero.tsx
@components/sections/placeholder-form-section.tsx
@components/sections/why-quibly.tsx
@components/sections/founder-voice.tsx
@components/sections/secondary-cta.tsx
@components/sections/footer.tsx

<interfaces>
<!-- All six section components are RSC named exports from Plans 02-02 + 02-03 -->

From components/sections/hero.tsx:
```typescript
export function Hero(): JSX.Element  // Hero with H1 + sub-headline + CTA + microcopy
```

From components/sections/placeholder-form-section.tsx:
```typescript
export function PlaceholderFormSection(): JSX.Element  // <section id="waitlist" scroll-mt-16>…</section>
```

From components/sections/why-quibly.tsx:
```typescript
export function WhyQuibly(): JSX.Element  // 3-column differentiator grid
```

From components/sections/founder-voice.tsx:
```typescript
export function FounderVoice(): JSX.Element  // Centered single-paragraph founder text
```

From components/sections/secondary-cta.tsx:
```typescript
export function SecondaryCTA(): JSX.Element  // H2 + <Button size="hero"> anchor pill
```

From components/sections/footer.tsx:
```typescript
export function Footer(): JSX.Element  // Minimal centered single-row footer
```

<!-- The RootLayout in app/layout.tsx (Phase 1, NOT modified by this plan) wraps the page with: -->
<!-- <html lang="en" className="… h-full antialiased"><body className="min-h-full flex flex-col">{children}</body></html> -->
<!-- The `min-h-full flex flex-col` on body lets <main> + <Footer> stretch correctly. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace `app/page.tsx` with the Phase 2 composition</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (the current Phase 1 smoke-test file — confirm what's being replaced; do NOT preserve any of it)
    - app/layout.tsx (the `<body className="min-h-full flex flex-col">` wrapper that this page renders inside)
    - .planning/phases/02-static-landing-page-no-form/02-PATTERNS.md § "app/page.tsx (page composition, RSC)" — canonical excerpt
    - .planning/phases/02-static-landing-page-no-form/02-CONTEXT.md D-16 (section order), D-26 (thin composition, no inline copy), D-27 (Footer imported by app/page.tsx in Phase 2)
    - .planning/phases/02-static-landing-page-no-form/02-UI-SPEC.md § Layout & Interaction Contract > Section order (the locked structure: `<main>` wraps sections 1–5, Footer outside `<main>`)
    - All six section files from Plans 02-02 + 02-03 (confirm named exports match the imports below)
  </read_first>
  <action>
    Open `app/page.tsx`. The current file is the Phase 1 smoke-test `SmokeTestPage` (39 lines). REPLACE THE ENTIRE FILE with this content (do NOT preserve any imports, JSX, or comments from the smoke test):

    ```tsx
    import { FounderVoice } from "@/components/sections/founder-voice"
    import { Footer } from "@/components/sections/footer"
    import { Hero } from "@/components/sections/hero"
    import { PlaceholderFormSection } from "@/components/sections/placeholder-form-section"
    import { SecondaryCTA } from "@/components/sections/secondary-cta"
    import { WhyQuibly } from "@/components/sections/why-quibly"

    /**
     * Quibly waitlist landing page (Phase 2).
     *
     * Section order (locked by CONTEXT D-16):
     *   1. Hero                       (H1 + mascot + sub-headline + primary CTA + microcopy)
     *   2. PlaceholderFormSection     (id="waitlist" anchor target — Phase 3 replaces inner body)
     *   3. WhyQuibly                  (3-column md:grid differentiator block)
     *   4. FounderVoice               (centered single-paragraph founder text)
     *   5. SecondaryCTA               (bottom-of-page anchor pill back to #waitlist)
     *   6. Footer                     (minimal centered single row, OUTSIDE <main>)
     *
     * Layout structure (UI-SPEC § Layout & Interaction Contract):
     *   <main className="flex flex-col">  ← contains sections 1–5
     *   <Footer />                         ← outside <main>, sticks to bottom via body's flex
     *
     * Pure RSC — no 'use client', no event handlers, no client-side state.
     * Phase 5 owns metadata (OG, title, description); Phase 2 leaves layout.tsx alone.
     */
    export default function HomePage() {
      return (
        <>
          <main className="flex flex-col">
            <Hero />
            <PlaceholderFormSection />
            <WhyQuibly />
            <FounderVoice />
            <SecondaryCTA />
          </main>
          <Footer />
        </>
      )
    }
    ```

    Locked rules:

    1. **Section order** (D-16): EXACTLY `Hero → PlaceholderFormSection → WhyQuibly → FounderVoice → SecondaryCTA → Footer`. Do NOT reorder. PlaceholderFormSection MUST sit between Hero and WhyQuibly because the Hero CTA `href="#waitlist"` smooth-scrolls down to it (a few hundred px below the fold), and the section needs to be reachable as the FIRST below-fold section so the scroll lands cleanly.

    2. **`<main>` wraps sections 1–5; `<Footer />` is outside `<main>`** (UI-SPEC § Layout): Critical for accessibility — `<main>` is the primary content landmark. The footer is a separate `<footer>` landmark (already declared in `footer.tsx`). Wrapping Footer inside `<main>` would create a nested landmark anti-pattern.

    3. **`<main className="flex flex-col">`** (UI-SPEC § Layout): The body is `min-h-full flex flex-col` (set by layout.tsx); `<main>` inherits as a flex child. NOT `min-h-screen` (that's a redundant constraint when body is already `min-h-full`).

    4. **No inline copy strings** (D-26): All copy lives inside the section components. `app/page.tsx` is a thin composition — no headline strings, no descriptions, no decision logic.

    5. **No `'use client'`** (D-26): Pure RSC. The page tree is server-rendered to static HTML.

    6. **No `metadata` export** (D-26 / RESEARCH § Pattern 2): Phase 5 owns title/description/OG. The Phase 1 baseline metadata in `app/layout.tsx` carries over unchanged.

    7. **Imports**: ONE import per section, all from `@/components/sections/*`. Sort alphabetically (the example above does — FounderVoice, Footer, Hero, PlaceholderFormSection, SecondaryCTA, WhyQuibly). ESLint's import-order rule (if active) is satisfied.

    8. **Default export**: `export default function HomePage()`. Next.js App Router requires the page route to be a default export. Use the name `HomePage` (more descriptive than `Page`); other names also work — Next.js doesn't care about the export's identifier name, only that it's the default.

    9. **Fragment wrapper**: `<>…</>` — no need for an extra `<div>` wrapper because both `<main>` and `<footer>` are valid top-level children of `<body>`.

    10. **No layout.tsx modification**: Phase 2 does NOT touch `app/layout.tsx`. Confirm this by NOT including layout.tsx in the file changes for this task. Phase 5 finalizes title/description/OG.
  </action>
  <verify>
    <automated>grep -F 'export default function' app/page.tsx &amp;&amp; grep -F 'import { Hero } from "@/components/sections/hero"' app/page.tsx &amp;&amp; grep -F 'import { PlaceholderFormSection }' app/page.tsx &amp;&amp; grep -F 'import { WhyQuibly }' app/page.tsx &amp;&amp; grep -F 'import { FounderVoice }' app/page.tsx &amp;&amp; grep -F 'import { SecondaryCTA }' app/page.tsx &amp;&amp; grep -F 'import { Footer }' app/page.tsx &amp;&amp; grep -F '<Hero />' app/page.tsx &amp;&amp; grep -F '<Footer />' app/page.tsx &amp;&amp; ! grep -F "'use client'" app/page.tsx &amp;&amp; ! grep -F 'SmokeTestPage' app/page.tsx &amp;&amp; ! grep -F 'QuibsIcon' app/page.tsx &amp;&amp; npm run check &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` contains a single `export default function` declaration (verified by `grep -c "^export default function" app/page.tsx` returning 1)
    - File contains exactly six imports, all from `@/components/sections/*`: `Hero`, `PlaceholderFormSection`, `WhyQuibly`, `FounderVoice`, `SecondaryCTA`, `Footer`
    - JSX section order verifies as: `<Hero />` then `<PlaceholderFormSection />` then `<WhyQuibly />` then `<FounderVoice />` then `<SecondaryCTA />` then `<Footer />` — read the file and confirm the order matches D-16
    - `grep -F '<main' app/page.tsx` returns 1 match (single `<main>` element)
    - `grep -F '</main>' app/page.tsx` returns 1 match
    - `<Footer />` appears AFTER `</main>` in the file (verified by reading the file)
    - `grep -F 'flex flex-col' app/page.tsx` returns 1 match (on `<main>`)
    - `grep -F 'SmokeTestPage' app/page.tsx` returns zero matches (Phase 1 smoke test is fully replaced)
    - `grep -F 'QuibsIcon' app/page.tsx` returns zero matches (Phase 1 direct import is gone — mascot is now inside HeroMascot inside Hero)
    - `grep -F 'Lorem ipsum' app/page.tsx` returns zero matches (Phase 1 placeholder copy removed)
    - `grep -c "'use client'" app/page.tsx` returns `0`
    - File contains zero `metadata` export (Phase 5 owns metadata; layout.tsx already has the Phase 1 baseline)
    - File line count is ≤40 (thin composition — D-26)
    - `npm run check` exits 0
    - `npm run lint` exits 0
    - `npm run build` exits 0 — Next.js can statically render `/` with no warnings about missing imports or invalid JSX
    - The build output's static-rendering report shows `/` rendered as a static page (the build console prints `○ /` indicating a static page; OR `λ /` would indicate a dynamic page which would be a regression)
  </acceptance_criteria>
  <done>
    `app/page.tsx` is a thin RSC composition rendering all six sections in D-16 order, with `<main>` wrapping sections 1–5 and `<Footer />` outside. Build passes; the route renders as a static page. The Phase 1 smoke test is fully replaced.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual viewport + LCP + reduced-motion verification</name>
  <what-built>
    Plan 02-04 Task 1 wired all six Phase 2 sections into `app/page.tsx` and confirmed `npm run build` passes. The page now renders the full marketing surface as static HTML on `/`.

    Plan 02-05 (Lighthouse CI gate) automates the performance + CLS + render-blocking-script assertions. But Plan 02-05 cannot automate four critical visual / interaction checks:

    1. **Above-fold composition at 320×568** — that the headline + sub-headline + mascot + CTA + microcopy all fit without scrolling on iPhone SE width (HERO-01..05, ROADMAP SC #1).
    2. **Multi-viewport scaling at 375 / 768 / 1440** (MOB-01, MOB-03, MOB-04) — no horizontal scroll, no broken layout, no clipped text.
    3. **LCP element identity** (HERO-06 / D-03) — Lighthouse reports WHICH element is LCP; a human must read the report to confirm it's the H1, not the mascot div.
    4. **`prefers-reduced-motion` smooth-scroll guard** (HERO-07 / SC #5) — needs OS-level Reduce Motion toggle.

    All four are visible in `next dev` + Chrome DevTools. The session before LHCI catches the visible regressions LHCI would flag with cryptic numbers ("LCP candidate: div.bg-gradient-to-br…") rather than directly.
  </what-built>
  <how-to-verify>
    Run `npm run dev` from the repo root (or `npm run build && npm run start` for a closer-to-production check). Visit `http://localhost:3000` in Chrome. Open DevTools (Cmd+Opt+I). Toggle Device Mode (Cmd+Shift+M).

    **Step 1 — 320×568 above-fold** (iPhone SE narrow viewport):
    - Set Device Mode to "iPhone SE" (375×667) THEN manually override width to 320 via the resolution dropdown OR enter "Responsive" with width 320, height 568.
    - Reload the page.
    - Without scrolling, confirm visible:
      - [ ] Quibly mascot (88px teal-gradient rounded square) — VISUALLY top
      - [ ] Headline `You know your business. Quibly knows how to market it.` (Quicksand Bold, 30px on mobile, ~3 lines wrapped)
      - [ ] Sub-headline (Figtree, ~22 words, muted gray)
      - [ ] Primary CTA pill `Join the waitlist` (28px-radius, teal background, white text)
      - [ ] Microcopy `Launching Summer 2026` directly below the CTA
    - If ANY of these is below the fold (requires scrolling), this is a HERO-04/D-07 violation. Tighten section padding to `pt-12` (mobile) and/or `mt-4` between mascot and headline per CONTEXT § Plan latitude.

    **Step 2 — Multi-viewport scaling sweep**:
    - Test at 320, 375, 768, 1440 (Device Mode dropdown for 375 + 768 + 1440 presets; type 320 manually).
    - At each width, confirm:
      - [ ] No horizontal scrollbar on `<body>` (no `overflow-x` from any child)
      - [ ] All text readable (no clipped lines; no headlines wrapping to 5+ lines awkwardly)
      - [ ] WhyQuibly section: single column on 320 / 375 / 700; 3-column at 768+ (the `md:` breakpoint in Tailwind is 768px)
      - [ ] Footer wraps gracefully at 320 (the `flex-wrap` accommodates narrow widths)
      - [ ] No layout breakage: sections don't overlap; gradient doesn't bleed into below-hero sections (Pitfall #3 in RESEARCH.md)

    **Step 3 — LCP element identity** (HERO-06 / D-03):
    - In DevTools, switch to the Lighthouse panel.
    - Set: Mobile preset, Performance category only, click "Analyze page load".
    - When the report renders, click the **Largest Contentful Paint** metric to expand it.
    - Read the "Element" row.
    - Confirm: the LCP element selector references the `<h1>` (e.g., `h1.font-heading.font-bold...`), NOT the mascot div (e.g., `div.bg-gradient-to-br…`).
    - If LCP is reported as the mascot div, this is a HERO-06 regression — the `flex-col-reverse` LCP guard (D-03) is failing. Inspect `components/sections/hero.tsx` to confirm `<h1>` is DOM-first inside the flex container; the wrapper should have `flex-col-reverse` (NOT `flex-col`).

    **Step 4 — `prefers-reduced-motion` guard** (HERO-07 / SC #5):
    - macOS: System Settings → Accessibility → Display → "Reduce motion" → Toggle ON.
    - Reload `http://localhost:3000`.
    - Click the hero "Join the waitlist" CTA.
    - Confirm: the page jumps INSTANTLY to the `#waitlist` section (no smooth-scroll animation).
    - Toggle "Reduce motion" OFF, reload, click the CTA again.
    - Confirm: the page now smooth-scrolls to `#waitlist`.
    - If both states animate identically (smooth-scroll regardless of OS setting), the `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }` rule from Plan 02-01 is missing or out-of-cascade. Verify in `app/globals.css`.

    **Step 5 — Lucide icons + middot accessibility spot-check**:
    - Inspect a WhyQuibly icon (`Target`, `Users`, or `LineChart`) in DevTools Elements panel; confirm `aria-hidden="true"` is present and stroke-width is 1.75 (rendered SVG attribute `stroke-width="1.75"` on the inner `<svg>`).
    - Inspect a footer middot `·` separator; confirm parent `<span>` has `aria-hidden="true"`.

    **Step 6 — Founder paragraph + secondary CTA spot-check**:
    - Scroll to the founder-voice section. Confirm: the paragraph is centered, in `max-w-prose` width, no italic, no quote marks, no avatar.
    - Continue to the secondary CTA. Confirm: the H2 reads `Ready to stop guessing at marketing?` and the button below reads `Don't miss launch — join the waitlist`. Click the button — it should smooth-scroll up to `#waitlist`.

    Document any issue inline (which step failed, what the actual rendering looked like, screenshot if helpful).
  </how-to-verify>
  <resume-signal>
    Type **"approved"** if all six steps pass cleanly. Otherwise describe the failure (which step, what was wrong, what the screen looked like) so a fix-up sub-task can be planned BEFORE Plan 02-05 runs the LHCI gate.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Static asset (HTML, CSS, inline SVG) | All artifacts are author-controlled compile-time strings. `app/page.tsx` is a thin composition of vetted RSC sections — no runtime user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `app/page.tsx` | mitigate | Acceptance criteria explicitly require `grep -c "'use client'"` to return `0`. The page imports six RSC components (all verified by Plans 02-02 + 02-03 to be `'use client'`-free), composing them in JSX. No env vars, no server-only modules. |
| T-02-02 | Tampering (XSS) | `app/page.tsx` | accept | The page contains zero copy strings (D-26). All renderable content lives inside the imported section components, where Plans 02-02 + 02-03 already verified no `dangerouslySetInnerHTML` and React JSX auto-escaping. Risk inherited; no new attack surface from this plan. |
</threat_model>

<verification>
After Task 1 + Task 2 land:

1. `npm run check && npm run lint && npm run build` exit 0.
2. `grep -rn "use client" app/ components/sections/` returns zero matches (the Phase 2 cross-cutting invariant).
3. The build console shows `/` as a STATIC route (`○ /` in the output table) — confirms RSC + static rendering.
4. Manual checkpoint (Task 2) signed off as "approved" by the developer.

Plan 02-05 will then layer on the Lighthouse CI gate — `categories:performance >= 0.90`, `cumulative-layout-shift < 0.1`, plus assertions against the live Vercel preview URL.
</verification>

<success_criteria>
- [ ] `app/page.tsx` is a thin RSC composition rendering all six sections in the D-16 locked order, with `<main>` wrapping sections 1–5 and `<Footer />` outside.
- [ ] The Phase 1 smoke-test page is fully replaced (no `SmokeTestPage`, no `QuibsIcon` import, no `Lorem ipsum`).
- [ ] `npm run check && npm run lint && npm run build` all exit 0.
- [ ] Build report shows `/` rendered as a static page.
- [ ] `grep -rn "use client" app/ components/sections/` returns zero matches.
- [ ] Developer confirms via the human-verify checkpoint:
  - Above-fold composition at 320×568 is correct (HERO-01..05 / SC #1)
  - Multi-viewport scaling 320 / 375 / 768 / 1440 is clean (MOB-01, MOB-03, MOB-04)
  - LCP element identity is the `<h1>` per Lighthouse DevTools (HERO-06 / D-03)
  - `prefers-reduced-motion: reduce` disables smooth-scroll (HERO-07 / SC #5)
  - Lucide icons render with `aria-hidden="true"` and `stroke-width="1.75"`
  - Footer middots are `aria-hidden`
  - Founder paragraph + secondary CTA copy are correct and link target works
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-landing-page-no-form/02-04-SUMMARY.md` documenting:
- The new `app/page.tsx` content (full file contents — it's only ~30 lines).
- Confirmation that `npm run check && npm run lint && npm run build` all exited 0.
- Confirmation that `grep -rn "use client" app/ components/sections/` returned zero matches.
- The build console output showing `/` as a static route.
- Developer's manual-verify response (the "approved" signal text + any noted minor issues that didn't fail the gate).
- The Lighthouse DevTools LCP element selector reported by Step 3 (e.g., `h1.font-heading.font-bold.text-3xl.…`) — useful evidence for Plan 02-05's assertion-debug context.
- A note that Plan 02-05 (LHCI gate) is now unblocked.
</output>
