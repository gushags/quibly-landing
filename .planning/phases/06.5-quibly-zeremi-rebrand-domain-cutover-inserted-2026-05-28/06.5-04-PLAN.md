---
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
plan: 04
type: execute
wave: 3
depends_on:
  - 06.5-03
files_modified:
  - app/actions/join-waitlist.ts
  - emails/WelcomeEmail.tsx
  - scripts/generate-wordmark.mjs
  - public/email/wordmark.png
  - tests/seo.spec.ts
  - tests/legal.spec.ts
  - tests/unit/disposable-domains.test.ts
  - tests/unit/join-waitlist-action.test.ts
  - package.json
autonomous: true
requirements:
  - D-07
  - D-08
  - CD-06
  - SC-1
  - SC-3
  - SC-5
# D-09 (consent_version via git-SHA pipeline) is implemented by Plan 06.5-03's
# privacy edit; this plan honors it by NOT touching the derivation pipeline.
must_haves:
  truths:
    - "Welcome email From is 'Jeff at Zeremi <hello@zeremi.app>'; subject is \"You're on the Zeremi list\"; List-Unsubscribe mailto is unsubscribe@zeremi.app"
    - "WORDMARK_CID is 'wordmark@zeremi' in both emails/WelcomeEmail.tsx (source of truth) and tests/unit/join-waitlist-action.test.ts (mock + assertion)"
    - "public/email/wordmark.png is re-generated as 'zeremi' lowercase in Bree Serif 400"
    - "Vitest unit suite passes with all updated assertions; no test in tests/ greps for 'Quibly' or 'Quibs' (SC #5)"
    - "Playwright e2e specs tests/seo.spec.ts and tests/legal.spec.ts assert Zeremi/zeremi.app/privacy@zeremi.app"
  artifacts:
    - path: emails/WelcomeEmail.tsx
      provides: "Welcome email template with WORDMARK_CID='wordmark@zeremi', alt='zeremi', body copy Zeremi, PreviewProps unsubscribeUrl https://zeremi.app"
      contains: "wordmark@zeremi"
    - path: app/actions/join-waitlist.ts
      provides: "Resend send call with from='Jeff at Zeremi <hello@zeremi.app>', subject Zeremi, List-Unsubscribe mailto zeremi.app"
      contains: "hello@zeremi.app"
    - path: scripts/generate-wordmark.mjs
      provides: "Bree Serif 400 'zeremi' wordmark renderer; outputs public/email/wordmark.png"
      contains: "Bree Serif"
    - path: public/email/wordmark.png
      provides: "Regenerated wordmark PNG for welcome email CID attachment"
  key_links:
    - from: app/actions/join-waitlist.ts
      to: emails/WelcomeEmail.tsx
      via: "import { default as WelcomeEmail, WORDMARK_CID } from '@/emails/WelcomeEmail'"
      pattern: "WORDMARK_CID"
    - from: tests/unit/join-waitlist-action.test.ts
      to: emails/WelcomeEmail.tsx
      via: "vi.mock with WORDMARK_CID: 'wordmark@zeremi'"
      pattern: "'wordmark@zeremi'"
    - from: scripts/generate-wordmark.mjs
      to: public/email/wordmark.png
      via: "writes the rendered Bree Serif 'zeremi' PNG"
      pattern: "public/email/wordmark\\.png"
---

<objective>
Wave 3 plan A: complete the email send-path rebrand (Pattern D triplet — `from` / `subject` / `List-Unsubscribe`), update the welcome email template (`WORDMARK_CID`, body copy, preview props), adapt `scripts/generate-wordmark.mjs` for Bree Serif and regenerate `public/email/wordmark.png` as the lowercase "zeremi" wordmark, and update all four test files (`seo.spec.ts`, `legal.spec.ts`, `disposable-domains.test.ts`, `join-waitlist-action.test.ts`) so the test suite passes with the new brand strings.

D-08 audience renames happen in the Resend Dashboard (Wave 5 plan 06) — the AUDIENCE_ID env values do NOT change so no code update is needed here. Per D-09, the consent_version SHA is already minted by plan 03's privacy edit; this plan does NOT touch `consent_version` logic in `join-waitlist.ts`.

Purpose: Make the email send-path send as Zeremi and make the test suite green so Wave 4 plan 05 (CI grep guard activation) finds zero violations.

Output: Email triplet + welcome template + wordmark PNG + four test files updated; Vitest unit + Playwright e2e suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md
@app/actions/join-waitlist.ts
@emails/WelcomeEmail.tsx
@scripts/generate-wordmark.mjs
@tests/seo.spec.ts
@tests/legal.spec.ts
@tests/unit/disposable-domains.test.ts
@tests/unit/join-waitlist-action.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep app/actions/join-waitlist.ts (Pattern D triplet) and emails/WelcomeEmail.tsx</name>
  <files>app/actions/join-waitlist.ts, emails/WelcomeEmail.tsx</files>
  <read_first>
    - app/actions/join-waitlist.ts (current — lines ~243 `siteUrl` fallback, ~270 `from:`, ~272 `subject:`, ~278 `List-Unsubscribe` mailto)
    - emails/WelcomeEmail.tsx (current — line ~21 `WORDMARK_CID`, ~57 `<Preview>`, ~68 `alt=`, body copy ~79-92, ~108-109 `unsubscribeUrl` PreviewProps)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§Pattern D — Server Action triplet; §emails/WelcomeEmail.tsx — line-numbered transformations)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§Welcome Email Visual Contract: from/subject/List-Unsubscribe + WORDMARK_CID change)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Code Examples — exact before/after for the triplet)
  </read_first>
  <action>
    `app/actions/join-waitlist.ts` (Pattern D triplet — these MUST stay aligned with the verified Resend sender domain):
    - `const siteUrl = resolvedSiteUrl ?? 'https://usequibly.com'` → `'https://zeremi.app'`.
    - `from: 'Jeff at Quibly <hello@usequibly.com>'` → `'Jeff at Zeremi <hello@zeremi.app>'`.
    - `subject: "You're on the Quibly list"` → `"You're on the Zeremi list"`.
    - `'List-Unsubscribe': \`<${unsubscribeUrl}>, <mailto:unsubscribe@usequibly.com>\`` → `<mailto:unsubscribe@zeremi.app>`.
    - JSDoc / comment references to Quibly → Zeremi.
    - Do NOT touch `consent_version` derivation (D-09 — already correct via Phase 4 pipeline; the policy edit in plan 03 mints the new SHA automatically).
    - Do NOT change `RESEND_AUDIENCE_ID` / `RESEND_AUDIENCE_PREVIEW_ID` env reads (D-08 audience rename preserves IDs).

    `emails/WelcomeEmail.tsx`:
    - `export const WORDMARK_CID = 'wordmark@quibly'` → `'wordmark@zeremi'`.
    - `<Preview>Welcome to the Quibly waitlist — I'll be in touch.</Preview>` → `Welcome to the Zeremi waitlist — I'll be in touch.`.
    - `alt='Quibly'` (on the wordmark `<Img>`) → `alt='zeremi'` (lowercase per CD-06).
    - All body copy: "Quibly waitlist" / "Quibly is a strategy-first..." / "the Quibly waitlist" → Zeremi equivalents.
    - `PreviewProps.unsubscribeUrl: 'https://useQuibly.com/unsubscribe?t=preview_token'` → `'https://zeremi.app/unsubscribe?t=preview_token'`.
    - Update the wordmark area `font-family` in inline styles to `'Bree Serif', Georgia, 'Times New Roman', serif` (UI-SPEC §Email-Client Font Fallback Stack) — preserve all other inline style values (hex colors `#0D9488`, `#404040`, etc. carry over).
    - Wordmark PNG is regenerated in Task 2; this template only updates the constant + alt + body strings.

    Commit: `feat(06.5-04): swap Quibly→Zeremi in server action triplet + welcome email template`.
  </action>
  <verify>
    <automated>grep -q "hello@zeremi.app" app/actions/join-waitlist.ts &amp;&amp; grep -q "You're on the Zeremi list" app/actions/join-waitlist.ts &amp;&amp; grep -q "unsubscribe@zeremi.app" app/actions/join-waitlist.ts &amp;&amp; ! grep -iE 'Quibly|usequibly|useQuibly' app/actions/join-waitlist.ts &amp;&amp; grep -q "WORDMARK_CID = 'wordmark@zeremi'" emails/WelcomeEmail.tsx &amp;&amp; ! grep -iE 'wordmark@quibly|Quibly|useQuibly' emails/WelcomeEmail.tsx &amp;&amp; grep -q "https://zeremi.app/unsubscribe" emails/WelcomeEmail.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/actions/join-waitlist.ts` triplet — `grep -q "from: 'Jeff at Zeremi <hello@zeremi.app>'" app/actions/join-waitlist.ts && grep -q "You're on the Zeremi list" app/actions/join-waitlist.ts && grep -q 'mailto:unsubscribe@zeremi.app' app/actions/join-waitlist.ts`.
    - Source assertion: `app/actions/join-waitlist.ts` siteUrl fallback is `https://zeremi.app`.
    - Source assertion: `app/actions/join-waitlist.ts` contains no `Quibly|useQuibly|usequibly` (case-insensitive): `! grep -iE 'Quibly|useQuibly|usequibly' app/actions/join-waitlist.ts`.
    - Source assertion: `emails/WelcomeEmail.tsx` exports `WORDMARK_CID` set to `'wordmark@zeremi'`: `grep -q "WORDMARK_CID = 'wordmark@zeremi'" emails/WelcomeEmail.tsx`.
    - Source assertion: `emails/WelcomeEmail.tsx` PreviewProps unsubscribeUrl host is `zeremi.app`.
    - Source assertion: `emails/WelcomeEmail.tsx` contains no `Quibly|useQuibly|wordmark@quibly` (case-insensitive).
    - Source assertion: WelcomeEmail wordmark font-family fallback includes `Bree Serif`: `grep -q "'Bree Serif'" emails/WelcomeEmail.tsx`.
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Resend send call sends as Zeremi (`hello@zeremi.app`, Zeremi subject, zeremi.app unsubscribe mailto). Welcome email template constants, body copy, and preview props all reference Zeremi. WORDMARK_CID is `wordmark@zeremi`. Type checks clean. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adapt scripts/generate-wordmark.mjs for Bree Serif "zeremi" + regenerate public/email/wordmark.png</name>
  <files>scripts/generate-wordmark.mjs, public/email/wordmark.png, package.json</files>
  <read_first>
    - scripts/generate-wordmark.mjs (current — uses `@fontsource/quicksand` WOFF2, renders 'Quibly' at weight 600, outputs public/email/wordmark.png)
    - package.json (devDependencies — verify whether `@fontsource/bree-serif` is already present)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§scripts/generate-wordmark.mjs — line-numbered transformations)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pattern 5 — wordmark re-generation; assumption A3 — @fontsource/bree-serif convention not yet verified)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-UI-SPEC.md (§Wordmark Rendering: "zeremi" lowercase, Bree Serif 400)
  </read_first>
  <action>
    Ensure `@fontsource/bree-serif` is installed as a devDependency:
    - Check `package.json` first; if absent, run `npm install -D @fontsource/bree-serif`.
    - After install, verify the WOFF2 file path: list `node_modules/@fontsource/bree-serif/files/` and locate the latin-400 WOFF2 file (expected name: `bree-serif-latin-400-normal.woff2`). If the filename differs, update the script `WOFF2_PATH` constant accordingly and document the actual path in the commit message.

    Edit `scripts/generate-wordmark.mjs`:
    - `WOFF2_PATH = 'node_modules/@fontsource/quicksand/files/quicksand-latin-600-normal.woff2'` → `'node_modules/@fontsource/bree-serif/files/bree-serif-latin-400-normal.woff2'` (or the verified path).
    - Console log `Decompressed Quicksand-600 WOFF2 -> TTF` → `Decompressed BreeSerif-400 WOFF2 -> TTF`.
    - Rendered text literal `'Quibly'` → `'zeremi'` (lowercase per CD-06).
    - `fontWeight: 600` → `400` (Bree Serif single-axis; RESEARCH Anti-pattern).
    - `fontFamily: 'Quicksand'` → `'Bree Serif'`.
    - Satori `fonts:` array entry `{ name: 'Quicksand', data: fontData, weight: 600, style: 'normal' }` → `{ name: 'Bree Serif', data: fontData, weight: 400, style: 'normal' }`.
    - Output path `public/email/wordmark.png` UNCHANGED — same path that `emails/WelcomeEmail.tsx` references via WORDMARK_CID.

    Execute the script to regenerate the PNG: `node scripts/generate-wordmark.mjs`. Verify the output file exists and is a valid PNG (`file public/email/wordmark.png` reports PNG, or `stat -f%z public/email/wordmark.png` returns a positive size).

    Commit: `feat(06.5-04): regenerate wordmark.png in Bree Serif "zeremi" (CD-06)`.
  </action>
  <verify>
    <automated>grep -q "bree-serif" scripts/generate-wordmark.mjs &amp;&amp; grep -q "'Bree Serif'" scripts/generate-wordmark.mjs &amp;&amp; grep -q "'zeremi'" scripts/generate-wordmark.mjs &amp;&amp; ! grep -iE 'Quicksand|Quibly' scripts/generate-wordmark.mjs &amp;&amp; node scripts/generate-wordmark.mjs &amp;&amp; test -s public/email/wordmark.png &amp;&amp; node -e "process.exit(require('fs').readFileSync('public/email/wordmark.png').slice(0,8).equals(Buffer.from([0x89,0x50,0x4E,0x47,0x0D,0x0A,0x1A,0x0A])) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - devDependency assertion: `node -e "process.exit(require('./package.json').devDependencies['@fontsource/bree-serif'] ? 0 : 1)"` exits 0.
    - Source assertion: `scripts/generate-wordmark.mjs` references `bree-serif` in WOFF2 path AND `'Bree Serif'` in font name AND `'zeremi'` as rendered text AND `fontWeight: 400`.
    - Source assertion: zero `Quicksand|Quibly` matches (case-insensitive) in `scripts/generate-wordmark.mjs`.
    - Behavior assertion: `node scripts/generate-wordmark.mjs` exits 0.
    - Filesystem assertion: `public/email/wordmark.png` exists and is non-empty (`test -s`).
    - Filesystem assertion: PNG magic bytes verified — the first 8 bytes are `89 50 4E 47 0D 0A 1A 0A` (PNG signature).
  </acceptance_criteria>
  <done>
    `scripts/generate-wordmark.mjs` renders the "zeremi" lowercase wordmark in Bree Serif 400 and writes to `public/email/wordmark.png`. The script is re-runnable. The output PNG has valid PNG magic bytes. `@fontsource/bree-serif` is in devDependencies. Change is committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update tests/seo.spec.ts, tests/legal.spec.ts, tests/unit/disposable-domains.test.ts, tests/unit/join-waitlist-action.test.ts</name>
  <files>tests/seo.spec.ts, tests/legal.spec.ts, tests/unit/disposable-domains.test.ts, tests/unit/join-waitlist-action.test.ts</files>
  <read_first>
    - tests/seo.spec.ts (current — `expect(await page.title()).toMatch(/Quibly/i)` + 4 `useQuibly.com` literal assertions)
    - tests/legal.spec.ts (current — `mailto:privacy@useQuibly.com` selector assertion)
    - tests/unit/disposable-domains.test.ts (current — `'founder@useQuibly.com'` test fixture)
    - tests/unit/join-waitlist-action.test.ts (current — line 12 NEXT_PUBLIC_SITE_URL fixture, line 75 `WORDMARK_CID: 'wordmark@quibly'` mock, lines 304/306/309/312/315 assertions)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-PATTERNS.md (§tests/seo.spec.ts, §tests/legal.spec.ts, §tests/unit/disposable-domains.test.ts, §tests/unit/join-waitlist-action.test.ts — line-numbered transformations)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Pitfall 4 — join-waitlist-action.test.ts hardcodes WORDMARK_CID in both mock AND assertion at lines 75 and 315)
  </read_first>
  <action>
    `tests/seo.spec.ts`:
    - `expect(await page.title()).toMatch(/Quibly/i)` → `/Zeremi/i`.
    - Sitemap assertion `/Sitemap:\s*https:\/\/useQuibly\.com\/sitemap\.xml/i` → `\/Sitemap:\s*https:\/\/zeremi\.app\/sitemap\.xml/i`.
    - All `expect(text).toContain('https://useQuibly.com[...]')` literals → `'https://zeremi.app[...]'` (root, `/privacy`, `/terms`).

    `tests/legal.spec.ts`:
    - `'a[href="mailto:privacy@useQuibly.com"]'` → `'a[href="mailto:privacy@zeremi.app"]'`.

    `tests/unit/disposable-domains.test.ts`:
    - `'founder@useQuibly.com'` → `'founder@zeremi.app'`.

    `tests/unit/join-waitlist-action.test.ts` (RESEARCH Pitfall 4 — TWO occurrences of `'wordmark@quibly'`: the mock at line 75 AND the assertion at line 315; both update in lockstep):
    - `process.env.NEXT_PUBLIC_SITE_URL = 'https://test.useQuibly.com'` → `'https://test.zeremi.app'`.
    - `WORDMARK_CID: 'wordmark@quibly'` (in `vi.mock('@/emails/WelcomeEmail', ...)`) → `'wordmark@zeremi'`.
    - `expect(sendArg.from).toBe('Jeff at Quibly <hello@usequibly.com>')` → `('Jeff at Zeremi <hello@zeremi.app>')`.
    - `expect(sendArg.subject).toBe("You're on the Quibly list")` → `"You're on the Zeremi list"`.
    - `expect(sendArg.headers['List-Unsubscribe']).toContain('<mailto:unsubscribe@usequibly.com>')` → `'<mailto:unsubscribe@zeremi.app>'`.
    - JSDoc comment "Inline wordmark attachment — referenced from WelcomeEmail.tsx as cid:wordmark@quibly" → `cid:wordmark@zeremi`.
    - `expect(sendArg.attachments[0].contentId).toBe('wordmark@quibly')` → `('wordmark@zeremi')`.
    - Sweep any other Quibly literal in the file.

    Run the full unit suite to verify no test breaks: `npm run test:unit -- --run` MUST exit 0.

    Commit: `test(06.5-04): update assertion strings to Zeremi/zeremi.app across 4 test files`.
  </action>
  <verify>
    <automated>! grep -irE 'Quibly|useQuibly|usequibly|wordmark@quibly' tests/ &amp;&amp; grep -q "/Zeremi/i" tests/seo.spec.ts &amp;&amp; grep -q "zeremi.app/sitemap.xml" tests/seo.spec.ts &amp;&amp; grep -q "mailto:privacy@zeremi.app" tests/legal.spec.ts &amp;&amp; grep -q "founder@zeremi.app" tests/unit/disposable-domains.test.ts &amp;&amp; [ "$(grep -c "'wordmark@zeremi'" tests/unit/join-waitlist-action.test.ts)" -ge 2 ] &amp;&amp; npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: zero `Quibly|useQuibly|usequibly|wordmark@quibly` matches in `tests/` directory (case-insensitive): `! grep -irE 'Quibly|useQuibly|usequibly|wordmark@quibly' tests/`. (This is SC #5: "no test in `tests/` may grep for `Quibly` or `Quibs` post-rebrand".)
    - Source assertion: `tests/seo.spec.ts` contains `/Zeremi/i` AND `zeremi.app/sitemap.xml`.
    - Source assertion: `tests/legal.spec.ts` contains `mailto:privacy@zeremi.app` selector.
    - Source assertion: `tests/unit/disposable-domains.test.ts` contains `founder@zeremi.app` fixture.
    - Source assertion: `tests/unit/join-waitlist-action.test.ts` contains `'wordmark@zeremi'` at least twice (mock + assertion per Pitfall 4); `'Jeff at Zeremi <hello@zeremi.app>'`; `"You're on the Zeremi list"`; `'<mailto:unsubscribe@zeremi.app>'`.
    - Behavior assertion: `npm run test:unit -- --run` exits 0 (all unit tests pass with updated assertions).
  </acceptance_criteria>
  <done>
    All four test files use Zeremi/zeremi.app assertions; the `tests/` tree contains zero Quibly references. Vitest unit suite is green. Change is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Resend API | `from:` domain must match the verified Resend sender (`zeremi.app` after Wave 5 verification); a stale `usequibly.com` from-address against a Zeremi-only verified sender would be rejected by Resend and fail signup |
| Welcome email → recipient inbox | WORDMARK_CID mismatch between template and the attachment in `join-waitlist.ts` would render a broken-image icon in inboxes — the constant must be the single source of truth |
| Test mocks → production module | `vi.mock` of `@/emails/WelcomeEmail` must export `WORDMARK_CID` matching the real module to avoid drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-04-01 | Spoofing | Resend send call with mismatched `from` domain | mitigate | Pattern D triplet (from / subject / List-Unsubscribe) updated together; acceptance criterion `! grep -iE 'Quibly|useQuibly|usequibly' app/actions/join-waitlist.ts` ensures no residual Quibly sender string. Wave 5 plan 06 verifies SPF/DKIM/DMARC for `zeremi.app` BEFORE the merge to main so the new sender is signed. |
| T-065-04-02 | Information Disclosure | Wordmark PNG carries old "Quibly" rasterization, leaking the prior brand into Zeremi-branded emails | mitigate | Task 2 regenerates `public/email/wordmark.png` from Bree Serif "zeremi" source; PNG magic-bytes acceptance check confirms a new file was written. |
| T-065-04-03 | Tampering | Test mock drift — WORDMARK_CID mismatch between mock and source | mitigate | Pitfall 4 explicitly cited; acceptance criterion asserts `'wordmark@zeremi'` appears AT LEAST twice in `join-waitlist-action.test.ts` (mock + assertion lockstep). |
| T-065-04-04 | Repudiation | consent_version not flowing correctly when policy changes | mitigate | D-09: privacy edit (plan 03) mints SHA via existing Phase 4 git-SHA pipeline; this plan does NOT touch consent_version derivation (verified by leaving `join-waitlist.ts` consent_version logic unchanged — only the triplet strings change). |
| T-065-04-05 | Tampering | Stale Quibly assertion in a test file silently passing post-rebrand | mitigate | SC #5 grep gate: `! grep -irE 'Quibly|useQuibly' tests/` is in acceptance criteria. CI grep guard activation (Wave 4 plan 05) re-asserts this in CI. |
</threat_model>

<verification>
- Zero `Quibly|useQuibly|usequibly|wordmark@quibly` matches in `app/actions/join-waitlist.ts`, `emails/WelcomeEmail.tsx`, `scripts/generate-wordmark.mjs`, and `tests/`.
- `public/email/wordmark.png` regenerated; valid PNG; size > 0.
- `npm run test:unit -- --run` exits 0.
- `npx tsc --noEmit` exits 0.
- Three atomic commits on `rebrand/zeremi` branch.
</verification>

<success_criteria>
- Pattern D triplet aligned: from / subject / List-Unsubscribe all Zeremi.
- WORDMARK_CID = 'wordmark@zeremi' in both source AND tests (Pitfall 4 closed).
- Wordmark PNG re-rasterized.
- Test suite green with no Quibly references — SC #5 satisfied.
</success_criteria>

<output>
Create `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-04-SUMMARY.md` when done.
</output>
