---
phase: 06-production-deploy-cutover-runbook
plan: 05
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
  - .planning/phases/06-production-deploy-cutover-runbook/screenshots/
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06
  - DEPLOY-07
autonomous: false
tags:
  - production-go-live
  - apex-bind
  - dns-verification
  - mail-tester
  - phase-6
user_setup:
  - service: vercel
    why: "Apex domain zeremi.app bound at Vercel team level (DEPLOY-01/02), production deployment of next.config.ts headers block (Plan 06-01)"
    env_vars: []
    dashboard_config:
      - task: "Confirm apex zeremi.app is bound to zeremi-landing project at the team level (Vercel Dashboard → Team scope → Domains tab)"
        location: "Vercel Dashboard → Team → Domains"
      - task: "Trigger production deployment from main branch with the new next.config.ts headers block"
        location: "Vercel Dashboard → zeremi-landing → Deployments (or `vercel --prod`)"
  - service: resend
    why: "Verify production sender domain DNS rows are green; verify production audience can be written to (DEPLOY-03 / DEPLOY-04 / DEPLOY-05)"
    env_vars: []
    dashboard_config:
      - task: "Confirm zeremi.app domain shows all DNS rows ✓ (SPF, 3× DKIM, DMARC, Return-Path)"
        location: "Resend Dashboard → Domains → zeremi.app"
      - task: "Confirm Zeremi Waitlist (production) audience exists; new contact appears after a real signup"
        location: "Resend Dashboard → Audiences → Zeremi Waitlist"
  - service: dns-provider
    why: "Verify SPF + 3× DKIM + DMARC + Return-Path TXT/CNAME records resolve correctly from public DNS for production apex (DEPLOY-04)"
    env_vars: []
    dashboard_config:
      - task: "No action needed unless verification fails — DNS records are inherited from Phase 4 setup. If a record fails dig verification: confirm at the DNS provider, do NOT touch nameservers (Pitfall 4)"
        location: "External DNS provider (only if dig verification fails)"
  - service: mailbox-host
    why: "privacy@zeremi.app mailbox provisioning (LEGAL-08 / D-02 carryover) — HARD launch-gate"
    env_vars: []
    dashboard_config:
      - task: "Provision privacy@zeremi.app via one of: Resend Inbound forward, Google Workspace alias, ImprovMX, or Cloudflare Email Routing"
        location: "Founder's chosen mailbox provider"
      - task: "Confirm receipt of test email at the destination inbox within 60 seconds"
        location: "Founder's email inbox"
  - service: mail-tester
    why: "10/10 deliverability score from production apex sender (DEPLOY-05)"
    env_vars: []
    dashboard_config:
      - task: "Visit mail-tester.com → copy single-use address → submit production form with that address → check score after welcome email arrives"
        location: "https://www.mail-tester.com"
must_haves:
  truths:
    - "https://zeremi.app resolves to zeremi-landing production deploy with HTTP 200 and brand markers present in body (DEPLOY-01)"
    - "Apex bound at Vercel team level, visible in Team → Domains tab with project assignment to zeremi-landing (DEPLOY-02)"
    - "All 5 hardening headers emit on the production apex with HSTS literal `max-age=300`, no preload, no includeSubDomains (DEPLOY-06)"
    - "Headers also emit on /robots.txt, /sitemap.xml, /opengraph-image, /privacy, /terms (source: '/(.*)' coverage)"
    - "SPF + 3× DKIM + DMARC p=none + Return-Path resolve via dig from public DNS (DEPLOY-03 / DEPLOY-04)"
    - "Resend Dashboard shows all DNS rows ✓ for zeremi.app (DEPLOY-03)"
    - "mail-tester.com returns 10/10 score from a production-apex signup (DEPLOY-05)"
    - "A real production signup writes to Resend Zeremi Waitlist (production) audience and welcome email arrives in Gmail within 60s with one-click unsubscribe round-trip working"
    - "privacy@zeremi.app mailbox is provisioned and receives test email (LEGAL-08 / D-02 HARD launch-gate)"
    - "DevTools Application → Service Workers panel is empty for zeremi.app origin (DEPLOY-07 / CD-03)"
    - "All 9 DEPLOY-XX requirements + LEGAL-08 carryover are checked off in 06-UAT.md (tests 1–10) with PASS or documented issue/fail"
    - "useQuibly.com is no longer bound to the zeremi-landing Vercel project after this plan ships (D-06)"
    - "useQuibly.com is no longer a verified Resend sender after this plan ships (D-07)"
  artifacts:
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-03-team-domains.png"
      provides: "Evidence: apex bound at Vercel team level"
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-04-resend-dns.png"
      provides: "Evidence: Resend Dashboard DNS rows green"
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-05-mailtester.png"
      provides: "Evidence: mail-tester 10/10 score"
    - path: ".planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-08-no-sw.png"
      provides: "Evidence: empty Service Workers panel on production apex load"
  key_links:
    - from: "Plan 06-01 next.config.ts headers block"
      to: "production apex curl -sI emission"
      via: "Vercel production deploy of main branch"
      pattern: "Strict-Transport-Security: max-age=300"
    - from: "Phase 4 Resend DNS setup"
      to: "Phase 6 dig verification on production apex"
      via: "DNS records survive any project rebinding (Architecture Pattern 2)"
      pattern: "v=spf1|v=DKIM1|v=DMARC1"
    - from: "Phase 5 privacy page DSAR contact"
      to: "Phase 6 mailbox provisioning + receipt test"
      via: "privacy@zeremi.app address rendered in /privacy referenced in 06-UAT.md test 1"
      pattern: "privacy@zeremi.app"
---

<objective>
Execute the production go-live: apex bind verification at Vercel team level, deploy `next.config.ts` headers block (from Plan 06-01) to production, run all DNS / header / mail-tester / signup / mailbox / Service Worker checkpoints, and flip 06-UAT.md tests 1–10 from `pending` to `pass` (or `fail`/`issue` with documented gaps).

Purpose: Phase 6 success criterion 1 — "Visiting `https://zeremi.app` resolves to this project's production deploy with the apex domain bound at the Vercel team level." Plus all DNS, deliverability, and security verifications gating public exposure of the form. The HARD launch-gate is test 1 (privacy@zeremi.app mailbox); production form MUST NOT be public until that test is `pass`.

Output: 06-UAT.md tests 1–10 transitioned from `pending` to terminal results; 4 evidence screenshots saved; STATE.md / PROJECT.md hygiene updates noted for the close-phase step (D-02 carryover closures).

This plan is `autonomous: false` — apex bind, DNS verification, mail-tester, mailbox provisioning, and DevTools checks all require founder hands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md
@.planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md
@.planning/phases/06-production-deploy-cutover-runbook/06-PATTERNS.md
@.planning/phases/06-production-deploy-cutover-runbook/06-VALIDATION.md
@.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
@.planning/phases/05-legal-seo-analytics/05-CONTEXT.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-CONTEXT.md
@next.config.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision privacy@zeremi.app mailbox + verify receipt (HARD launch-gate)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - .planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md (D-02 carryover; LEGAL-08 hard launch-gate)
    - .planning/phases/05-legal-seo-analytics/05-CONTEXT.md (CD-07 mailbox provisioning context)
    - 06-UAT.md test 1 expected: block (verbatim founder steps)
  </read_first>
  <what-built>
06-UAT.md test 1 contains the verbatim mailbox provisioning + receipt test steps. This task is the HARD launch-gate — the production form must not be exposed publicly until the mailbox receives a real test email.
  </what-built>
  <how-to-verify>
1. Provision the mailbox via ONE of:
   - **Resend Inbound forward** (if available on the Resend plan): Resend Dashboard → Domains → zeremi.app → set up Inbound → forward `privacy@zeremi.app` to founder's existing inbox.
   - **Google Workspace alias** (if Google Workspace owns the apex MX): Admin → Users → founder → add alias `privacy@zeremi.app`.
   - **ImprovMX** (free email-forwarding service): set up forward `privacy@zeremi.app` → founder's inbox; add MX records as instructed by ImprovMX (DO NOT touch existing apex DNS records other than these new MX records — Pitfall 4 reminder).
   - **Cloudflare Email Routing** (if Cloudflare owns DNS): Email Routing → Routing rules → forward `privacy@zeremi.app` → founder's existing inbox.
2. Wait for any new MX-record propagation (1–10 min depending on TTL).
3. From an EXTERNAL email account (e.g., a personal Gmail unrelated to this domain), send a test email to `privacy@zeremi.app` with subject `DSAR provisioning test <YYYY-MM-DD>`.
4. Confirm receipt in the destination inbox within 60 seconds.
5. Update 06-UAT.md test 1's `note:` field with: date/time sent, date/time received, destination inbox name (e.g., `gushags@gmail.com`), and the mailbox provider chosen (Resend Inbound / Google / ImprovMX / Cloudflare).
6. Flip 06-UAT.md test 1 from `result: pending` to `result: pass`.

If receipt fails after 5 minutes: do NOT proceed with any subsequent task. Halt Phase 6 production go-live until mailbox is reachable. Document the failure mode in 06-UAT.md test 1 with `result: fail`, `severity: blocker`.

DO NOT modify any existing apex DNS records (SPF, DKIM, DMARC, Return-Path). MX records for `privacy@zeremi.app` are additive, not replacements.
  </how-to-verify>
  <resume-signal>
Type "approved + mailbox verified" once the test email is received and 06-UAT.md test 1 is `result: pass`. Or "blocker: mailbox not receiving" with documented failure mode.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- 06-UAT.md test 1 has `result: pass` (or `result: fail` with `severity: blocker` documented)
- 06-UAT.md test 1 note: contains the date/time of receipt + destination inbox + mailbox provider name
- No existing apex DNS records (SPF / DKIM / DMARC / Return-Path) were modified — `dig +short txt zeremi.app | grep spf1` still resolves the SPF record (verify with the same dig command pre and post mailbox setup)
- HARD-GATE enforcement: subsequent tasks 2+ must NOT begin if test 1 is `fail`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy + verify apex bind at team level + DEPLOY-01/02 (screenshot 06-uat-03-team-domains.png)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-03-team-domains.png</files>
  <read_first>
    - 06-UAT.md tests 2 + 3 expected: blocks
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pattern 2 (lines 249–287, team-level binding semantics)
    - next.config.ts (current state — must contain the headers() block from Plan 06-01)
  </read_first>
  <what-built>
Plan 06-01 has merged the `next.config.ts` headers() block to main. Now: deploy that to production, then verify the apex bind state in the Vercel team Domains tab.
  </what-built>
  <how-to-verify>
1. Confirm Plan 06-01 has shipped: `grep -c "max-age=300" next.config.ts` returns 1 on the deployed branch.
2. Trigger a production deployment from main:
   - Either: push the merged commit to main (Vercel CI auto-deploys), OR
   - Run: `vercel --prod` from the repo root (with operator auth).
3. Wait for the deployment to complete (Vercel Dashboard → Deployments → latest production deployment shows "Ready").
4. Smoke test: `curl -sI https://zeremi.app | head -1` → HTTP/2 200; `curl -s https://zeremi.app | grep -c Zeremi` → >0. Paste both outputs into 06-UAT.md test 2's `note:` field. Flip test 2 to `result: pass` if both pass.
5. Open Vercel Dashboard → switch scope to the **team** that owns zeremi-landing → Domains tab.
6. Confirm `zeremi.app` is listed there with project assignment to `zeremi-landing`.
7. **Take screenshot of the team Domains tab** showing the apex listed. Save at exactly:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-03-team-domains.png
   ```
   Create the screenshots/ directory if it doesn't exist (`mkdir -p`).
8. Flip 06-UAT.md test 3 to `result: pass` and add the screenshot path to its `note:` field.

If the apex shows in `zeremi-landing → Settings → Domains` but NOT in the team's Domains tab: per 06-RESEARCH.md DEPLOY-02 finding, the team-level binding is satisfied automatically when the project is owned by the team — confirm the project's owner is the team (not a personal account). Document the verification path in test 3's note: field.

If the deployment fails or shows non-200 from the apex after 5 min: halt; debug. Do NOT proceed to Tasks 3+. The hardening headers (Task 3) cannot verify against a non-deployed apex.
  </how-to-verify>
  <resume-signal>
Type "approved + apex live" once: (a) `curl https://zeremi.app` returns 200 with Zeremi markers, (b) screenshot of team Domains tab is saved, (c) 06-UAT.md tests 2 + 3 flipped to pass.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- `curl -sI https://zeremi.app | head -1` returns `HTTP/2 200`
- `curl -s https://zeremi.app | grep -c Zeremi` returns a count >0
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-03-team-domains.png` returns success
- 06-UAT.md test 2 has `result: pass`
- 06-UAT.md test 3 has `result: pass` (with screenshot path in note:)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify 5 hardening headers emit on production apex (DEPLOY-06)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - 06-UAT.md test 7 expected: block
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Code Examples lines 521–548 (verbatim curl commands)
    - .planning/phases/06-production-deploy-cutover-runbook/06-PATTERNS.md lines 73–77 (forbidden token list)
  </read_first>
  <action>
From a fresh terminal, run the verbatim verification commands from 06-RESEARCH.md lines 521–548 (do NOT paraphrase):

```bash
# Step 1: All 5 hardening headers emit on the apex root
curl -sI https://zeremi.app | grep -iE "strict-transport-security|x-content-type-options|x-frame-options|referrer-policy|permissions-policy"
# Expected: 5 lines (case may vary)

# Step 2: HSTS literal value (no preload, no includeSubDomains)
curl -sI https://zeremi.app | grep -i strict-transport
# Expected EXACTLY: strict-transport-security: max-age=300
# REJECT if response contains "; includeSubDomains" or "; preload"

# Step 3: Sub-route coverage (source: '/(.*)' applies)
curl -sI https://zeremi.app/robots.txt | grep -i strict-transport
curl -sI https://zeremi.app/sitemap.xml | grep -i strict-transport
curl -sI https://zeremi.app/opengraph-image | grep -i strict-transport
curl -sI https://zeremi.app/privacy | grep -i strict-transport
curl -sI https://zeremi.app/terms | grep -i strict-transport
# All 5 sub-routes must emit the HSTS header
```

Paste ALL command outputs verbatim into 06-UAT.md test 7's `note:` field — do not summarize.

Flip 06-UAT.md test 7 to `result: pass` if:
- All 5 headers present in step 1 output
- HSTS line is exactly `max-age=300` (no other tokens)
- All 5 sub-route checks emit the HSTS header

Flip to `result: fail` with `severity: blocker` if:
- HSTS line contains `preload` OR `includeSubDomains` (would mean Plan 06-01 regressed during deploy or PR review let through a forbidden value — IMMEDIATE rollback required; trigger /gsd-debug)
- Any of the 5 headers is missing entirely from step 1 output
- A sub-route returns the apex content but no HSTS header (would mean source pattern is wrong)

DO NOT touch next.config.ts in this task; if test 7 fails, the failure path triggers a separate debug session, not an inline fix.
  </action>
  <verify>
    <automated>curl -sI https://zeremi.app | grep -iE "strict-transport-security|x-content-type-options|x-frame-options|referrer-policy|permissions-policy" | wc -l | grep -q '^5$' && curl -sI https://zeremi.app | grep -i strict-transport | grep -q "max-age=300" && curl -sI https://zeremi.app | grep -i strict-transport | grep -vqE "preload|includeSubDomains" && curl -sI https://zeremi.app/robots.txt | grep -iq strict-transport && curl -sI https://zeremi.app/sitemap.xml | grep -iq strict-transport && curl -sI https://zeremi.app/privacy | grep -iq strict-transport</automated>
  </verify>
  <acceptance_criteria>
- `curl -sI https://zeremi.app` emits all 5 headers (strict-transport-security, x-content-type-options, x-frame-options, referrer-policy, permissions-policy)
- HSTS line is literally `max-age=300` with NO additional tokens
- HSTS header also emits on /robots.txt, /sitemap.xml, /opengraph-image, /privacy, /terms
- 06-UAT.md test 7 has `result: pass` (or `result: fail` with severity: blocker if regression detected)
- 06-UAT.md test 7 note: contains all command outputs verbatim
  </acceptance_criteria>
  <done>
The 5 hardening headers from Plan 06-01 are confirmed emitting on the production apex with HSTS literal value `max-age=300`. Source pattern coverage verified across 5 sub-routes. 06-UAT.md test 7 flipped from pending to pass (or fail with blocker if regression).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify SPF + 3× DKIM + DMARC + Return-Path DNS (DEPLOY-03/04) + Resend Dashboard screenshot</name>
  <files>
    .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md,
    .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-04-resend-dns.png
  </files>
  <read_first>
    - 06-UAT.md test 4 expected: block
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Code Examples lines 486–518 (verbatim dig commands)
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pitfall 4 (DO NOT touch nameservers)
  </read_first>
  <action>
From a fresh terminal, run the verbatim DNS-verification commands from 06-RESEARCH.md lines 486–518. Replace the placeholder DKIM selector names with the actual selectors shown in Resend Dashboard → Domains → zeremi.app → DNS Records pane.

```bash
# Nameserver setup (informational; informs CD-05 in dry-run, recorded here too)
dig +short ns zeremi.app

# SPF
dig +short txt zeremi.app | grep spf1
# Expected: "v=spf1 include:_spf.resend.com ~all" (or Resend regional equivalent)

# DKIM — 3 selectors. Get exact selector names from Resend Dashboard.
# Substitute <selector1>, <selector2>, <selector3> with the real Resend-issued names.
dig +short txt resend._domainkey.zeremi.app
dig +short txt <selector2>._domainkey.zeremi.app
dig +short txt <selector3>._domainkey.zeremi.app
# Each expected: "v=DKIM1; k=rsa; p=<long-base64-public-key>"

# DMARC
dig +short txt _dmarc.zeremi.app
# Expected: "v=DMARC1; p=none; rua=mailto:..."

# Return-Path (Resend-issued subdomain — exact name in Resend Dashboard)
dig +short cname send.zeremi.app
# OR
dig +short txt send.zeremi.app
# Expected: a CNAME pointing to a *.resend.com host
```

Paste ALL outputs verbatim into 06-UAT.md test 4's `note:` field. Substitute the actual Resend-issued selector names where placeholders appear.

Then: open Resend Dashboard → Domains → zeremi.app. Confirm every DNS row shows green ✓. Take a screenshot. Save at exactly:
```
.planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-04-resend-dns.png
```

Flip 06-UAT.md test 4 to `result: pass` if:
- All 5 DNS lookups return non-empty results matching the expected patterns
- DMARC contains `p=none` (not `p=quarantine` or `p=reject` — Phase 4 setup required p=none)
- Resend Dashboard shows all rows green ✓

Flip to `result: fail` with `severity: blocker` if:
- ANY of the 5 DNS lookups return empty (record missing — Resend cannot send mail correctly; rollback triggered)
- DMARC `p=` is anything other than `none`
- Any Resend Dashboard row shows red/pending status

DO NOT touch nameservers (Pitfall 4). DO NOT modify any DNS record in this task.
  </action>
  <verify>
    <automated>dig +short txt zeremi.app | grep -q "spf1" && dig +short txt _dmarc.zeremi.app | grep -q "p=none" && test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-04-resend-dns.png</automated>
  </verify>
  <acceptance_criteria>
- `dig +short txt zeremi.app | grep -c spf1` returns ≥1
- `dig +short txt _dmarc.zeremi.app | grep -c "p=none"` returns ≥1 (DMARC present with p=none)
- 3 DKIM TXT records resolve at the 3 Resend-issued selectors (each contains `v=DKIM1`)
- Return-Path subdomain resolves to a Resend host
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-04-resend-dns.png` returns success
- 06-UAT.md test 4 has `result: pass` (or `result: fail` with documented failure)
- 06-UAT.md test 4 note: contains all dig outputs verbatim + screenshot path
- No DNS records were modified during this task (verify by re-running the dig commands at the end of the task with same outputs)
  </acceptance_criteria>
  <done>
SPF + 3× DKIM + DMARC p=none + Return-Path all resolve from public DNS for the production apex. Resend Dashboard shows the verified state with green check marks. 06-UAT.md test 4 flipped from pending to pass.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: mail-tester 10/10 score from production apex (DEPLOY-05) + screenshot</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-05-mailtester.png</files>
  <read_first>
    - 06-UAT.md test 5 expected: block (verbatim mail-tester workflow)
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pitfall 7 (mail-tester regression rationale)
  </read_first>
  <what-built>
Tasks 3 + 4 confirmed headers emit and DNS resolves. Now: end-to-end deliverability verification via mail-tester from a real production-apex signup.
  </what-built>
  <how-to-verify>
1. Open https://www.mail-tester.com in a browser.
2. Copy the single-use email address shown on mail-tester (format: `test-<random>@srv1.mail-tester.com`).
3. From a fresh browser tab, visit https://zeremi.app (the production apex).
4. Submit the form with the mail-tester address as the email value.
5. Form should show success state (production audience write).
6. Wait up to 60 seconds for the welcome email to arrive at mail-tester (Resend Dashboard → Logs → confirm send status).
7. Return to mail-tester.com tab → click "Then check your score".
8. Expected score: **10/10**.
9. **Take a screenshot** showing the 10/10 score and the breakdown table. Save at exactly:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-05-mailtester.png
   ```
10. Copy the mail-tester result URL (e.g., `https://www.mail-tester.com/test-abc123`) into 06-UAT.md test 5's `note:` field.
11. Flip 06-UAT.md test 5 to `result: pass`.

If score < 10/10: do NOT lower the gate. Inspect which check failed (SPF / DKIM / DMARC / content / blacklist / etc.) and debug before exposing form publicly. Common regression cause from Phase 4: From: address case mismatch (`hello@zeremi.app` vs `hello@zeremi.app`) — verify exact case in Resend Dashboard sender.

If form submission errors out or no welcome email arrives in 60s: also halt — Resend write path is broken; trigger /gsd-debug.

NOTE: this task DOES write to production Resend audience. The mail-tester address will appear in the audience contact list — that's expected and intentional. Optionally: after the test, mark this contact `unsubscribed` in Resend Dashboard so it doesn't receive future broadcasts.
  </how-to-verify>
  <resume-signal>
Type "approved + 10/10" once: (a) mail-tester returned 10/10, (b) screenshot saved, (c) 06-UAT.md test 5 flipped to pass with mail-tester URL in note. Or "score N/10" with breakdown of which checks failed.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-05-mailtester.png` returns success
- 06-UAT.md test 5 has `result: pass` (only valid if score == 10/10)
- 06-UAT.md test 5 note: contains the mail-tester result URL (format: `https://www.mail-tester.com/...`)
- Resend Dashboard shows the welcome email send completed (Logs view, status: delivered)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Real-signup smoke test against production audience + welcome email round-trip</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - 06-UAT.md test 6 expected: block (verbatim end-to-end test steps)
    - .planning/phases/04-resend-wiring-bot-protection-welcome-email/04-CONTEXT.md (contract for welcome email body, From:, Subject, unsubscribe round-trip)
  </read_first>
  <what-built>
Task 5 already exercised the audience-write + welcome-send via mail-tester. Now: end-to-end check from a real human inbox (Gmail) to verify the welcome email renders correctly outside mail-tester's synthetic environment, and that the unsubscribe link round-trip works against the production audience.
  </what-built>
  <how-to-verify>
1. From a fresh personal Gmail inbox (NOT the mail-tester address from Task 5), submit a real signup at https://zeremi.app.
2. Verify each item:
   a. Form replaces with success message ("You're on the list…")
   b. Resend Dashboard → Audiences → "Zeremi Waitlist" (production) → new contact row appears with the test email and current created_at timestamp
   c. Welcome email arrives at the Gmail inbox within 60 seconds
   d. From: renders as `Jeff at Zeremi <hello@zeremi.app>`
   e. Subject: `You're on the Zeremi list`
   f. Body: 4 D-01 paragraphs render correctly; footer shows real postal address (NOT placeholder); Unsubscribe link present
   g. Click the Unsubscribe link in the email → browser navigates to `https://zeremi.app/unsubscribe?t=<token>` → returns 200 OK
   h. Refresh Resend Dashboard → the contact row now shows `unsubscribed: true`
3. Update 06-UAT.md test 6's `note:` field with: test Gmail address used, Resend contact id, send timestamp, unsubscribe timestamp.
4. Flip 06-UAT.md test 6 to `result: pass` if all 8 sub-checks pass.

If any sub-check fails:
- 6c (welcome email no-show): check Resend Logs; could be a Phase 4 regression
- 6f (placeholder postal address visible): means RESEND_FROM_POSTAL_ADDRESS is not set on production; check Vercel env vars (D-02 carryover already supposed to be closed; flag as regression)
- 6g (unsubscribe URL goes wrong host): Phase 4 GAP-1 regression — would mean NEXT_PUBLIC_SITE_URL env var is wrong on production
- 6h (unsubscribed state not set): unsubscribe handler regression

In any failure case: flip test 6 to `result: issue` or `result: fail` with `severity: blocker`. Document specific sub-check that failed in the gap entry. Do NOT proceed to Task 7 until the regression is closed.

DO NOT use the same test email as Task 5 — that one was already added to the audience. Pick a fresh email.
  </how-to-verify>
  <resume-signal>
Type "approved + welcome verified" once all 8 sub-checks pass and 06-UAT.md test 6 is flipped to pass. Or describe specific sub-check failure.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- 06-UAT.md test 6 has `result: pass` (or `result: fail`/`issue` with documented gap)
- 06-UAT.md test 6 note: contains test email + Resend contact id + send/unsubscribe timestamps
- Resend Dashboard shows the test contact row with `unsubscribed: true` after Unsubscribe-link click
- Welcome email rendered with real postal address (NOT placeholder)
- Unsubscribe URL rendered to zeremi.app host (NOT a Vercel preview URL)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Vercel — remove useQuibly.com from zeremi-landing project domains (D-06)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-useQuibly-unbound.png</files>
  <read_first>
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md (D-06 — Vercel unbind; D-05 — release useQuibly.com entirely, no redirect; CD-07 — UI step with screenshot)
  </read_first>
  <what-built>
Tasks 1–6 confirmed zeremi.app is live and Zeremi Waitlist signups + welcome emails are flowing correctly from hello@zeremi.app. Now: remove the legacy useQuibly.com from Vercel so it no longer points to this project. The domain registration stays at the registrar (D-05 — defensive squat), but Vercel stops serving it.
  </what-built>
  <how-to-verify>
Prerequisite: Tasks 1–6 above are all `pass`. zeremi.app is confirmed live with HTTP 200 AND at least one real signup round-trip succeeded (Task 6).

Steps:
1. Open Vercel Dashboard → zeremi-landing Project → Settings → Domains.
2. Locate `useQuibly.com` (and `www.useQuibly.com` if present). Click the "Remove" (or "..." → Remove) action for each.
3. Confirm the removal dialog for each. Vercel stops serving useQuibly.com immediately after removal.
4. Verify the unbind took effect:
   ```bash
   curl -sI https://useQuibly.com | head -5
   ```
   Expected: connection failure or a generic 404/error from the registrar's parking page — NOT a Vercel `x-vercel-id:` response. (The domain registration is still valid at the registrar; it just no longer routes to Vercel.)
5. Take a screenshot of the Vercel Domains panel showing useQuibly.com is **not** listed → save to:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-useQuibly-unbound.png
   ```
   Create the evidence/ directory if it does not exist:
   ```bash
   mkdir -p .planning/phases/06-production-deploy-cutover-runbook/evidence
   ```
  </how-to-verify>
  <resume-signal>
Type "unbound" with the screenshot path once: (a) useQuibly.com is removed from Vercel, (b) curl no longer returns a Vercel response, (c) screenshot saved at the exact path above.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for evidence/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to acceptance_criteria below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all acceptance_criteria conditions verified.</done>
  <acceptance_criteria>
- `test -f .planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-useQuibly-unbound.png` returns success
- `curl -sI https://useQuibly.com` no longer returns a Vercel response (no `x-vercel-id:` header)
- Screenshot shows useQuibly.com NOT listed in zeremi-landing Vercel project domains
- zeremi.app is still listed and serving (confirm with `curl -sI https://zeremi.app | head -1` returns 200)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: Resend — delete useQuibly.com sender domain (D-07)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-resend-useQuibly-deleted.png</files>
  <read_first>
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md (D-07 — Resend sender delete gated by Phase 6.5 plan 06 mail-tester ≥9/10 from zeremi.app)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-06-SUMMARY.md (mail-tester 9.2/10 confirmed; useQuibly.com sender already deleted during Phase 6.5 plan 06 hot-swap; zeremi.app is now the only verified sender)
  </read_first>
  <what-built>
NOTE: Per Phase 6.5 plan 06 deviation, useQuibly.com was already deleted from Resend as part of the forced hot-swap (Resend free-plan single-domain limit). This task verifies that state and captures screenshot evidence for the audit trail. If useQuibly.com has somehow re-appeared in Resend Domains (unlikely), delete it per the steps below.

Prerequisites:
- Phase 6.5 plan 06 confirmed zeremi.app sender verified + mail-tester 9.2/10 (≥9/10 gate PASSED).
- Task 7 above: useQuibly.com unbound from Vercel (D-06 complete).
  </what-built>
  <how-to-verify>
Steps:
1. Open Resend Dashboard → Domains.
2. Verify useQuibly.com is NOT listed (Phase 6.5 plan 06 already deleted it).
   - If it IS listed: Click the domain → Settings → Delete Domain → Confirm. This should not be needed but is included for completeness.
3. Confirm via API (optional belt-and-suspenders):
   ```bash
   curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains | jq '.data[].name'
   ```
   Expected: output contains `"zeremi.app"` only — `"useQuibly.com"` does NOT appear.
4. Take a screenshot of the Resend Domains list showing only `zeremi.app` → save to:
   ```
   .planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-resend-useQuibly-deleted.png
   ```
  </how-to-verify>
  <resume-signal>
Type "deleted" with the screenshot path once: (a) Resend Domains confirms useQuibly.com is absent, (b) zeremi.app is the only listed sender domain, (c) screenshot saved at the exact path above.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for evidence/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to acceptance_criteria below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all acceptance_criteria conditions verified.</done>
  <acceptance_criteria>
- `test -f .planning/phases/06-production-deploy-cutover-runbook/evidence/06-05-resend-useQuibly-deleted.png` returns success
- Screenshot shows Resend Domains list with zeremi.app as the only sender domain
- useQuibly.com does NOT appear in the Resend API domains list
- Zeremi Waitlist audience and contact data are unaffected (sender domain deletion does not affect audience storage)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Verify No Service Worker registered + grep belt-and-suspenders (DEPLOY-07 / CD-03)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-08-no-sw.png</files>
  <read_first>
    - 06-UAT.md test 8 expected: block (verbatim DevTools steps)
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Code Examples lines 551–571 (DevTools workflow + optional grep)
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pitfall 2 (SW dependency leakage rationale)
  </read_first>
  <what-built>
Per CD-03, Phase 6 verifies SW absence via manual DevTools checkpoint (chosen over Playwright spec / CI grep). Belt-and-suspenders: source-level grep that should return zero matches.
  </what-built>
  <how-to-verify>
1. **Source grep (auto):**
   ```bash
   grep -rEn "navigator\\.serviceWorker|register\\s*\\(.*sw\\.|register\\s*\\(.*service-worker" app/ lib/ components/ 2>/dev/null
   ```
   Expected: zero output lines. If any match: HALT — Service Worker code has slipped in. Rollback or remove before exposing form publicly.
2. **DevTools (manual):**
   a. Open https://zeremi.app in a fresh incognito/private window (Chrome or Firefox).
   b. Open DevTools (Cmd-Opt-I / Ctrl-Shift-I).
   c. Click Application tab → Storage section in left sidebar → Service Workers.
   d. Confirm panel is EMPTY for the zeremi.app origin. Acceptable states:
      - "No service workers" message
      - Only "Service workers from other origins" header with no rows beneath
   e. **Take screenshot** of the empty Service Workers panel showing the URL bar at zeremi.app. Save at exactly:
      ```
      .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-08-no-sw.png
      ```
3. Update 06-UAT.md test 8's `note:` field with: grep output (expected: zero lines), DevTools screenshot path, browser used.
4. Flip 06-UAT.md test 8 to `result: pass`.

If DevTools shows any registered SW for zeremi.app origin: HALT and trigger /gsd-debug. This is a blocker for production exposure (cutover atomicity violation).
  </how-to-verify>
  <resume-signal>
Type "approved + no SW" once: (a) source grep returns zero matches, (b) DevTools panel empty, (c) screenshot saved, (d) 06-UAT.md test 8 flipped to pass.
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- `grep -rEn "navigator\\.serviceWorker|register\\s*\\(.*sw\\.|register\\s*\\(.*service-worker" app/ lib/ components/ 2>/dev/null | wc -l` returns 0
- `test -f .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-08-no-sw.png` returns success
- 06-UAT.md test 8 has `result: pass`
- 06-UAT.md test 8 note: contains grep output (zero lines) + screenshot path + browser name
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 10: Verify production OG / sitemap / robots / favicon smoke (Phase 5 carryover)</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - 06-UAT.md test 9 expected: block
    - .planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md (D-02: Phase 5 OG validation already complete against preview; Phase 6 re-verifies against prod apex)
  </read_first>
  <action>
From a fresh terminal, run all of:

```bash
curl -sI https://zeremi.app/opengraph-image | head -1
# Expected: HTTP/2 200

curl -s https://zeremi.app/sitemap.xml | head -20
# Expected: valid XML; <loc>https://zeremi.app/</loc>, <loc>...privacy</loc>, <loc>...terms</loc>

curl -s https://zeremi.app/robots.txt
# Expected: 10 AI-crawler User-agent rules with Allow: / + Sitemap: line

curl -sI https://zeremi.app/icon | head -1
# Expected: HTTP/2 200

curl -sI https://zeremi.app/apple-icon | head -1
# Expected: HTTP/2 200
```

Paste each output verbatim into 06-UAT.md test 9's `note:` field.

Flip 06-UAT.md test 9 to `result: pass` if all 5 endpoints return as expected. Flip to `result: fail`/`issue` if any returns non-200 or missing content.

DO NOT re-validate via opengraph.xyz / X / LinkedIn — Phase 5 already did that against the preview deploy and the apex now serves the same artifacts. Only re-run those external validators if a curl check fails.
  </action>
  <verify>
    <automated>curl -sI https://zeremi.app/opengraph-image | head -1 | grep -q 200 && curl -s https://zeremi.app/sitemap.xml | grep -q "zeremi.app" && curl -s https://zeremi.app/robots.txt | grep -qE "(Allow|GPTBot|Sitemap)" && curl -sI https://zeremi.app/icon | head -1 | grep -q 200 && curl -sI https://zeremi.app/apple-icon | head -1 | grep -q 200</automated>
  </verify>
  <acceptance_criteria>
- `curl -sI https://zeremi.app/opengraph-image | head -1` returns `HTTP/2 200`
- `curl -s https://zeremi.app/sitemap.xml` returns valid XML containing `zeremi.app/`, `/privacy`, `/terms`
- `curl -s https://zeremi.app/robots.txt` returns the 10 AI-crawler rules + Sitemap line (per Phase 5 robots.txt contract)
- `curl -sI https://zeremi.app/icon | head -1` returns 200
- `curl -sI https://zeremi.app/apple-icon | head -1` returns 200
- 06-UAT.md test 9 has `result: pass`
- 06-UAT.md test 9 note: contains all 5 command outputs
  </acceptance_criteria>
  <done>
Production apex serves all SEO/OG/branding artifacts that Phase 5 produced. 06-UAT.md test 9 flipped from pending to pass.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 11: Resend Audience CSV export inspection — Pitfall 6 / A1 empirical verification</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - 06-UAT.md test 10 expected: block
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Pitfall 6 lines 431–441 + §Code Examples lines 587–630 (CSV export workflow + API-fallback path)
  </read_first>
  <what-built>
The production audience now has at least 2 contacts (Task 5 mail-tester + Task 6 Gmail signup, the latter unsubscribed). Now: empirically verify the dashboard CSV export includes the `consent_version` custom property — this gates whether docs/cutover.md Step 2 needs an API-fallback snapshot script for the future real cutover.
  </what-built>
  <how-to-verify>
1. Open Resend Dashboard → Audiences → "Zeremi Waitlist" (production).
2. Click "Export Contacts" (or context menu → Export → CSV).
3. If audience <1000 contacts (highly likely at this stage): download starts immediately.
   If audience ≥1000: an emailed link arrives within minutes (link expires in 7 days, admin-only access).
4. Open the downloaded CSV in any spreadsheet/text editor.
5. Inspect the column header row.
6. **Required check:** Does the column list contain `consent_version` (or `properties.consent_version`, depending on Resend's serialization)?
7. Update 06-UAT.md test 10's `note:` field with: full column header list (verbatim), audience contact count, presence/absence of consent_version column, Resend export delivery method (immediate / emailed link).

Outcomes:
- **consent_version present:** Flip test 10 to `result: pass`. The dashboard export is the canonical cutover.md Step 2 path. No follow-up needed.
- **consent_version absent:** Flip test 10 to `result: fail` with `severity: major` (NOT blocker — does not block today's go-live, but does block cutover.md Step 2 execution at future cutover). Document the gap with:
  ```yaml
  - truth: "Resend Audience CSV export drops consent_version custom property"
    status: missing
    reason: "Pitfall 6 / A1 confirmed empirically — dashboard CSV exports standard columns only"
    severity: major
    test: "Test 10"
    root_cause: "Resend dashboard CSV uses contacts.list API endpoint which does not serialize custom properties"
    artifacts: ["downloaded CSV from Resend Dashboard"]
    missing: ["consent_version column"]
    debug_session: null
  ```
  Then: Plan a follow-up (gap closure) plan to add `lib/audience-snapshot.ts` API-fallback script per 06-RESEARCH.md lines 609–626; update docs/cutover.md Step 2 to reference the new script.

This task is the empirical resolution of A1 — the highest-risk assumed claim in 06-RESEARCH.md.
  </how-to-verify>
  <resume-signal>
Type either "approved + consent_version present" (test 10 pass) or "approved + consent_version absent" (test 10 fail with major severity, gap entry written, follow-up planned).
  </resume-signal>
  <action>This is a checkpoint:human-action task — the executor steps are inside <how-to-verify> above. Claude prepares the environment (mkdir for screenshots/, etc.), surfaces the verification surface, then blocks on the human's resume-signal. Once the human approves, Claude proceeds to validate the acceptance_criteria below before completing the task.</action>
  <verify>
    <automated>echo "checkpoint:human-action — automated verification deferred to <acceptance_criteria> below; gate is human resume-signal"</automated>
  </verify>
  <done>Human resume-signal received and all <acceptance_criteria> conditions verified.</done>
<acceptance_criteria>
- 06-UAT.md test 10 has either `result: pass` or `result: fail` with `severity: major` (NOT pending)
- 06-UAT.md test 10 note: contains the full CSV column header list (verbatim)
- If consent_version absent: 06-UAT.md `## Gaps` block contains the specific YAML gap entry described above (per 05-UAT.md schema)
- A1 assumption from 06-RESEARCH.md is empirically resolved — Phase 6 close-phase summary records the outcome
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 12: Update 06-UAT.md Summary block + frontmatter</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - 06-UAT.md (current state — all tests post-execution)
    - .planning/phases/05-legal-seo-analytics/05-UAT.md lines 123–131 (Summary format reference)
  </read_first>
  <action>
After all earlier tasks complete (Plan 06-04 has flipped tests 11 + 12; this plan has flipped tests 1–10):

1. Tally the test results across all 12 tests. Each test must now have a non-pending `result:` field. If any are still pending: HALT — that test was missed in execution.
2. Update the `## Summary` block to reflect the final tallies. Format (matching 05-UAT.md):
   ```markdown
   ## Summary

   total: 12
   passed: <count of result: pass>
   issues: <count of result: issue>
   pending: 0
   skipped: <count of result: skipped, expected 0>
   blocked: <count of result: blocked, expected 0>
   note: "<optional summary annotation, e.g. 'Phase 6 launch checklist complete; consent_version CSV-export gap deferred to cutover.md Step 2 fallback per Test 10' if applicable>"
   ```
   Also include `failed:` count if any test ended in `result: fail`. Match exactly the field names used in 05-UAT.md / 04-UAT.md.
3. Update the frontmatter `status:` field:
   - All 12 tests `pass` → `status: complete`
   - Any `issue` or `fail` → `status: gaps-identified` (matches 04-UAT.md "diagnosed" pattern; choose closest existing convention)
4. Update the frontmatter `updated:` timestamp to current ISO time.
5. Update the frontmatter `source:` array to list the SUMMARY.md filenames produced by Plans 06-01 through 06-05.
6. Set `## Current Test` to `[testing complete]`.
7. Final sanity check: `grep -c "result: pending" 06-UAT.md` returns 0.
  </action>
  <verify>
    <automated>UAT=.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && grep -c "result: pending" "$UAT" | grep -q '^0$' && grep -c "^### " "$UAT" | grep -q '^12$' && grep -q "total: 12" "$UAT" && grep -qE "status: (complete|gaps-identified|diagnosed)" "$UAT"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "result: pending" 06-UAT.md` returns 0 (every test has a terminal result)
- `grep -c "^### " 06-UAT.md` still returns 12 (no tests added/removed during execution)
- `## Summary` block totals add up: passed + issues + failed + skipped + blocked == 12
- Frontmatter `status:` is no longer `pending` (terminal state set per outcome)
- Frontmatter `updated:` timestamp is current
- `## Current Test` is set to `[testing complete]`
- If any gaps exist: `## Gaps` section is populated per 05-UAT.md YAML schema
  </acceptance_criteria>
  <done>
06-UAT.md is fully populated with terminal results across all 12 tests. Frontmatter reflects the final phase state. The artifact is ready for `/gsd-verify-work` consumption and Phase 6 close-phase update of STATE.md / PROJECT.md (D-02 carryovers closure).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Vercel UI / Resend Dashboard / DNS provider / mailbox host | Manual verification across 4 external services |
| Production apex → public traffic | Form is publicly exposed only after all 10 tests pass |
| Founder's email inbox → DSAR contract | privacy@zeremi.app receipt is the GDPR contract verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-V8-DSAR-MISS | Information Disclosure (DSAR contract violation) | privacy@zeremi.app mailbox | mitigate | Task 1 is the HARD launch-gate. Production form must NOT be public until Task 1 = pass. ASVS V8.x. |
| T-06-V9-WIRE-FMT | Tampering (HSTS preload accidentally shipped) | Production apex header emission | mitigate | Task 3 verifies wire-format `max-age=300` literal. ASVS V9.1.x. Failure path = immediate rollback + /gsd-debug, not inline fix. |
| T-06-V9-DKIM-MISS | Spoofing (mail spoofing via missing DKIM record) | DNS records | mitigate | Task 4 dig commands verify all 3 DKIM selectors return non-empty TXT records. ASVS V9.x. |
| T-06-V9-DMARC-WEAK | Spoofing (overly permissive DMARC) | DNS records | accept | Task 4 verifies DMARC `p=none` (Phase 4 setting). NOT tightened to quarantine/reject in v1 — staged rollout per Phase 4 D-XX; future v1.x tightening planned post-launch. |
| T-06-V9-DELIVERABILITY | Information Disclosure (inbox-folder placement degrades to spam) | mail-tester score | mitigate | Task 5 requires 10/10. Score regression = halt go-live. ASVS V9.x. |
| T-06-V9-SW-PERSIST | Denial of Service (cutover atomicity broken by SW caching) | Production apex client state | mitigate | Task 7 verifies via DevTools (CD-03) + source grep. Belt-and-suspenders. ASVS V9.x. |
| T-06-V8-CSV-AUDIT | Information Disclosure (GDPR audit trail loss on cutover) | Resend Audience CSV export | mitigate | Task 9 empirically verifies consent_version inclusion (Pitfall 6 / A1). Failure = documented gap + follow-up plan, but does NOT block today's go-live. ASVS V8.x. |
| T-06-V14-CFG-DRIFT | Repudiation (06-UAT.md left in pending state) | Phase 6 close-out | mitigate | Task 10 enforces zero-pending state before closure. ASVS V14.x. |
</threat_model>

<verification>
1. Task 1: privacy@zeremi.app mailbox provisioned, test email received, 06-UAT.md test 1 = pass.
2. Task 2: production deployed, apex 200, team-Domains screenshot saved, tests 2 + 3 = pass.
3. Task 3: 5 hardening headers emit on apex with HSTS literal `max-age=300`, sub-route coverage verified, test 7 = pass.
4. Task 4: SPF + 3× DKIM + DMARC p=none + Return-Path resolve via dig; Resend Dashboard screenshot saved, test 4 = pass.
5. Task 5: mail-tester 10/10 from production apex, screenshot saved, test 5 = pass.
6. Task 6: real Gmail signup → audience write + welcome email + unsubscribe round-trip, test 6 = pass.
7. Task 7: source grep = 0 matches, DevTools SW panel empty, screenshot saved, test 8 = pass.
8. Task 8: production OG/sitemap/robots/favicon smoke = pass, test 9 = pass.
9. Task 9: Resend CSV export inspected for consent_version, A1 resolved, test 10 = pass or fail-with-gap.
10. Task 10: 06-UAT.md Summary tallied, frontmatter status set, no test left pending.
</verification>

<success_criteria>
- All 9 DEPLOY-XX requirements verified against the production apex (DEPLOY-01..06 inline; DEPLOY-07 via DevTools + grep; DEPLOY-08 ships separately as docs/cutover.md from Plan 06-02; DEPLOY-09 ships from Plan 06-04).
- LEGAL-08 carryover (privacy mailbox) closed.
- 06-UAT.md tests 1–10 transitioned to terminal results; tests 11–12 already in terminal state from Plan 06-04.
- 4 evidence screenshots saved at the canonical paths.
- A1 assumption (consent_version CSV export) empirically resolved.
- If any test failed: gap entries documented per 05-UAT.md YAML schema; follow-up plan filed if needed.
- STATE.md / PROJECT.md hygiene updates noted in 06-05-SUMMARY.md for the close-phase step (D-02 carryovers closure: postal address, Outlook/iCloud, zero-cookie, OG validation move from Active → Validated; mailbox provisioning move from Pending → Closed).
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-deploy-cutover-runbook/06-05-SUMMARY.md` documenting:
- Mailbox provider chosen for privacy@zeremi.app
- Production deploy timestamp + Vercel deployment URL
- Header verification output (paste verbatim from Task 3)
- DNS verification output (paste verbatim from Task 4)
- mail-tester result URL + score
- Real-signup test details (test inbox, Resend contact id, unsubscribe round-trip outcome)
- Service Worker verification (grep output + DevTools screenshot path)
- OG/sitemap/robots/favicon smoke output
- A1 resolution: consent_version present or absent in CSV export, gap entry filed if absent
- Final 06-UAT.md tally (e.g., "passed: 12, failed: 0, ...")
- D-02 carryovers closure list for STATE.md / PROJECT.md hygiene update during close-phase
</output>
