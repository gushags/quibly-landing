---
phase: 6.5
slug: quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 6.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.x (unit) + Playwright (e2e) |
| **Config file** | `vitest.config.ts`, `playwright.config.ts` |
| **Quick run command** | `pnpm test:unit -- --run` |
| **Full suite command** | `pnpm test:unit -- --run && pnpm test:e2e` |
| **Estimated runtime** | ~120 seconds (unit ~15s + e2e ~90s + ramp-up) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test:unit -- --run` (or the relevant suite subset)
- **After every plan wave:** Run full suite + `pnpm exec tsc --noEmit` + `pnpm lint`
- **Before `/gsd-verify-work`:** Full suite must be green; CI grep guard (`scripts/check-no-quibly.mjs`) exits 0
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

> Populated by the planner. Each task in every PLAN.md MUST appear here with an automated command or be flagged as Manual-Only below.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 6.5-01-XX | 01 | 1 | REQ-N/A (net-new phase) | — | N/A | unit | `pnpm test:unit -- tests/branding.test.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/branding.test.ts` — DOM/string assertions that no `Quibly`/`Quibs` substring appears in rendered output of `app/page.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`, layout metadata, sitemap, robots.
- [ ] `tests/e2e/brand-paint.spec.ts` — Playwright test that loads the landing page on localhost and asserts (a) Bree Serif and Figtree fonts appear in network panel, (b) Quicksand does NOT, (c) gradient Z mark is visible in hero.
- [ ] `scripts/check-no-quibly.mjs` — repo-wide grep guard (excludes `.planning/`, `node_modules/`, `pnpm-lock.yaml`, archived docs, this VALIDATION.md). Wired as a CI step AFTER all source edits are confirmed clean locally (see RESEARCH.md §"CI grep guard sequencing").

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Resend `zeremi.app` sending domain shows `verified: true` on all four DNS records (SPF + 3× DKIM + DMARC) | Phase 6.5 Success Criterion #4 | Resend dashboard verification is a one-time provider-side check; SDK exposes `domains.get` but operator must manually trigger DNS propagation and click "Verify" | Resend dashboard → Domains → `zeremi.app` → all green checkmarks. Then `curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains` shows `status: "verified"` for `zeremi.app`. |
| Welcome email scores ≥9/10 on mail-tester.com | Phase 6.5 Success Criterion #4 | Third-party deliverability score; no API surface in our codebase | Trigger welcome email to a fresh mail-tester.com address from production-equivalent env; record the score in the phase summary. |
| Vercel project `zeremi.app` domain is live with valid SSL | Phase 6.5 Success Criterion #3 | Vercel domain attach + SSL provisioning runs outside the repo | Vercel dashboard → Project → Domains → `zeremi.app` shows "Valid Configuration" + green padlock at `https://zeremi.app`. |
| Old `useQuibly.com` returns 301 → `zeremi.app` (graceful straggler handling) | Recommended in RESEARCH.md §5 | Tested against live DNS, not a unit boundary | `curl -sI https://useQuibly.com` returns `301` with `location: https://zeremi.app/`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (branding test, font assertion, CI grep guard)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
