---
phase: 06-production-deploy-cutover-runbook
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06
  - DEPLOY-07
  - DEPLOY-08
  - DEPLOY-09
autonomous: true
tags:
  - uat
  - launch-gating
  - manual-checkpoints
  - phase-6
must_haves:
  truths:
    - "06-UAT.md exists in the phase directory matching the 04-UAT.md / 05-UAT.md numbered-test format (CD-07)"
    - "Frontmatter has status, phase, source (placeholder), started, updated"
    - "Document has `## Current Test`, `## Tests`, `## Summary`, `## Gaps` sections matching sibling format"
    - "Each `### N. <title>` test contains an `expected: |` YAML-block-scalar with explicit URL/surface, exact human action, observable success criterion (per 06-PATTERNS.md shared pattern)"
    - "Each test has `result: pending` initially (executors flip to pass/fail/issue at execution time)"
    - "Each manual-only test specifies the screenshot path or recorded-output location in 06-UAT.md (per CD-08, 06-VALIDATION.md Manual-Only table)"
    - "All 9 DEPLOY-XX requirements have at least one test row each"
    - "LEGAL-08 carryover (privacy@useQuibly.com mailbox) has its OWN dedicated test marked HARD launch-gate (D-02 / Phase 5 CD-07 carryover)"
    - "DEPLOY-07 has a manual-DevTools-Service-Worker checkpoint (CD-03 picked manual)"
    - "DEPLOY-09 has the 4-screenshot dry-run test (initial bind, transfer to marketing-app + load, transfer back + load — D-05/D-06/D-07)"
    - "Pitfall 6 / A1 empirical CSV-export inspection has a dedicated test (Resend Audience CSV includes consent_version column)"
    - "DEPLOY-06 wire-format verification has a dedicated test (5 headers via curl -sI; HSTS literal max-age=300)"
  artifacts:
    - path: ".planning/phases/06-production-deploy-cutover-runbook/06-UAT.md"
      provides: "Phase 6 launch-gating manual checkpoint checklist"
      min_lines: 150
      contains: "DEPLOY-0"
  key_links:
    - from: "06-UAT.md test rows"
      to: "06-VALIDATION.md per-task verification map"
      via: "shared verification commands (curl, dig, grep) embedded in each `expected:` block"
      pattern: "curl -sI|dig \\+short"
    - from: "06-UAT.md test for privacy mailbox"
      to: "Phase 5 LEGAL-08 carryover (D-02)"
      via: "manual receipt-of-test-email confirmation"
      pattern: "privacy@useQuibly\\.com"
---

<objective>
Author `06-UAT.md` — the Phase 6 launch-gating verification artifact. Numbered tests with PASS/FAIL/NOTES results, matching the format used in `04-UAT.md` and `05-UAT.md` (CD-07). This plan ships the test scaffold; tests transition from `pending` → `pass`/`fail`/`issue` during Plan 06-04 (dry-run) and Plan 06-05 (production go-live) execution.

Purpose: Phase 6 is mostly manual checkpoints (DNS state, Vercel UI flows, mail-tester score, mailbox provisioning) — that's the correct sampling rate for platform-configuration deliverables, not a regression from automated coverage. 06-UAT.md is the canonical checklist gating production exposure of the form to public traffic; it includes the HARD launch-gate for `privacy@useQuibly.com` mailbox provisioning (D-02 / LEGAL-08 carryover).

Output: One file at `.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md` matching sibling format. No code changes outside the .planning/ tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md
@.planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md
@.planning/phases/06-production-deploy-cutover-runbook/06-PATTERNS.md
@.planning/phases/06-production-deploy-cutover-runbook/06-VALIDATION.md
@.planning/phases/05-legal-seo-analytics/05-UAT.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-UAT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 06-UAT.md with all manual launch-gate checkpoints in sibling-phase format</name>
  <files>.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</files>
  <read_first>
    - .planning/phases/05-legal-seo-analytics/05-UAT.md (entire file — sibling format reference; copy frontmatter + section structure verbatim, replace test content)
    - .planning/phases/04-resend-wiring-bot-protection-welcome-email/04-UAT.md (sibling format reference, especially the `result: issue` + `severity: blocker` pattern for failing tests)
    - .planning/phases/06-production-deploy-cutover-runbook/06-CONTEXT.md (D-02 launch-gating items + carryovers, D-05/D-06/D-07 dry-run scope, CD-03 SW manual checkpoint, CD-07 format match, CD-08 mail-tester evidence retention)
    - .planning/phases/06-production-deploy-cutover-runbook/06-VALIDATION.md (Per-Task Verification Map lines 42–58 — exact verification commands per test ID; Manual-Only Verifications table lines 79–88 — manual-only test instructions verbatim)
    - .planning/phases/06-production-deploy-cutover-runbook/06-RESEARCH.md §Code Examples lines 486–518 (verbatim `dig` commands), lines 521–548 (verbatim `curl` commands), lines 551–571 (Service Worker DevTools steps), lines 587–605 (CSV export workflow), lines 631–671 (dry-run UI flow)
    - .planning/phases/06-production-deploy-cutover-runbook/06-PATTERNS.md lines 173–246 (UAT artifact pattern + per-test pattern + test inventory list)
  </read_first>
  <action>
Create `.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md` matching the 05-UAT.md / 04-UAT.md format (CD-07).

**Frontmatter (verbatim, replace dates with current):**
```yaml
# === FRONTMATTER START ===
status: pending
phase: 06-production-deploy-cutover-runbook
source: [06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-04-SUMMARY.md, 06-05-SUMMARY.md]
started: 2026-04-29T00:00:00Z
updated: 2026-04-29T00:00:00Z
# === FRONTMATTER END ===
```

**Section structure (per 05-UAT.md verbatim):**
```markdown
## Current Test

[awaiting Plan 06-04 / 06-05 execution]

## Tests

### 1. <title>
expected: |
  <multi-line YAML block scalar describing exact manual steps + observable
  success criterion>
result: pending

### 2. <title>
...

## Summary

total: N
passed: 0
issues: 0
pending: N
skipped: 0
blocked: 0

## Gaps

[populated only after execution if any test result is `issue` or `fail`]
```

**Required tests (in this exact order, all numbered, all `result: pending`). Each `expected:` block MUST embed the EXACT verification command from 06-VALIDATION.md / 06-RESEARCH.md §Code Examples — do NOT summarize, do NOT re-paraphrase, copy verbatim.**

```markdown
### 1. privacy@useQuibly.com mailbox provisioned and reachable (HARD launch-gate)
expected: |
  Founder action item — Phase 5 CD-07 / D-02 carryover. Provision the
  privacy@useQuibly.com mailbox via one of: Resend Inbound forward, Google
  Workspace alias, ImprovMX, or Cloudflare Email Routing.
  Test: send a fresh email from an external address (e.g., a personal Gmail) to
  privacy@useQuibly.com with subject "DSAR provisioning test <date>".
  Observe: founder receives the email in the destination inbox within 60 seconds.
  Record date/time received and destination inbox name in this test's `note:`.
  HARD LAUNCH-GATE — production form MUST NOT be exposed to public traffic
  until this test is `pass`. Privacy-policy DSAR contract violation otherwise.
result: pending

### 2. Production apex resolves to quibly-landing prod deploy (DEPLOY-01)
expected: |
  After production deploy + apex bind: from a fresh terminal, run:
    curl -sI https://useQuibly.com | head -1
  Expected: HTTP/2 200
  Then: curl -s https://useQuibly.com | grep -c Quibly
  Expected: a count > 0 (Quibly brand markers present in body)
  Record both command outputs in this test's `note:` field.
result: pending

### 3. Apex domain bound at Vercel team level (DEPLOY-02)
expected: |
  Vercel Dashboard → switch scope to the team that owns quibly-landing →
  Domains tab. Confirm `useQuibly.com` is listed there with project assignment
  to `quibly-landing`. Take a screenshot of the team Domains tab showing the
  apex listed. Save screenshot at:
    .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-03-team-domains.png
  Record screenshot path in this test's `note:` field.
result: pending

### 4. SPF + 3× DKIM + DMARC p=none + Return-Path DNS records resolve (DEPLOY-03, DEPLOY-04)
expected: |
  From a fresh terminal, run all of:
    dig +short ns useQuibly.com
    dig +short txt useQuibly.com | grep spf1
    dig +short txt resend._domainkey.useQuibly.com
    dig +short txt _dmarc.useQuibly.com | grep 'p=none'
    dig +short cname send.useQuibly.com   # exact subdomain per Resend Dashboard
  Expected:
    - ns: 2+ nameserver records (vercel-dns or external — note which)
    - SPF: "v=spf1 include:_spf.resend.com ~all" (or Resend equivalent)
    - DKIM: 3 records across 3 selectors (resend._domainkey, plus 2 others
      named in Resend Dashboard) — each contains "v=DKIM1; k=rsa; p=<key>"
    - DMARC: "v=DMARC1; p=none; rua=mailto:..."
    - Return-Path: CNAME pointing to a Resend-issued host
  Paste each command's output verbatim into this test's `note:` field.
  Then: Resend Dashboard → Domains → useQuibly.com → confirm all DNS rows
  show green ✓ status. Take a screenshot. Save at:
    .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-04-resend-dns.png
result: pending

### 5. mail-tester.com 10/10 score from production apex sender (DEPLOY-05)
expected: |
  Visit https://www.mail-tester.com → copy the generated single-use email
  address. From the production apex form (https://useQuibly.com), submit a
  signup using the mail-tester address (the single-use address acts as a
  fresh inbox). Wait for the welcome email to arrive at mail-tester (check
  Resend Dashboard → Logs to confirm send), then on mail-tester.com click
  "Then check your score". Expected: 10/10. If <10, debug the specific
  failed check (SPF / DKIM / DMARC / content) before exposing form publicly.
  Paste the mail-tester result URL into this test's `note:`. Save screenshot at:
    .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-05-mailtester.png
result: pending

### 6. Production real-signup writes to production audience + welcome arrives in Gmail
expected: |
  From a fresh Gmail inbox (or any real test inbox), submit a fresh email
  through the production form at https://useQuibly.com. Verify:
    a) Form replaces with success message ("You're on the list…")
    b) Resend Dashboard → Audiences → "Quibly Waitlist" (production) → new
       contact row appears with the test email and current created_at
    c) Welcome email arrives at the test inbox within 60 seconds
    d) From: renders as "Quibly <hello@useQuibly.com>"
    e) Subject: "You're on the Quibly list"
    f) Body: 4 D-01 paragraphs render correctly, footer shows real postal
       address (not placeholder), unsubscribe link present
    g) Click unsubscribe → /unsubscribe?t=<token> returns 200 → contact in
       Resend Dashboard now shows unsubscribed: true
  Record test email + Resend contact id + timestamps in this test's `note:`.
result: pending

### 7. Five hardening headers emit on production apex (DEPLOY-06)
expected: |
  From a fresh terminal, run:
    curl -sI https://useQuibly.com | grep -iE "strict-transport-security|x-content-type-options|x-frame-options|referrer-policy|permissions-policy"
  Expected: 5 lines (case may vary), exact values:
    strict-transport-security: max-age=300
    x-content-type-options: nosniff
    x-frame-options: DENY
    referrer-policy: strict-origin-when-cross-origin
    permissions-policy: camera=(), microphone=(), geolocation=(), interest-cohort=()
  Then verify HSTS is EXACTLY max-age=300 (no preload, no includeSubDomains):
    curl -sI https://useQuibly.com | grep -i strict-transport
  REJECT if response contains "; includeSubDomains" or "; preload".
  Verify sub-routes also receive headers:
    curl -sI https://useQuibly.com/robots.txt | grep -i strict-transport
    curl -sI https://useQuibly.com/sitemap.xml | grep -i strict-transport
    curl -sI https://useQuibly.com/opengraph-image | grep -i strict-transport
    curl -sI https://useQuibly.com/privacy | grep -i strict-transport
    curl -sI https://useQuibly.com/terms | grep -i strict-transport
  All 5 sub-routes must emit the HSTS header (source: '/(.*)' covers all).
  Paste full output in this test's `note:` field.
result: pending

### 8. No Service Worker registered on production load (DEPLOY-07)
expected: |
  Per CD-03 (manual DevTools checkpoint chosen over CI grep / Playwright spec).
  Steps:
    1. Open https://useQuibly.com in a fresh incognito/private window
    2. Open DevTools (Cmd-Opt-I / Ctrl-Shift-I)
    3. Application tab → Storage section → Service Workers
    4. Confirm panel is empty for the useQuibly.com origin (no entries; or
       only "Service workers from other origins" header with no rows beneath)
    5. Belt-and-suspenders source check (optional): from the repo, run:
         grep -rEn "navigator\\.serviceWorker|register\\s*\\(.*sw\\.|register\\s*\\(.*service-worker" app/ lib/ components/ 2>/dev/null
       Expected: zero output lines.
  Take a screenshot of the empty Service Workers panel. Save at:
    .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-08-no-sw.png
result: pending

### 9. Production OG / sitemap / robots / favicon smoke (Phase 5 carryover re-verify against prod)
expected: |
  From a fresh terminal, run:
    curl -sI https://useQuibly.com/opengraph-image | head -1   # → HTTP/2 200
    curl -s https://useQuibly.com/sitemap.xml                  # → valid XML, includes useQuibly.com/, /privacy, /terms
    curl -s https://useQuibly.com/robots.txt                   # → 10 AI-crawler Allow rules + Sitemap line
    curl -sI https://useQuibly.com/icon | head -1              # → HTTP/2 200
    curl -sI https://useQuibly.com/apple-icon | head -1        # → HTTP/2 200
  Paste each command's output in this test's `note:` field.
result: pending

### 10. Resend Audience CSV export includes consent_version column (Pitfall 6 / A1 empirical)
expected: |
  Resend Dashboard → Audiences → "Quibly Waitlist" (production) → Export
  Contacts → CSV. If audience <1000 contacts: download starts immediately;
  if ≥1000: link arrives via email (7-day expiry, admin-only).
  Open the downloaded CSV and verify the column header row contains AT MINIMUM:
    id, email, created_at, unsubscribed, consent_version
  If consent_version column is MISSING:
    - Mark this test as result: fail with severity: blocker
    - Document the gap: cutover.md Step 2 must invoke the API-fallback
      snapshot script path (06-RESEARCH.md §Code Examples lines 609–626)
      before any future cutover proceeds
    - This DOES NOT block today's go-live (cutover is future-only)
  Record column list + audience contact count in this test's `note:` field.
result: pending

### 11. Cutover dry-run transfer back-and-forth on staging.useQuibly.com (DEPLOY-09 / D-05/D-06/D-07)
expected: |
  Pre-flight: dig +short ns useQuibly.com → if response is ns1.vercel-dns.com
  / ns2.vercel-dns.com, sub-flow A (auto-CNAME) applies. Else sub-flow B
  (manual CNAME at external provider: staging → cname.vercel-dns.com).
  Steps (verbatim 06-RESEARCH.md lines 635–671):
    1. Vercel Dashboard → quibly-landing → Settings → Domains → Add Domain
       → enter staging.useQuibly.com → confirm
       (auto-CNAME if Vercel NS, else add CNAME at external provider first)
       Wait until status shows "Valid Configuration".
       Smoke test: curl -sI https://staging.useQuibly.com | head -5 → 200
       SCREENSHOT 1: staging.useQuibly.com bound to quibly-landing,
       Valid Configuration. Save at:
         .planning/phases/06-production-deploy-cutover-runbook/screenshots/06-uat-11-1-bound.png
    2. Vercel Dashboard → marketing-app → Settings → Domains → Add Domain
       → enter staging.useQuibly.com → in-use prompt: "This domain is
       currently in use by another project. Move it here?" → Confirm
       SCREENSHOT 2: capture the in-use prompt verbatim. Save at:
         06-uat-11-2-prompt.png
       Smoke test: curl -sI https://staging.useQuibly.com | head -5 → 200
       served from marketing-app
       SCREENSHOT 3: marketing-app loading at staging.useQuibly.com. Save at:
         06-uat-11-3-marketing-load.png
    3. Vercel Dashboard → quibly-landing → Settings → Domains → Add Domain
       → enter staging.useQuibly.com → in-use prompt → Confirm (transfer back)
       Smoke test: curl -sI https://staging.useQuibly.com | head -5 → 200
       served from quibly-landing
       SCREENSHOT 4: quibly-landing serving at staging.useQuibly.com. Save at:
         06-uat-11-4-back-to-landing.png
  Record exact button label observed (Vercel UI may say "Move", "Transfer",
  or other variant). Update docs/cutover.md Step 5 if the recorded label
  differs from the runbook prose. Note approximate transfer time (target <5s).
  All 4 screenshots MUST land in the screenshots/ directory.
  D-07 SCOPE: do NOT write to Resend during this dry-run; do NOT configure
  a separate staging sender. Mechanics + smoke load only.
result: pending

### 12. Apex unaffected after dry-run completes
expected: |
  After test 11 completes: from a fresh terminal, run:
    curl -sI https://useQuibly.com | head -1
  Expected: HTTP/2 200 from quibly-landing (the production apex was untouched
  by the staging.useQuibly.com transfer flow — different subdomain).
  curl -sI https://useQuibly.com | grep -i strict-transport
  Expected: strict-transport-security: max-age=300 (still emits, dry-run
  did not regress the headers config).
result: pending
```

**`## Summary` block** (12 tests total, all initially pending):
```markdown
## Summary

total: 12
passed: 0
issues: 0
pending: 12
skipped: 0
blocked: 0
note: "Phase 6 launch-gating checklist; populated by Plan 06-04 (dry-run, tests 10–12) and Plan 06-05 (production go-live, tests 1–9 + 12)."
```

**`## Gaps` block** (empty placeholder, populated only if any test fails):
```markdown
## Gaps

[populated only after execution if any test result is `issue` or `fail` — schema per 05-UAT.md lines 133–212]
```

Constraints:
- DO NOT mark any test as pass/fail/issue at this plan stage. ALL tests start `pending`.
- DO NOT skip the privacy@useQuibly.com test — it is the HARD launch-gate (D-02).
- DO NOT skip the consent_version CSV-export test — it is the Pitfall 6 / A1 empirical verification (gates cutover.md Step 2 fallback decision).
- DO NOT use any `result:` value other than `pending`, `pass`, `fail`, `issue`, `skipped`, `blocked` (matches 05-UAT.md schema).
- DO NOT add tests for Phase 4/5 items already verified (postal address, Outlook/iCloud, zero-cookie, OG validation against preview) per D-02.
- DO NOT add today's-launch-checklist items to docs/cutover.md (D-03 — those live HERE).
- The screenshots/ directory does NOT need to be created in this plan — Plans 06-04 and 06-05 create the directory and files when capturing screenshots during execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && grep -c '^### ' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md | grep -q '^12$' && grep -c 'result: pending' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md | awk '{ if ($1 >= 12) exit 0; else exit 1 }' && grep -q 'privacy@useQuibly.com' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && grep -q 'HARD LAUNCH-GATE' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && grep -q 'max-age=300' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && ! grep -E 'preload|includeSubDomains' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && grep -q 'consent_version' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && grep -q 'staging.useQuibly.com' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md && grep -q 'serviceWorker\|Service Workers' .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md</automated>
  </verify>
  <acceptance_criteria>
- `test -f .planning/phases/06-production-deploy-cutover-runbook/06-UAT.md` returns success
- File starts with `---` YAML frontmatter, contains `phase: 06-production-deploy-cutover-runbook`, `status: pending`
- `grep -c '^### ' 06-UAT.md` returns `12` (exactly 12 numbered tests)
- `grep -c 'result: pending' 06-UAT.md` returns ≥12 (all tests start pending)
- `grep -c '^expected: |' 06-UAT.md` returns ≥12 (each test has expected block-scalar)
- `grep -q 'privacy@useQuibly.com' 06-UAT.md` → matches (test 1 / LEGAL-08 carryover)
- `grep -q 'HARD LAUNCH-GATE' 06-UAT.md` → matches (test 1 marker)
- `grep -q 'curl -sI https://useQuibly.com' 06-UAT.md` → matches (DEPLOY-01/06 verifications)
- `grep -q 'dig +short' 06-UAT.md` → matches (DEPLOY-04 DNS verifications)
- `grep -q 'mail-tester' 06-UAT.md` → matches (DEPLOY-05)
- `grep -q 'max-age=300' 06-UAT.md` → matches (HSTS literal value present in test 7)
- `! grep -E 'preload|includeSubDomains' 06-UAT.md` → success (FORBIDDEN tokens absent)
- `grep -q 'consent_version' 06-UAT.md` → matches (Pitfall 6 / A1 test 10)
- `grep -q 'staging.useQuibly.com' 06-UAT.md` → matches (test 11 dry-run subject)
- `grep -c 'screenshots/06-uat-' 06-UAT.md` returns ≥6 (test 3, 4, 5, 8, 11×4 screenshot path references)
- `grep -q 'X-Frame-Options' 06-UAT.md` → matches (header verification test)
- `grep -q 'X-Content-Type-Options' 06-UAT.md` → matches
- `grep -q 'Referrer-Policy' 06-UAT.md` → matches
- `grep -q 'Permissions-Policy' 06-UAT.md` → matches
- All 9 DEPLOY-XX requirement IDs are referenced in test bodies — verify with: `for n in 01 02 03 04 05 06 07 08 09; do grep -q "DEPLOY-$n" 06-UAT.md || echo "MISSING DEPLOY-$n"; done` returns no MISSING lines
- `grep -c '^## Summary' 06-UAT.md` returns 1
- `grep -c '^## Gaps' 06-UAT.md` returns 1
- `grep -q 'total: 12' 06-UAT.md` → matches (Summary count correct)
- `grep -q 'pending: 12' 06-UAT.md` → matches (all tests pending)
- No file modifications outside `.planning/phases/06-production-deploy-cutover-runbook/06-UAT.md` (`git diff --name-only` shows only this file)
- `grep -q 'Service Workers' 06-UAT.md` → matches (test 8 / DEPLOY-07 / CD-03)
- `grep -q 'Resend Dashboard' 06-UAT.md` → matches (Resend manual workflows referenced)
  </acceptance_criteria>
  <done>
06-UAT.md exists with frontmatter + 12 tests + Summary + Gaps. All tests start `result: pending`. Format matches 05-UAT.md exactly. Test 1 is the HARD launch-gate for `privacy@useQuibly.com`. Test 7 verifies the 5 hardening headers wire-format with HSTS literal `max-age=300` (no preload/includeSubDomains). Test 8 verifies no Service Worker via DevTools (CD-03). Test 10 is the Pitfall 6 / consent_version empirical CSV inspection. Test 11 is the 4-screenshot dry-run cutover. Plans 06-04 and 06-05 will flip these tests to pass/fail/issue during execution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (founder) → external services (Vercel, Resend, mail-tester, DNS provider, mailbox host) | Manual checkpoint operations require trust in human-recorded evidence (screenshots, command output paste-backs) |
| 06-UAT.md → production go-live decision | The artifact gates production exposure; missing checkpoints = unverified launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-V8-DSAR-MISS | Information Disclosure (DSAR contract violation) | privacy@useQuibly.com mailbox | mitigate | Test 1 is the HARD launch-gate; production form MUST NOT be public until test 1 = pass. ASVS V8.x. |
| T-06-V14-MISSING-EVIDENCE | Repudiation (operator claims test passed without evidence) | screenshot artifacts | mitigate | Each manual-only test specifies an exact screenshot path under `.planning/phases/06-.../screenshots/`. Plan 06-05 verifies file existence as part of its acceptance criteria. ASVS V14.x. |
| T-06-V8-AUDIT-CSV | Information Disclosure (GDPR audit trail loss on cutover) | Resend CSV export | mitigate | Test 10 is the Pitfall 6 / A1 empirical verification before any future cutover; if `consent_version` missing, test fails and gates Step 2 fallback in cutover.md. ASVS V8.x. |
| T-06-V9-WIRE-FMT | Tampering (HSTS preload accidentally shipped to production) | next.config.ts deploy | mitigate | Test 7 verifies wire-format `max-age=300` literal (no preload/includeSubDomains) via `curl -sI`. Belt-and-suspenders for the 06-01 plan's source-level constraint. ASVS V9.1.x. |
| T-06-V9-SW-PERSIST | Denial of Service (cutover atomicity broken by SW caching) | DevTools Service Workers panel | mitigate | Test 8 manual DevTools checkpoint (CD-03). Belt-and-suspenders source grep referenced in expected: block. ASVS V9.x. |
</threat_model>

<verification>
1. File exists, has frontmatter matching 05-UAT.md schema.
2. Exactly 12 tests, all `result: pending`.
3. All 9 DEPLOY-XX IDs referenced in test bodies.
4. HARD launch-gate marker on test 1 (privacy mailbox).
5. HSTS literal `max-age=300` referenced; no preload/includeSubDomains tokens anywhere.
6. consent_version explicitly tested.
7. Service Workers panel test present (CD-03).
8. Dry-run test references 4 screenshot paths.
9. Summary totals match (total: 12, pending: 12).
</verification>

<success_criteria>
- 06-UAT.md exists with the canonical sibling format (frontmatter + Tests + Summary + Gaps).
- Plans 06-04 and 06-05 can flip individual `result: pending` to `result: pass` (or fail/issue) during execution without restructuring the file.
- All Phase 6 manual-only verifications from 06-VALIDATION.md are represented as test rows with verbatim verification commands.
- The HARD launch-gate (privacy mailbox) is unambiguous; downstream plans cannot accidentally bypass it.
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-deploy-cutover-runbook/06-03-SUMMARY.md` documenting:
- Final 06-UAT.md test count (should be 12)
- The 12 test titles in order (paste from `grep '^### ' 06-UAT.md`)
- Confirmation that all tests start `pending`
- Confirmation that no anti-pattern tokens (preload, includeSubDomains) appear
</output>
