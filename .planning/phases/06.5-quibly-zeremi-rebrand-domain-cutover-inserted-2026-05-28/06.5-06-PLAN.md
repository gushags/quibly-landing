---
phase: 06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28
plan: 06
type: execute
wave: 5
depends_on:
  - 06.5-04
  - 06.5-05
files_modified: []
autonomous: false
requirements:
  - D-07
  - D-08
  - CD-01
  - SC-4
user_setup:
  - service: resend
    why: "Verify zeremi.app sender domain; rename Quibly Waitlist audiences to Zeremi Waitlist"
    env_vars: []
    dashboard_config:
      - task: "Add zeremi.app as a sender domain; verify SPF + 3× DKIM + DMARC TXT + Return-Path"
        location: "Resend Dashboard -> Domains -> Add Domain"
      - task: "Rename 'Quibly Waitlist' audience to 'Zeremi Waitlist' (D-08)"
        location: "Resend Dashboard -> Audiences -> Quibly Waitlist -> Settings -> Name"
      - task: "Rename 'Quibly Waitlist (Preview)' to 'Zeremi Waitlist (Preview)' (D-08)"
        location: "Resend Dashboard -> Audiences -> Quibly Waitlist (Preview) -> Settings -> Name"
  - service: dns
    why: "Add zeremi.app DNS records for Resend sender verification (CD-01 — provider depends on registrar/Vercel)"
    env_vars: []
    dashboard_config:
      - task: "Pre-flight check: dig +short ns zeremi.app — determine whether Vercel-managed or external nameservers"
        location: "Terminal"
      - task: "Add 5 DNS records (SPF TXT, 3× DKIM CNAME, DMARC TXT) per Resend Dashboard instructions"
        location: "DNS provider control panel (Vercel-managed if dig shows ns*.vercel-dns.com)"
must_haves:
  truths:
    - "zeremi.app is a verified sender domain in Resend with all 5 DNS records green ✓ (SPF + 3× DKIM + DMARC + Return-Path)"
    - "A welcome-email round-trip from hello@zeremi.app scores ≥9/10 on mail-tester.com (SC #4)"
    - "Resend audience formerly named 'Quibly Waitlist' is renamed to 'Zeremi Waitlist'; AUDIENCE_ID env value is unchanged (D-08)"
    - "Resend audience formerly named 'Quibly Waitlist (Preview)' is renamed to 'Zeremi Waitlist (Preview)'; AUDIENCE_PREVIEW_ID env value is unchanged"
    - "useQuibly.com sender domain still exists in Resend at the time of merge — its deletion happens inside the edited 06-05-PLAN.md after Phase 6.5 lands (D-07 gated by ≥9/10 mail-tester from zeremi.app)"
  artifacts: []
  key_links:
    - from: app/actions/join-waitlist.ts (post-plan-04)
      to: Resend verified sender on zeremi.app
      via: "From: 'Jeff at Zeremi <hello@zeremi.app>'"
      pattern: "hello@zeremi\\.app"
---

<objective>
Wave 5: human-gated external-service verification. Add `zeremi.app` as a Resend sender domain, complete DNS verification (SPF + 3× DKIM + DMARC + Return-Path), achieve ≥9/10 on mail-tester.com from a hello@zeremi.app send, and rename both Resend audiences (production + preview) per D-08.

This plan is `autonomous: false` because the work happens in the Resend Dashboard and DNS provider control panel — Claude cannot do these via CLI/API in the project's current configuration. Per CD-01 the DNS provider must be determined at execution time (Vercel-managed nameservers vs external) via `dig +short ns zeremi.app`.

This plan does NOT delete the `useQuibly.com` sender from Resend. That deletion is intentionally deferred to inside the edited `06-05-PLAN.md` (D-06/D-07/CD-07), gated by the production deploy having mail-tester ≥9/10 already verified from `zeremi.app`. Per CONTEXT scope-boundary: "useQuibly.com sender stays active until zeremi.app is verified".

Output: External services configured; mail-tester score recorded; audience names flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md
@.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-VALIDATION.md
@.planning/phases/04-resend-wiring-bot-protection-welcome-email/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: DNS pre-flight + add zeremi.app sender domain in Resend</name>
  <files></files>
  <read_first>
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Resend Sender Domain Re-Verification — 5 DNS record types and selectors; §Open Question 2 — CD-01 nameserver provider)
  </read_first>
  <what-built>Phase 6.5 plans 01–05 have already swept the codebase to send from `hello@zeremi.app`. This task verifies the Resend dashboard side: add the new sender, fetch the 5 DNS records, and write them at the registered DNS provider.</what-built>
  <how-to-verify>
    1. Run `dig +short ns zeremi.app` in a terminal. Record the result.
       - If output contains `vercel-dns.com` (Vercel-managed nameservers, mirrors Phase 6 CD-05 pattern for useQuibly.com): DNS edits happen in Vercel Dashboard → Domains.
       - Else: DNS edits happen at whichever registrar/external provider hosts the zone.
    2. Open Resend Dashboard → Domains → Add Domain. Enter `zeremi.app`.
    3. Resend returns 5 DNS records: 1× SPF TXT on `zeremi.app` (`v=spf1 include:_spf.resend.com ~all`), 3× DKIM CNAME (Resend-generated selectors), 1× DMARC TXT on `_dmarc.zeremi.app` (`v=DMARC1; p=none; rua=mailto:...`), 1× Return-Path CNAME on `send.zeremi.app` (or similar Resend-specified subdomain).
    4. Add all 5 records at the DNS provider identified in step 1.
    5. Click "Verify" in the Resend Dashboard. Wait for all rows to show green ✓ (propagation usually <5 min for Vercel-managed; up to 24h for external — though typically much faster).
    6. Sanity-check from terminal: `curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains | jq '.data[] | select(.name=="zeremi.app") | .status'` should return `"verified"`.
    7. Take a screenshot of the Resend Domains page showing `zeremi.app` with all 5 green checks — save to `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/evidence/06.5-06-1-resend-dns.png`.
  </how-to-verify>
  <resume-signal>Type "verified" after all 5 DNS records show green in the Resend Dashboard AND the API confirms `status: "verified"` for `zeremi.app`. Paste the screenshot path. If verification stalls (DNS propagation >24h, Resend reports persistent errors), type "blocked: &lt;reason&gt;" and a checker thread will be created.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: mail-tester ≥9/10 from hello@zeremi.app (SC #4)</name>
  <files></files>
  <read_first>
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md (SC #4 — ≥9/10 on mail-tester.com from hello@zeremi.app)
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-RESEARCH.md (§Mail deliverability warm-up — newly verified low-volume domain typically achieves ≥9/10 without multi-week warm-up)
  </read_first>
  <what-built>The Wave 3 plan 04 code path sends welcome email via `resend.emails.send({ from: 'Jeff at Zeremi <hello@zeremi.app>', ... })`. This task verifies deliverability of that exact From address.</what-built>
  <how-to-verify>
    1. Open https://www.mail-tester.com/ in a browser. Copy the test email address it generates (e.g. `test-abc123@mail-tester.com`).
    2. Trigger a welcome-email send to that test address from the production-equivalent environment. Two options:
       a. Easiest: from the running app (preview deploy or `npm run dev` with production Resend keys), enter the mail-tester address in the waitlist form and submit. The Server Action will call `resend.emails.send` with from `hello@zeremi.app`.
       b. Alternative: from a terminal with `RESEND_API_KEY` set, run a one-shot Node script invoking `resend.emails.send({ from: 'Jeff at Zeremi <hello@zeremi.app>', to: '<mail-tester-address>', subject: "You're on the Zeremi list", react: WelcomeEmail({...}) })` — match the production code path exactly.
    3. Return to mail-tester.com and click "Then check your score". Wait for the score to render.
    4. Record the score. Per SC #4: ≥9/10 is REQUIRED.
    5. If score is <9/10: review the mail-tester breakdown. Most common issues at this stage: missing or partial DKIM (re-check Task 1 records); SPF misalignment; DMARC misconfigured. Fix the offending records and re-test until ≥9/10.
    6. Take a screenshot of the mail-tester result page showing the score — save to `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/evidence/06.5-06-2-mailtester.png`.
  </how-to-verify>
  <resume-signal>Type "score: N/10" with the achieved score and the screenshot path. If N >= 9, proceed to Task 3. If N < 9, type "score: N/10 — blocked" and detail the failed mail-tester check (DKIM/SPF/DMARC/etc).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Rename Resend audiences in Dashboard (D-08)</name>
  <files></files>
  <read_first>
    - .planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-CONTEXT.md (D-08 — rename both audiences in lockstep; AUDIENCE_ID values UNCHANGED)
  </read_first>
  <what-built>Plan 03 already updated `lib/env.ts` Zod error message to reference "Zeremi Waitlist". This task aligns the actual Dashboard names.</what-built>
  <how-to-verify>
    1. Resend Dashboard → Audiences. Locate the audience currently named `Quibly Waitlist` (this is the production audience whose ID is in `RESEND_AUDIENCE_ID`).
    2. Open its Settings (gear icon or context menu). Rename to `Zeremi Waitlist`. Save.
    3. Repeat for `Quibly Waitlist (Preview)` → `Zeremi Waitlist (Preview)`.
    4. Verify the AUDIENCE_ID env value is unchanged: the Resend dashboard should show the same audience UUID for the renamed audience as before. (Per Resend docs, renaming preserves IDs — D-08 invariant.)
    5. Optional sanity: `curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/audiences | jq '.data[] | select(.id=="<production audience id>")'` should return name `"Zeremi Waitlist"`.
    6. Take a screenshot of the Audiences list showing both renamed audiences — save to `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/evidence/06.5-06-3-audiences.png`.
  </how-to-verify>
  <resume-signal>Type "renamed" when both audiences show their new names in the Dashboard AND the API confirms the rename. Paste the screenshot path.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Resend API | The `from:` address `hello@zeremi.app` must be a verified Resend sender domain; an unverified domain causes Resend to reject the send with `Domain not verified` |
| Resend → recipient MTA | DKIM signature must validate at the recipient — SPF + DKIM + DMARC alignment is the deliverability gate (≥9/10 mail-tester) |
| Old sender keep-alive | useQuibly.com sender STAYS active during Phase 6.5 — D-05/D-07 schedule its deletion inside the edited 06-05 plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-06-01 | Spoofing | Unverified zeremi.app sender → Resend rejects send | mitigate | Task 1 blocking gate: all 5 DNS records green ✓ in Resend Dashboard + API `status: "verified"` confirmed before proceeding. |
| T-065-06-02 | Spoofing | Deliverability under threshold (DKIM/SPF/DMARC misalignment lets spoofed messages through) | mitigate | Task 2 hard gate: mail-tester ≥9/10 BEFORE the merge to main. |
| T-065-06-03 | Repudiation | Audience rename loses contact metadata or breaks AUDIENCE_ID env wiring | mitigate | D-08 invariant: Resend rename preserves UUIDs; Task 3 sanity check via API confirms IDs unchanged. |
| T-065-06-04 | Denial of Service | Deleting useQuibly.com sender too early (before zeremi.app verified) blocks ALL sends | mitigate | useQuibly.com sender deletion is DEFERRED to 06-05 plan edits (Wave 6 plan 07), gated by Task 2 score ≥9/10. This plan does NOT delete it. |
| T-065-06-05 | Tampering | DNS records pasted with typo at provider → silent verification failure | mitigate | Acceptance signal "verified" requires both green ✓ in Resend Dashboard AND API status check via curl + jq. |
</threat_model>

<verification>
- `dig +short ns zeremi.app` recorded.
- All 5 DNS records green in Resend Dashboard; API reports `status: "verified"` for zeremi.app.
- mail-tester score ≥9/10 from hello@zeremi.app.
- Both audiences renamed to "Zeremi Waitlist" / "Zeremi Waitlist (Preview)"; AUDIENCE_ID values unchanged.
- 3 screenshots saved under `.planning/phases/06.5-.../evidence/`.
- No code commits in this plan (manual external service work only).
</verification>

<success_criteria>
- SC #4 met: Resend zeremi.app sender verified + mail-tester ≥9/10.
- D-08 satisfied: both audiences renamed; env values unchanged.
- useQuibly.com sender remains intact (deletion deferred to 06-05 per D-07).
</success_criteria>

<output>
Create `.planning/phases/06.5-quibly-zeremi-rebrand-domain-cutover-inserted-2026-05-28/06.5-06-SUMMARY.md` when done. Include the recorded mail-tester score and the three screenshot paths.
</output>
